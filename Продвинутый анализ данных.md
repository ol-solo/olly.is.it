---
type: note
aliases:
  - "? Продвинутый анализ данных"
tags: []
parent: [[Python для анализа данных]]
next:
prev:
---
# Получение и сохранение данных
## Работа с файлами
В процессе работы бывает нужно проанализировать произвольные файлы с текстовой информацией или поработать с файлами внутри архива. В этом уроке мы узнаем, как функция `open` помогает читать и записывать файлы, научимся работать с файловой системой, а также поймём, как работать с архивными файлами через модуль `zipfile`. Это поможет понять, как аналитики могут организовывать и хранить данные.

### Открываем файлы

Функция `open` в Python используется для открытия файла. Она обращается к файловой системе и создаёт объект, который предоставляет методы для чтения или записи. Эта функция принимает два основных аргумента: имя файла и режим открытия, такой как `'r'` (read) для чтения, `'w'` (write) для полной перезаписи содержимого файла или его создания, `'a'` (append) для того, чтобы добавить содержимое в конец файла. Открыть и прочитать содержимое небольшого файла можно так:

```
file = open('example.txt', 'r')
content = file.read()
print(content)
file.close() 
```

В этом случае после чтения содержимого файла связанный с ним объект необходимо закрыть вызовом `file.close()` для освобождения системных ресурсов. Так вы показываете программе, что больше не будете обращаться к файлу.

Конструкция `with...as` упрощает управление файлами, обеспечивая автоматическое закрытие файла после выхода из блока `with`. Это делает код более чистым и безопасным:

```
with open('example.txt', 'r') as file:
    content = file.read()
    print(content) 
```

Этот код имеет такое же действие, как и код выше. Здесь файл автоматически закрывается после завершения чтения, и не нужно беспокоиться о закрытии файла вручную.

### Создаём текстовый файл

Иногда бывает необходимо создать файл с произвольным содержимым и сохранить его на диск. Откроем и перезапишем или создадим файл при помощи функции`open` в режиме записи (`"w"`) и сохраним в него имеющийся у нас текст:

```
content = """Ежедневный отчёт по продажам:
Дата: 21 мая 2024 года
Общее количество продаж: 150
Общая сумма продаж: 25,000

Основные категории товаров:
1. Электроника - 50 продаж
2. Одежда - 30 продаж
3. Бытовая техника - 70 продаж

Замечания:
- Увеличение продаж бытовой техники на 20% по сравнению с предыдущим днём.
- Снижение продаж одежды на 10%."""

with open('example.txt', 'w') as file:
    file.write(content) 
```

В данном случае создаётся файл `example.txt` и в него записывается текст из переменной `content`. Функция `write` записывает текст в файл, а использование конструкции `with...as` нужно, чтобы файл корректно закрылся после завершения записи.

### Построчная обработка данных

Не всегда полностью читать файл это хорошая идея, особенно если файл очень большой. У нас есть логи сервера `server.log`, если логи собираются за длительный срок, они могут достигать значительных объёмов. Чтобы понять, как автоматически проанализировать логи построчно, откроем такой файл со следующим содержимым:

```
2024-05-14 08:00:00 INFO Server started.
2024-05-14 08:15:23 ERROR Failed to connect to database.
2024-05-14 09:00:17 INFO New user registered.
2024-05-14 09:17:45 ERROR Timeout occurred processing request. 
```

Для того, чтобы перебрать строки файла, необходимо начать цикл, итерируясь по объекту файла. Внутри цикла мы перебираем каждую строку файла, проверяя наличие слова "ERROR". Если строка содержит это слово, она добавляется в список `errors` после удаления пробелов и символов новой строки с помощью метода `strip()`. В полученном списке подсчитываем число элементов и выводим его на экран:

```
errors = []
with open('server.log', 'r') as file:
    for line in file:
        if "ERROR" in line:
            errors.append(line.strip())

print(f"Найдено ошибок: {len(errors)}") 
```

```
Всего ошибок в логах: 2 
```

### Открываем много файлов

Не всегда мы знаем точно название файла, который нам нужно открыть. Также, зачастую в процессе работы приходится иметь дело не с одним и не с двумя файлами, а с большим их количеством. Например, каждый день филиал присылает файл, имя которого начинается с `report_` и заканчивается на `.xlsx` и нам нужно обработать все эти файлы. Сначала найдём все такие файлы. В этом нам поможет библиотека `os`. Её метод `listdir` позволяет получить содержимое указанной папки. Строкой из одной точки обычно обозначается текущая папка.

```
import os

reports = [f for f in os.listdir('.') 
            if f.startswith('report_') and f.endswith('.xlsx')] 
```

В переменную `reports` теперь помещён список названий файлов `['report_2024-05-06.xlsx', 'report_2024-05-07.xlsx', 'report_2024-05-08.xlsx', 'report_2024-05-09.xlsx', 'report_2024-05-10.xlsx']` с названиями ежедневных отчётов за предыдущую неделю. Теперь мы можем обработать их в цикле необходимым образом.

Для открытия отчётов нам потребуется метод Pandas `read_excel`. Отчёт типовой, сверху у него шапка, снизу место для подписей сотрудников, посередине данные. Пропустить строки шапки нам поможет параметр `skiprows`, он принимает список строк, начиная с нуля, которые нужно пропустить. Пропустить окончание документа нам поможет параметр `skipfooter`, он принимает одно число — количество строк в конце документа, которые не нужно загружать.

У отчёта много объединённых ячеек и не каждый столбец содержит данные. Так как нужные нам столбцы у отчёта не меняются, мы указываем их в нотации Excel в параметре `usecols`, где можем использовать как отдельные столбцы, так и их диапазоны. После загрузки отчёта метод `set_axis` с параметром `axis=1` позволит нам задать названия столбцам загружаемого датафрейма. Важно изменять список новых названий согласованно со значением, передаваемым в `usecols`. Весь пример кода загрузки датасетов в цикле целиком:

```
import pandas as pd

# Для каждого названия файла отчёта
for report in reports:
    # Открываем файл с таблицей
    df_raw = pd.read_excel(
        report,
        # Пропускаем шапку отчёта из 35 строк
        skiprows=range(35),
        # Пропускаем последние 10 строк документа
        skipfooter=10,
        # Используем заранее известные столбцы
        usecols="K:L,AY,BF,BU,CJ")\
        .set_axis(
            # Задаём названия оси таблицы 
            [   "Раздел", 
                "Показатель", 
                "Код показателя", 
                "Текущий год", 
                "Предыдущий год", 
                "Предшествующий предыдущему"],
            # Заменяем названия колонок
            axis=1) 
```

Далее нам может потребоваться предобработка данных и расчёт каких-то показателей. Информацию можно брать не только из таблицы: само имя файла `report` у нас содержало дату — её можно извлечь и сохранить, чтобы потом анализировать динамику метрик.

### Работаем с архивами

Библиотека `zipfile` в Python предоставляет возможности для работы с ZIP-архивами, включая возможности их создания, чтения, записи, а также извлечения содержимого. Допустим, что в конце предыдущего раздела мы подготовили сводный отчёт и сохранили его на диске под названием `consolidated_report.xlsx`. Этот файл вместе с отчётами за отдельные дни нам нужно добавлять в архив. Так как предыдущие действия мы автоматизировали, попробуем автоматизировать и это! Помня, что файлы продолжают лежать в той же папке, в которой мы запускаем скрипт, сделаем это при помощи модуля `zipfile`:

```
import zipfile

with zipfile.ZipFile('reports_archive.zip', 'w', zipfile.ZIP_DEFLATED) as archive:
    for report in reports:
        archive.write(report)
    else:
        archive.write('consolidated_report.xlsx') 
```

Конструкция `else` после циклов отрабатывает в случае успешного завершения цикла. Ура, теперь всё сохранили, можем отсылать! Обратите внимание, `zipfile` по умолчанию не сжимает файлы при создании архива, поэтому мы указываем тип сжатия `zipfile.ZIP_DEFLATED`.

### Автоматизация процессов с помощью чтения ZIP-архивов

Текстовые форматы хранения данных, например формат `csv`, могут очень быстро прибавлять в объёме по мере добавления данных, но они обычно хорошо сжимаются. Очень часто такие файлы распространяются в запакованном виде.

Выше мы записывали файлы в архив, но архивы иногда бывает необходимо читать. Используем библиотеку `zipfile` для этого.

Для более надёжной и безопасной работы с файлами и архивами используем использовавшуюся выше конструкцию `with...as`, причём сделаем её вложенной. Конструкция гарантирует, что файл и архив будут корректно закрыты после завершения нужных операций.

Например, внутри архива расположен файл `purchases.csv`, который выглядит следующим образом:

```
PurchaseID,Amount
001,25.50
002,45.00
003,17.75
004,34.00 
```

Используем модуль `zipfile` для открытия архива `monthly_data.zip` с помощью конструкции `with...as`. Затем внутри архива открываем файл `purchases.csv`. Первую строку файла, содержащую заголовки, проверяем с помощью функции `next(file)` и конструкции `assert` на соответствие ожидаемому значению. В случае несоответствия заголовков `assert` выведет сообщение об ошибке. Файл для Python — это набор байтов, как и отдельные его части, поэтому мы ставим `b` перед строкой.

Используя генераторное выражение внутри функции `sum`, подсчитываем количество строк, которое соответствует количеству покупок. В конце результат выводится на экран.

```
import zipfile

# Открытие ZIP-архива и чтение содержимого CSV-файла внутри
with zipfile.ZipFile('purchases.zip', 'r') as archive:
    with archive.open('purchases.csv') as file:
        assert next(file) == b'PurchaseID,Amount\n', \
            'Заголовки файла не соответствуют ожидаемым'
        # Подсчёт количества строк и покупок
        total_purchases = sum(1 for _ in file)

print(f"Общее количество покупок: {total_purchases}") 
```

Мы научились добавлять файлы в архивы и открывать текстовые файлы в Python, даже если они запакованы. Если есть возможность импортировать библиотеку pandas и в архиве лежит всего один файл csv, то можно использовать привычный метод `read_csv`. Однако для других случаев удобно знать другие методы открытия текстовых данных.

### Запуск кода

C тем, **какой** код запускать мы разобрались, но чтобы автоматизация была удобной, надо придумать, **как** его запускать. Работа в Python может быть весьма разнообразной, как и задачи, которые решаются с его помощью.

Основными форматами файлов в Python считаются `.py`, они используются для создания и запуска собранных программ и проектов. Файлы `.ipynb` применяются для интерактивного выполнения кода и ведения журнала вычислений в удобном интерфейсе Jupyter Notebook, так как они удобнее для написания кода, особенно если его нужно кому-то передавать. Однако для запуска небольшого скрипта тетрадки Jupyter Notebook подходят хуже.

Если скрипт выше сохранён в файле `purchases.py` и на компьютере установлен Python, то достаточно в командной строке ввести `python purchases.py`, чтобы увидеть результат: `Общее количество покупок: 4`.

Вы можете использовать подходящие для вашей системы планировщики запуска скриптов для автоматизации рабочих задач.

## Работа с таблицами
Аналитика начинается с данных. Бывает, что они хранятся уже в готовых csv/tsv-файлах и вы используете метод `read_csv()`, чтобы превратить таблицы в датафреймы. Бывает, что данные хранятся в Excel-файле, но и это уже нестрашно и вы вызываете метод `read_excel()`.

Так или иначе, всё это время вы работали с таблицами, которые хранятся на вашем компьютере, перед этим скачивая их по ссылке.

Но что делать, если таблица с данными, которые вам нужны, хранится где-то в интернете, на веб-странице, а кнопки `скачать` просто нет?

### **В этом уроке вы узнаете**

- Как работать с данными из интернета и что интересного там можно найти.
- Зачем сохранять результаты в Excel и как это делать правильно.

### Сайты тоже хранят данные

Переходя по ссылке на любой из сайтов, ваш браузер всегда начинает с того, что скачивает файлы, это называют загрузкой сайта. Все файлы, которые скачивает браузер, делятся на несколько типов, вот самые важные:

- `html` — получая такой файл, браузер понимает, как должна выглядеть страница сайта для пользователя, и начинает её отрисовку. `html`-файл говорит браузеру, как именно нарисовать страницу сайта и что она будет содержать. По сути, это разметка с содержимым, как Markdown в jupyter-третрадке, но посложнее. Именно об этом файле мы сегодня поговорим.
- `png/jpg/gif` и прочее — это картинки, которые сопровождают вас почти всегда: логотип сайта, видеолендинг. Это называют ассетами.
- `js` — это те файлы, которые говорят браузеру, что нужно делать, когда начинаете взаимодействовать с сайтом: кликать, скроллить или печатать.

Всё содержимое страницы сайта хранится в `html`-файле, и выглядит он примерно так:

```
<!DOCTYPE html>
<html>
<head>
  <title>Мой первый веб-сайт</title>
</head>
<body>
  <header>
    <h1>Добро пожаловать на мой сайт!</h1>
    <nav>
      <ul>
        <li><a href="#">Главная</a></li>
        <li><a href="#">О нас</a></li>
        <li><a href="#">Услуги</a></li>
        <li><a href="#">Контакты</a></li>
      </ul>
    </nav>
  </header>
  
... 
```

Что же там может быть такого интересного для аналитика?

На деле вы часто встречаете **табличные** данные прямо на веб-страницах сайта, просто не обращаете на это внимания, особенно когда не можете их напрямую скачать. Вы точно пользовались Википедией, а ведь это самое большое хранилище самых разных интернет-таблиц.

Вот страница о [чемпионате России по футболу](https://ru.wikipedia.org/wiki/%D0%A7%D0%B5%D0%BC%D0%BF%D0%B8%D0%BE%D0%BD%D0%B0%D1%82_%D0%A0%D0%BE%D1%81%D1%81%D0%B8%D0%B8_%D0%BF%D0%BE_%D1%84%D1%83%D1%82%D0%B1%D0%BE%D0%BB%D1%83) в Википедии, и в ней хранится довольно много полезной информации. В том числе о количестве выигранных медалей каждым футбольным клубом.

![](https://pictures.s3.yandex.net/resources/image_1716808442.png)

Как бы нам сохранить это в датафрейм? Может, просто перепечатать? Есть способ получше!

Попробуем прочитать эти данные прямо с сайта, но используя Pandas. Для этого воспользуемся методом `read_html()`, указывая параметром только адрес URL-ссылки на страницу. Этот метод исследует html-файл страницы и пытается найти всё, что похоже на таблицу, и превратить в датафрейм. Строку подаём сразу в зашифрованном виде (`%D0%A7`…), скопировав из адресной строки браузера.

```
import pandas as pd

tables = pd.read_html('https://ru.wikipedia.org/wiki/%D0%A7%D0%B5%D0%BC%D0%BF%D0%B8%D0%BE%D0%BD%D0%B0%D1%82_%D0%A0%D0%BE%D1%81%D1%81%D0%B8%D0%B8_%D0%BF%D0%BE_%D1%84%D1%83%D1%82%D0%B1%D0%BE%D0%BB%D1%83')
 
```

На этой странице находится сразу несколько таблиц, и Pandas пытается сохранить их все по порядку в списке. Мы сохранили этот список в переменной `tables`.

```
len(tables) 
```

```
24 
```

На странице нашлось 24 таблицы!

Наша таблица под номером 5.

```
tables[5] 
```

||**Unnamed: 0**|**Клуб**|**Золото**|**Серебро**|**Бронза**|**Всего медалей**|
|---|---|---|---|---|---|---|
|**0**|1|Спартак (Москва)|10|6|4|20|
|**1**|2|Зенит|9|3|4|16|
|**2**|3|ЦСКА|6|9|3|18|
|**3**|4|Локомотив (Москва)|3|6|6|15|
|**4**|5|Рубин|2|-|2|4|
|**5**|6|Алания|1|2|-|3|
|**6**|7|Ротор|-|2|1|3|
|**7**|8|Динамо (Москва)|-|1|5|6|
|**8**|9|Ростов|-|1|-|1|
|**9**|9|Сочи|-|1|-|1|
|**10**|10|Краснодар|-|-|3|3|
|**11**|11|Торпедо (Москва)|-|-|1|1|
|**12**|11|Анжи|-|-|1|1|
|**13**|11|Крылья Советов|-|-|1|1|

Получилось неидеально, вылезла колонка без названия, какие-то прочерки. Но это не проблема, нужно только немного предобработки.

```
champions = tables[5]

# Удалим ненужную колонку и заменим прочерки на 0
champions = (champions
             .drop(columns=['Unnamed: 0'])
             .replace('-', 0)) 
```

||**Клуб**|**Золото**|**Серебро**|**Бронза**|**Всего медалей**|
|---|---|---|---|---|---|
|**0**|Спартак (Москва)|10|6|4|20|
|**1**|Зенит|9|3|4|16|
|**2**|ЦСКА|6|9|3|18|
|**3**|Локомотив (Москва)|3|6|6|15|
|**4**|Рубин|2|0|2|4|
|**5**|Алания|1|2|0|3|
|**6**|Ротор|0|2|1|3|
|**7**|Динамо (Москва)|0|1|5|6|
|**8**|Ростов|0|1|0|1|
|**9**|Сочи|0|1|0|1|
|**10**|Краснодар|0|0|3|3|
|**11**|Торпедо (Москва)|0|0|1|1|
|**12**|Анжи|0|0|1|1|
|**13**|Крылья Советов|0|0|1|1|

Пара секунд — и готово к анализу! Для исследований нам теперь доступен почти весь интернет, и Википедия это лишь начало.

Попробуем провести ещё одно исследование. В этот раз будем исследовать производительность мобильных устройств на основе их бенчмарков. Бенчмарк-тесты — это тесты, которые проверяют, насколько хорошо устройства с новой начинкой справляются с высокой нагрузкой.

Есть несколько популярных компаний, которые замеряют мобильные устройства и фиксируют результаты, формируя свой рейтинг. Одна из таких компаний — geekbench, и вот её страница с результатами [https://browser.geekbench.com/mobile-benchmarks](https://browser.geekbench.com/mobile-benchmarks).

![](https://pictures.s3.yandex.net/resources/image_1716808650.png)

И да, эти результаты тоже хранятся в табличном виде. Попробуем их получить.

```
tables = pd.read_html('https://browser.geekbench.com/mobile-benchmarks')
len(tables) 
```

```
3 
```

Нам доступны три таблицы, общие результаты по всем устройствам хранятся в первой.

```
benchmarks = tables[0]
benchmarks.head() 
```

||**Device**|**Score**|**Unnamed: 2**|
|---|---|---|---|
|**0**|iPhone 15 Pro Apple A17 Pro 3780 MHz (6 cores)|2907|nan|
|**1**|iPhone 15 Pro Max Apple A17 Pro 3780 MHz (6 cores)|2899|nan|
|**2**|iPhone 14 Pro Apple A16 Bionic 3460 MHz (6 cores)|2569|nan|
|**3**|iPhone 14 Pro Max Apple A16 Bionic 3460 MHz (6 cores)|2564|nan|
|**4**|iPhone 15 Plus Apple A16 Bionic 3460 MHz (6 cores)|2562|nan|

Осталось только предобработать — и уже готово к анализу.

```
# Удалим лишнюю колонку
benchmarks = benchmarks.drop(columns=['Unnamed: 2'])
# Преобразуем колонку с результатами к числу
benchmarks['Score'] = benchmarks['Score'].astype('int') 
```

А теперь сравним бренды между собой.

```
import seaborn as sns
from matplotlib import pyplot as plt

# Получим название бренда из модели
def brand(x):
    brand = x.split(' ')[0]
    if brand in ['iPhone', 'iPad']:
        brand = 'Apple'

    return brand

benchmarks['Brand'] = benchmarks['Device'].apply(brand)

# Ящик с усами по самым популярным моделям
sns.boxplot(data=benchmarks[benchmarks['Brand'].isin(['Apple', 'Samsung', 'Xiaomi', 'Google', 'Oppo'])],
            y='Score',
            hue='Brand')
plt.title('geekbench')
plt.show() 
```

![Ящик с усами по самым популярным моделям](https://pictures.s3.yandex.net/resources/image_1716808750.png)

Apple впереди, а среди Android — самые производительные устройства от Google.

### Excel будет недалеко

Когда вам нужно **передать** результаты своего исследования, в котором есть таблицы, редко когда подойдёт Jupyter-тетрадка с кодом, ведь это больше инструмент аналитика данных. Вы можете сохранить таблицы в csv-файлы с помощью метода `to_csv()`, но и такой формат некоторых может насторожить. Презентуя результаты своим коллегам, стоит использовать знакомый большинству Excel.

В самом начале обучения вы знакомились с преимуществами Python перед Excel. И всё же Excel остаётся на сегодня наиболее популярным инструментом для решения простых задач. Любой, даже не аналитик, знаком с этим инструментом просто потому, что Excel есть почти у каждого, а если его и нет, то всегда можно открыть таблицу в этом формате в Google Sheets.

Вы уже знаете, как импортировать данные из Excel, а точнее из xlsx-файла, а теперь научимся сохранять данные в формате `xlsx`. Для этого в Pandas используют метод `to_excel()` с параметрами:

- `excel_writer` — файл, в который необходимо сохранить данные.
- `sheet_name` — название листа в Excel-файле. Файл может состоять из нескольких листов.
- `engine` — движок, который используется для преобразования датафрейма в `xlsx`-формат. Либо `openpyxl`, либо `xlsxwriter`. Нам потребуется `openpyxl`, используемый по умолчанию.

Продолжим исследовать результаты бенчмарк-тестов. Посмотрим, какие результаты бенчмарк-тестов в среднем у топовых производителей.

```
top_brand = (benchmarks[benchmarks['Brand'].isin(['Apple', 'Samsung', 'Xiaomi', 'Google', 'Oppo', 'Huawei'])]
             .groupby('Brand')
             .agg({'Score': 'mean'})
             .sort_values(by='Score', ascending=False)) 
```

|**Brand**|**Score**|
|---|---|
|**Apple**|1619|
|**Google**|883.773|
|**Xiaomi**|698.929|
|**Samsung**|682.596|
|**Huawei**|515.538|
|**Oppo**|428.1|

А теперь сохраним результат в Excel-файл с помощью `to_excel()`.

```
top_brand.to_excel('geekbench.xlsx', sheet_name='Топ бренды', engine='openpyxl') 
```

![](https://pictures.s3.yandex.net/resources/image_1716808965.png)

Готово!

Используя контекстный менеджер `with as`, мы можем сохранить сразу несколько датафреймов на разных листах в Excel-файле.

```
# pd.ExcelWriter создаёт Excel-файл, готовый к записи
with pd.ExcelWriter('geekbench.xlsx') as writer:  
    benchmarks.to_excel(writer, sheet_name='Бенчмарки', engine='openpyxl')
    top_brands.to_excel(writer, sheet_name='Топ бренды', engine='openpyxl') 
```

![](https://pictures.s3.yandex.net/resources/image_1716809010.png)

### Итоги

- Данные могут храниться не только в файлах, а ещё и на веб-страницах в виде HTML-таблиц. Вы можете читать данные не только из csv- или Excel-файлов, но и прямо из html.
- Метод `pd.read_html()` позволяет извлекать такие таблицы напрямую с веб-страниц по их URL.
- Показывать коллегам результаты работы лучше в том виде, с которым они привыкли работать. Несмотря на преимущества Python, Excel по-прежнему остаётся популярным инструментом для работы с данными.
- Чтобы сохранить данные в виде Excel-файла, используют метод `to_excel()`.

## Подготовка графики
При исследовании данных аналитики часто используют визуализацию результатов для их представления. В работе часто возникает необходимость использовать графики отдельно от кода, например вставить их в презентацию или в текстовый документ при составлении отчёта для коллег или заказчика.

Для использования графиков в документах важно корректно их сохранить в файле с графическим форматом, чтобы визуализация была информативной и правильно оформленной. В прошлых уроках вы узнали, какие элементы стоит отобразить на графике, чтобы он был максимально информативным: задать заголовок, подпись по осям, легенду и другую необходимую информацию. Ещё вы изучили, как изменить размер графика, сохранить визуализацию в отдельном графике, выбрать корректный формат и разрешение файла, а также настроить отображение всех элементов графика.

В этом уроке мы рассмотрим особенности отображения и сохранения визуализаций в виде отдельных графических файлов:

- как настроить размер шрифтов;
- как вместить все элементы графика в сохраняемую область и избежать их пересечения;
- как сохранить график в виде отдельного файла, выбрав его разрешение и формат.

Для примера будем использовать графики с динамикой кумулятивных метрик, которые вы разбирали в предыдущих спринтах.

### Меняем размер графика и размер шрифта

В текстовый файл или презентацию графические файлы вставляются в виде изображения с заданным размером — в сантиметрах или дюймах. А также с установленным разрешением — количеством точек на дюйм.

По умолчанию графики, построенные с использованием распространённых библиотек визуализаций matplotlib и seaborn, имеют размер 6.4 на 4.8 дюйма, или примерно 16 на 12 см. Этого размера хватит, чтобы вставить визуализацию в текст на странице формата А4 или в страницу презентации. При работе с данными в тетрадке Jupyter Notebook размеры графиков часто увеличивают — это повышает информативность визуализации. Для этого используют параметр `figsize=()` метода `plt.figure()`, где задаются два значения: первое — размер графика по ширине, а второе — размер по высоте. Размеры задают в дюймах.

При подготовке графика к публикации следует не ставить размер больше свободного места на странице текста или слайде в презентации, поскольку в этом случае визуализация будет масштабирована под размер страницы. Как правило, для текстовых файлов используется лист формата А4, имеющий размер 210 на 297 мм, или 8.3 на 11.7 дюймов. Поэтому с учётом полей страницы желательно, чтобы размер сохраняемого графика не превышал 18 см по ширине страницы и 27 см по её высоте.

В большинстве случаев текстовые документы имеют альбомную ориентацию — можно взять за ограничение ширины графика 18 см, или около 7 дюймов по ширине. В таком случае задаваемые размеры шрифтов для подписи заголовка графика или его осей будут соответствовать распространённым размерам шрифтов в текстовых редакторах.

Разберём на примере графика кумулятивного значения среднего количества покупок на покупателя. Вначале рассчитаем необходимые метрики:

```
# Импортируем необходимые библиотеки:
import pandas as pd
from matplotlib import pyplot as plt
import seaborn as sns

# Загрузим данные с результатом теста:
orders = pd.read_csv('https://code.s3.yandex.net/datasets/orders_us.csv')
orders['date'] = pd.to_datetime(orders['date'])

# Найдём минимальную и максимальную даты сбора статистики:
start_date = orders['date'].min()
end_date = orders['date'].max()

# Создадим последовательность дат проведения теста:
test_dates = pd.date_range(start=start_date, end=end_date, freq='1D')

# Создадим пустой датафрейм с двумя столбцами — группами теста:
cum_avg_purchases = pd.DataFrame(columns = ['A', 'B'])

# Зададим цикл, перебирающий последовательность дат:
for i, date in enumerate(test_dates):
    current_purchases = (orders.query('date <= @date')
         # Для каждого пользователя в группах найдём количество заказов:
          .groupby(['group','visitorId'])
          .agg({'transactionId': 'nunique'}).reset_index()
         # Для каждой группы подсчитаем среднее количество заказов:
          .groupby('group')
          .agg({'transactionId': 'mean'}).T
        )

    # Добавим информацию о дате и дне проведения теста:
    current_purchases['date'] = date
    current_purchases['test_day'] = i

    # Добавим данные к датафрейму cum_revenue:
    cum_avg_purchases = pd.concat([cum_avg_purchases, current_purchases],
                                  ignore_index=True)

# Подсчитаем относительное значение метрики в группе В по отношению к группе А
cum_avg_purchases['rel_BA'] = cum_avg_purchases['B'] / cum_avg_purchases['A'] - 1 
```

А затем начнём строить график динамики отношения значений кумулятивного количества заказов на пользователя в группе В по отношению к группе А. Зададим размер графика, равный 7 на 4 дюйма:

```
# Визуализируем результаты:
plt.figure(figsize=(7, 4))
sns.lineplot(x='date', 
             y='rel_BA', 
             data=cum_avg_purchases, 
             label='Отношение группы В к группе А')

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1716822046.png)

В результате область визуализации получила корректные размеры, но сейчас нет заголовка графика, информативных и отформатированных подписей по осям. Добавим их. Также метки по оси X перекрывают друг друга — их стоит разместить под углом, например 45 градусов:

```
# Визуализируем результаты:
plt.figure(figsize=(7, 4))
sns.lineplot(x='date', 
             y='rel_BA', 
             data=cum_avg_purchases, 
             label='Отношение группы В к группе А')

# Добавим форматирование графика:
plt.title('Динамика изменения отношения кумулятивного среднего количества покупок в группе В относительно группы А')
plt.xlabel('Дата')
plt.ylabel('Отношение среднего количества покупок в группе В к группе А')

# Развернём подписи по оси Х на 45 градусов:
plt.xticks(rotation=45)

# Добавим графику сетку:
plt.grid()

plt.show() 
```

Для добавления заголовка использовали метод `plt.title()`, методы `plt.xlabel()` и `plt.ylabel()` использовали для назначения подписей по осям X и Y, а метод `plt.xticks()` с параметром `rotation` — для изменения ориентировки подписей меток по оси X:

![](https://pictures.s3.yandex.net/resources/image_1716822067.png)

Подписи добавили, но сейчас график выглядит не так, как хотелось бы — заголовок слишком длинный и выходит за пределы графика, как и подписи по оси Y. Также стоит увеличить размер шрифта заголовка и подписей по осям, чтобы они были хорошо читаемыми при изучении отчёта или просмотре презентации. Для этого сделаем следующие действия:

- С помощью параметра `fontsize` установим новые значения для заголовка графика и подписей по осям. По умолчанию размер шрифта заголовка используется 12 кегль, а для подписей по осям — 10. Установим значения 14 и 12 соответственно.
- Сделаем при помощи подстроки `\n` перенос строк в заголовке графика и подписи по оси Y:

```
# Визуализируем результаты:
plt.figure(figsize=(7, 4))
sns.lineplot(x='date', 
             y='rel_BA', 
             data=cum_avg_purchases, 
             label='Отношение группы В к группе А')

# Добавим форматирование графика:
plt.title('Динамика изменения отношения кумулятивного \nсреднего количества покупок в группе В \nотносительно группы А', fontsize=14)
plt.xlabel('Дата', fontsize=12)
plt.ylabel('Отношение среднего количества покупок \nв группе В к группе А', fontsize=12)

# Развернём подписи по оси X на 45 градусов:
plt.xticks(rotation=45)

# Добавим графику сетку:
plt.grid()

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1716822215.png)

График готов для публикации — заголовок выделен крупным шрифтом, длинный текст перенесён, а подписи меток повёрнуты на 45 градусов. Теперь его можно сохранять в удобный графический формат.

### Сохранение графика

График готов. Для дальнейшего использования его стоит сохранить в виде отдельного графического файла.

Графические данные могут быть представлены в различных форматах:

- Растровое изображение, представленное в виде набора пикселей и раскрашенное в определённые цвета. Например, если размер картинки 100 на 100 пикселей, то изображение будет состоять из 100х100 или 10 000 пикселей. В таком формате хранится большая часть изображений, например фотографии в телефоне.
- Векторное изображение, для которого прописаны правила отображения: с такого-то места до такого провести линию синего цвета или в таком месте нарисовать красный круг заданного диаметра. Такой формат хорошо подходит для отображения схематичных рисунков или графиков.

Графики в Python можно сохранить изображение как в растровом, так и в векторном виде:

- в растровом виде, например с расширением `png` или `jpg`;
- в векторном формате `svg`;
- В формате `pdf`.

Для сохранения визуализации, созданной на основе matplotlib, используется метод `plt.savefig()`. Для быстрого сохранения графика с базовыми настройками достаточно передать методу в качестве аргумента название файла:

```
# Сохраним визуализацию в формате PNG: 
plt.savefig("my_first_plot.png")  
```

Визуализация сохранилась с базовыми настройками в файл, расположенный в корневом каталоге тетрадки Jupyter Notebook. Метод `plt.savefig()` имеет ещё несколько параметров, которые могут быть полезными при сохранении визуализаций:

- `dpi` — параметр задаёт размер разрешения — количества точек дюйм. Чем выше это значение — тем более чёткое изображение, но и тем больше размер файла с изображением. По умолчанию стоит размер 100 `dpi`. Этого вполне достаточно для отображения графика на экране монитора. Однако для демонстрации на большом экране при сохранении в растровом формате или при распечатывании отчёта на бумажном носителе лучше увеличить это значение до 200 или 300 точек на дюйм.
- `transparent` — параметр, определяющий прозрачность контейнера визуализации. Такой эффект доступен для растрового формата `png`. В расширении `jpg` установить прозрачность не получится, поскольку этот формат не поддерживает такой опции. Значение `True` — позволяет сделать график прозрачным, тогда как значение `False`, используемое по умолчанию, делает контейнер с изображением непрозрачным и по умолчанию — белым.

Например, сохраним график с разрешением 300 DPI и включённой прозрачностью:
```python
plt.savefig("my_first_plot.png", dpi=300, transparent=True)
```

Результат будет таким:

![](https://pictures.s3.yandex.net/resources/image_1716822684.png)

### Сохранение сабплотов и проблема пересечения элементов графика

Иногда для отображения результатов аналитики используют несколько графиков на одной визуализации. Или несколько «сабплотов». При сохранении таких графиков есть свои особенности. Поскольку в одном графике включается несколько, то общий размер контейнера со всеми графиками увеличивают, например, до 12 дюймов.

Рассмотрим на примере, добавим график с накопительным значением метрик для двух групп:

```
# Создание фигуры и осей для сабплотов
fig, ax = plt.subplots(nrows=1, ncols=2, figsize=(12, 5))

# Динамика кумулятивного значения метрики в группе А и В:
sns.lineplot(x='date', y='A', data=cum_avg_purchases, label='Группа А', ax=ax[0])
sns.lineplot(x='date', y='B', data=cum_avg_purchases, label='Группа B', ax=ax[0])

# Добавим форматирование графика:
ax[0].set_title('Динамика накопительного значения среднего \nколичества покупок на посетителя', fontsize=14)
ax[0].set_xlabel('Дата', fontsize=12)
ax[0].set_ylabel('Среднее количество покупок', fontsize=12)
ax[0].tick_params('x', labelrotation=45)
ax[0].grid()

# График отношения метрики в группе В к группе А:
sns.lineplot(x='date',
             y='rel_BA',
             data=cum_avg_purchases,
             label='Отношение группы В к группе А',
             ax=ax[1])

# Добавим форматирование графика:
ax[1].set_title('Динамика изменения отношения кумулятивного \nсреднего количества покупок в группе В \nотносительно группы А', fontsize=14)
ax[1].set_xlabel('Дата', fontsize=12)
ax[1].set_ylabel('Отношение среднего количества покупок \nв группе В к группе А', fontsize=12)
ax[1].tick_params('x', labelrotation=45)
ax[1].grid()

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1716822711.png)

График по размеру получится больше, чем стандартный лист А4 в альбомной ориентации. Поэтому при использовании такого графика в текстовых документах произойдет его масштабирование под размер страницы — это может повлиять на размер шрифта заголовка и подписей осей.

Однако подпись по оси Y правого графика перекрылась с площадью левого графика. Чтобы исключить такое перекрытие можно использовать метод `plt.tight_layout()`. В этом случае элементы графика разместятся таким образом, чтобы исключить перекрытие между собой:

```
# Создание фигуры и осей для сабплотов
fig, ax = plt.subplots(nrows=1, ncols=2, figsize=(12, 5))

# Динамика кумулятивного значения метрики в группе А и В:
sns.lineplot(x='date', y='A', data=cum_avg_purchases, label='Группа А', ax=ax[0])
sns.lineplot(x='date', y='B', data=cum_avg_purchases, label='Группа B', ax=ax[0])

# Добавим форматирование графика:
ax[0].set_title('Динамика кумулятивного значения среднего \nколичества покупок на посетителя', fontsize=14)
ax[0].set_xlabel('Дата', fontsize=12)
ax[0].set_ylabel('Среднее количество покупок', fontsize=12)
ax[0].tick_params('x', labelrotation=45)
ax[0].grid()

# График отношения метрики в группе В к группе А:
sns.lineplot(x='date',
             y='rel_BA',
             data=cum_avg_purchases,
             label='Отношение группы В к группе А',
             ax=ax[1])

# Добавим форматирование графика:
ax[1].set_title('Динамика изменения отношения кумулятивного \nсреднего количества покупок в группе В \nотносительно группы А', fontsize=14)
ax[1].set_xlabel('Дата', fontsize=12)
ax[1].set_ylabel('Отношение среднего количества покупок \nв группе В к группе А', fontsize=12)
ax[1].tick_params('x', labelrotation=45)
ax[1].grid()

# Включим автоматическое распределение элементов внутри контейнера:
plt.tight_layout()

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1716822735.png)

Результат готов! Все элементы масштабированы таким образом, чтобы не перекрывать друг друга. Теперь график можно сохранять.

В этом уроке вы рассмотрели основные подходы к сохранению графиков в виде графических файлов. А в следующем уроке потренируетесь это делать самостоятельно в нескольких практических задачах!

## Формат данных JSON
Мы уже касались данной темы в дополнительных материалах предыдущего модуля.

JSON (JavaScript Object Notation) стал неотъемлемой частью современного анализа данных. Это текстовый формат, который используется для представления структурированных данных и который основан на синтаксисе JavaScript.

JSON стал популярным благодаря своей простоте, лёгкости и читабельности как для человека, так и для машины. В мире данных JSON используется повсеместно, и умение работать с ним считается важным навыком для аналитика данных.

В этом уроке вы узнаете, как использовать библиотеку `json` для конвертации объектов Python в JSON-строку и обратно. Обсудим конкретные случаи, когда аналитику данных нужно работать с JSON. Этот урок поможет вам освоить базовые техники работы с таким форматом данных.

В Python JSON-объекты очень схожи со словарями (dict), что облегчает их использование. Рассмотрим некоторые ключевые моменты:

- **структура:** JSON представляет собой пары «ключ-значение», что аналогично структуре словаря в Python;
- **типы данных:** JSON поддерживает простые типы данных, такие как строки, числа, массивы (списки в Python), и вложенные объекты (словари в Python);
- **чтение и запись:** в Python существует встроенная библиотека **`json`**, которая предоставляет простые методы для преобразования данных между JSON- и Python-объектами.

Пример словаря в Python:

```
data = {
    "name": "John",
    "age": 30,
    "city": "New York",
    "children": ["Ann", "Billy"]
} 
```

Эквивалент в JSON:

```
{
    "name": "John",
    "age": 30,
    "city": "New York",
    "children": ["Ann", "Billy"]
} 
```

### **В каких случаях аналитику нужно работать с данными в формате JSON**

1. **Веб-API:** Большинство современных веб-API возвращают данные в формате JSON. Если аналитик взаимодействует с API для получения данных, например, от социальных сетей, финансовых сервисов или метеорологических служб, ему потребуется умение читать и обрабатывать JSON. **Пример JSON-ответа от API социальной сети:**
    
    ```
     {
         "user": {
             "id": 12345,
             "name": "John Doe",
             "followers": 150,
             "posts": [
                 {
                     "id": 54321,
                     "content": "Hello, world!",
                     "likes": 10,
                     "comments": [
                         {"id": 1, "user": "Alice", "text": "Great post!"},
                         {"id": 2, "user": "Bob", "text": "Thanks for sharing!"}
                     ]
                 }
             ]
         }
     }
      
    ```
    
2. **Хранение и передача данных:** JSON широко используется для хранения конфигураций и настроек, а также для передачи данных между различными системами. Это делает JSON незаменимым для интеграции данных из разных источников и систем. **Пример JSON-файла конфигурации:**
    
    ```
     {
         "app_name": "DataAnalyzer",
         "version": "1.2.3",
         "settings": {
             "theme": "dark",
             "auto_save": true,
             "language": "en"
         }
     }
      
    ```
    
3. **Базы данных NoSQL:** Многие современные базы данных NoSQL, такие как MongoDB, используют формат, схожий с JSON (BSON). Понимание JSON помогает аналитику эффективно работать с такими базами данных. **Пример документа в базе данных MongoDB:**
    
    ```
     {
         "_id": {"$oid": "507f1f77bcf86cd799439011"},
         "name": "Jane Smith",
         "email": "jane.smith@example.com",
         "age": 28,
         "address": {
             "street": "123 Maple Street",
             "city": "Springfield",
             "state": "IL",
             "postalCode": "62704"
         },
         "orders": [
             {"id": 1, "product": "Laptop", "quantity": 1, "price": 1200.00},
             {"id": 2, "product": "Mouse", "quantity": 2, "price": 25.00}
         ]
     }
      
    ```
    

Освоение работы с JSON даёт аналитику возможность решать широкий спектр задач, от получения данных через API до их хранения и последующей обработки. Далее мы рассмотрим, как преобразовывать объекты Python в JSON и обратно, что считается основой для дальнейшей работы с этим форматом.

### **Преобразование объектов Python в JSON**

Для преобразования объектов Python (например, словарей и списков) в JSON-строку используется функция **`json.dumps()`** из встроенной библиотеки **`json`**.

Пример:

```
import json

# Создаём объект в формате словаря Python
data = {
    "name": "Alice",
    "age": 25,
    "city": "London",
    "languages": ["English", "Spanish"]
}

# Преобразуем объект в JSON-строку
json_string = json.dumps(data)
print(json_string) 
```

Итоговый результат:

```
{"name": "Alice", "age": 25, "city": "London", "languages": ["English", "Spanish"]} 
```

### **Преобразование JSON-строки в объект Python**

Для преобразования JSON-строки обратно в объект Python используется функция **`json.loads()`**.

Пример:

```
import json

# Определим JSON-строку
json_string = '{"name": "Alice", "age": 25, "city": "London", "languages": ["English", "Spanish"]}'

# Преобразуем JSON-строку в объект Python
data = json.loads(json_string)
print(data)
# Выводим тип данных полученного объекта
print(f'Тип данных: {type(data)}') 
```

Итоговый результат:

```
{'name': 'Alice', 'age': 25, 'city': 'London', 'languages': ['English', 'Spanish']}
Тип данных: <class 'dict'> 
```

Умение преобразовывать объекты Python в JSON и обратно считается важным навыком для аналитика данных. Это позволяет эффективно работать с данными, полученными из различных источников: веб-API, базами данных и файлами конфигураций.

Внутри JSON-строки мы использовали двойные кавычки для обозначения текста — это единственный возможный вариант, другие дадут ошибку. Если двойная кавычка есть внутри передаваемой строки, то используется экранирование.

Далее мы рассмотрим более сложные примеры работы с JSON, включая вложенные структуры данных и их обработку.

### Работа с многоуровневыми JSON-объектами

Примеры, которые мы рассматривали ранее, были достаточно простыми. В реальной работе аналитикам данных часто приходится сталкиваться с более сложными и многоуровневыми JSON-объектами. Такие структуры могут содержать вложенные списки и словари, что усложняет их преобразование и анализ.

Рассмотрим пример многоуровневого JSON-объекта, который может быть получен, например, из API социальной сети. В зависимости от структуры JSON-объекта будет меняться и конкретный способ преобразования, но структура объектов обычно похожа на такую:

```
{
    "user": {
        "id": 1,
        "name": "John Doe",
        "location": {
            "city": "New York",
            "country": "USA"
        },
        "posts": [
            {
                "id": 101,
                "title": "First Post",
                "content": "This is my first post",
                "comments": [
                    {"id": 1001, "text": "Great post!", "user": "Alice"},
                    {"id": 1002, "text": "Thanks for sharing", "user": "Bob"}
                ]
            },
            {
                "id": 102,
                "title": "Second Post",
                "content": "Back with another post",
                "comments": [
                    {"id": 1003, "text": "Nice read", "user": "Charlie"}
                ]
            }
        ]
    }
} 
```

Для преобразования сложных JSON-объектов в DataFrame необходимо с помощью функции **`json.loads()`** преобразовать JSON-строку в объект Python (словарь).

```
# Преобразуем JSON-строку в объект Python
data = json.loads(json_string)
display(data) 
```

```
{'user': {'id': 1,
  'name': 'John Doe',
  'location': {'city': 'New York', 'country': 'USA'},
  'posts': [{'id': 101,
    'title': 'First Post',
    'content': 'This is my first post',
    'comments': [{'id': 1001, 'text': 'Great post!', 'user': 'Alice'},
     {'id': 1002, 'text': 'Thanks for sharing', 'user': 'Bob'}]},
   {'id': 102,
    'title': 'Second Post',
    'content': 'Back with another post',
    'comments': [{'id': 1003, 'text': 'Nice read', 'user': 'Charlie'}]}]}} 
```

Далее создаём словарь **`user_info`**, в котором сохраняем данные пользователя: его идентификатор, имя, город и страну.

```
user_info = {
    "user_id": data['user']['id'],
    "user_name": data['user']['name'],
    "city": data['user']['location']['city'],
    "country": data['user']['location']['country']
} 
```

Инициализируем пустой список **`posts_data`**, куда будем складывать информацию о постах и комментариях. В цикле **`for`** проходим по каждому посту пользователя. Извлекаем данные о посте и сохраняем их в словарь **`post_info`**. Обновляем **`post_info`**, добавляя данные пользователя из **`user_info`**. Вложенным циклом **`for`** обрабатываем каждый комментарий к посту. Извлекаем данные о комментарии и сохраняем их в словарь **`comment_info`**. Обновляем **`comment_info`**, добавляя данные о посте из **`post_info`**. Добавляем **`comment_info`** в список **`posts_data`**.

```
posts_data = []

for post in data['user']['posts']:
    post_info = {
        "post_id": post['id'],
        "post_title": post['title'],
        "post_content": post['content']
    }
    
    # Добавляем информацию о пользователе
    post_info.update(user_info)
    
    # Обработка комментариев к посту
    for comment in post['comments']:
        comment_info = {
            "comment_id": comment['id'],
            "comment_text": comment['text'],
            "comment_user": comment['user']
        }
        
        # Добавляем информацию о посте
        comment_info.update(post_info)
        
        # Добавляем запись в список
        posts_data.append(comment_info)

display(posts_data) 
```

```
[{'comment_id': 1001,
  'comment_text': 'Great post!',
  'comment_user': 'Alice',
  'post_id': 101,
  'post_title': 'First Post',
  'post_content': 'This is my first post',
  'user_id': 1,
  'user_name': 'John Doe',
  'city': 'New York',
  'country': 'USA'},
 {'comment_id': 1002,
  'comment_text': 'Thanks for sharing',
  'comment_user': 'Bob',
  'post_id': 101,
  'post_title': 'First Post',
  'post_content': 'This is my first post',
  'user_id': 1,
  'user_name': 'John Doe',
  'city': 'New York',
  'country': 'USA'},
 {'comment_id': 1003,
  'comment_text': 'Nice read',
  'comment_user': 'Charlie',
  'post_id': 102,
  'post_title': 'Second Post',
  'post_content': 'Back with another post',
  'user_id': 1,
  'user_name': 'John Doe',
  'city': 'New York',
  'country': 'USA'}] 
```

И под конец создаём сам DataFrame из этого списка:

```
# Создаём DataFrame из списка
df = pd.DataFrame(posts_data)

# Выводим DataFrame
display(df) 
```

||**comment_id**|**comment_text**|**comment_user**|**post_id**|**post_title**|**post_content**|**user_id**|**user_name**|**city**|**country**|
|---|---|---|---|---|---|---|---|---|---|---|
|0|1001|Great post!|Alice|101|First Post|This is my first post|1|John Doe|New York|USA|
|1|1002|Thanks for sharing|Bob|101|First Post|This is my first post|1|John Doe|New York|USA|
|2|1003|Nice read|Charlie|102|Second Post|Back with another post|1|John Doe|New York|USA|

Этот DataFrame объединяет информацию о пользователе, его постах и комментариях, что позволяет легко проводить дальнейший анализ данных.

Мы рассмотрели основные аспекты работы с JSON в Python, включая преобразование простых и многоуровневых JSON-объектов в DataFrame с использованием библиотеки **`pandas`**. Эти навыки позволяют эффективно манипулировать данными, полученными из различных источников, и интегрировать их в аналитические процессы. В следующем уроке на конкретных задачах закрепим полученные знания.

## Принципы работы с API
Сегодня в мире, где полно разнообразных веб-сервисов и приложений, программные интерфейсы (API) играют ключевую роль. API обеспечивают взаимодействие между различными системами и платформами: позволяют приложениям обмениваться данными и функциональностью и делают возможными интеграцию и автоматизацию процессов. Для аналитиков данных умение работать с API — важный навык, потому что многие источники данных предоставляют информацию именно через API.

В этом разделе вы познакомитесь с основами работы через API, научитесь отправлять запросы, получать и обрабатывать данные в формате JSON. Затем преобразовывать эти данные в удобные для анализа форматы, такие как pandas DataFrame.

Для начала стоит разобрать общие принципы работы API. Вы узнаете, как использовать библиотеку Requests, чтобы отправлять запросы и получать ответы.

### Что такое API?

**API (Application Programming Interface)** — это набор инструментов и протоколов, который позволяет различным программным приложениям взаимодействовать между собой.

API предоставляет методы и правила для обмена данными и выполнения операций, чтобы разработчики могли использовать функциональность одного приложения в другом. Это особенно важно в условиях, когда различные системы и приложения должны работать совместно. Так обеспечивается эффективная передача данных и функциональность.

Существует несколько типов API — каждый имеет свои особенности и области применения. Вот основные:

1. **REST (Representational State Transfer)** **API** — протокол обмена сообщениями, который использует стандартные методы HTTP (GET, POST, PUT, DELETE) для взаимодействия с ресурсами. Каждый ресурс идентифицируется уникальным URL.
2. **SOAP (Simple Object Access Protocol)** **API** — протокол обмена сообщениями, который использует XML для кодирования запросов и ответов. SOAP работает по различным протоколам передачи данных, таким как HTTP, SMTP и другие.

В современных веб-разработке и анализе данных наиболее популярен REST API. Именно через него многие крупные компании и сервисы предоставляют свои данные и функциональные возможности. Так как доступ к этим данным часто необходим для анализа и принятия решений, знание REST API — важный навык для аналитиков данных.

Взаимодействие с API происходит таким образом: вы отправляете HTTP-запрос и получаете ответ. Запросы могут содержать параметры и заголовки.

REST API часто возвращает данные в формате JSON, который легко парсится в Python. Этот формат стал стандартом для обмена данными в веб-приложениях благодаря своей простоте и гибкости.

HTTP-ответы содержат статусные коды, которые указывают на успешность или неуспешность запроса. Вы наверняка сталкивались с кодом 404 — «страница не найдена». Это HTTP-ответ. Код 200, например, обозначает успешный запрос, а 500 — ошибку сервера.

### **Принципы работы с библиотекой Requests**

Библиотеку Requests в Python используют для отправки HTTP-запросов. Вот основные методы и их синтаксис:

**GET-запрос** используется для получения данных с сервера. Это самый распространённый тип запроса, который не изменяет состояние ресурса (`resource`) на сервере.

```
response = requests.get('https://api.example.com/resource') 
```

**POST-запрос** нужен для отправки данных на сервер с целью создания новых данных. В отличие от GET, этот запрос изменяет состояние ресурса.

```
response = requests.post('https://api.example.com/resource', 
                         data={'key': 'value'}) 
```

**PUT-запрос** используют, чтобы обновить существующий ресурс на сервере или создать новый, если ресурса не существует. PUT-запросы обычно отправляют все данные о ресурсе, даже если изменились только некоторые из них.

```
response = requests.put('https://api.example.com/resource', 
                        data={'key': 'value'}) 
```

**DELETE-запрос** удаляет ресурс с сервера.

```
response = requests.delete('https://api.example.com/resource') 
```

Посмотреть, как работает API, можно с помощью платформы Hipo Labs Universities.

Эта платформа предоставляет открытый API по адресу [http://universities.hipolabs.com/search](http://universities.hipolabs.com/search) для поиска и получения информации о высших учебных заведениях по всему миру на основе страны, названия, домена и других параметров.

Основные возможности API:

1. **Поиск по стране** — вы получите список университетов определённой страны.
2. **Поиск по названию.**
3. **Поиск по домену** позволит получить данные об университетах по доменному имени их сайтов.

API возвращает данные в формате JSON — так их удобнее интегрировать и обрабатывать в различных приложениях и аналитических системах.

### **Пример запроса и ответа**

Импортируем необходимые библиотеки: Requests для отправки HTTP-запросов и pandas для работы с данными.

```
import requests
import pandas as pd 
```

Указываем URL API и параметры запроса (в данном случае страна `"Russian Federation"`).

```
# URL API для получения университетов в Российской Федерации:
url = "http://universities.hipolabs.com/search"
params = {"country": "Russian Federation"} 
```

Используем `requests.get()` для отправки GET-запроса к API.

```
# Отправка GET-запроса:
response = requests.get(url, params=params) 
```

Ответ необязательно будет положительным. Если код статуса ответа начинается с 4 или 5, значит, произошла ошибка.

Проверяем статус ответа. Если запрос успешен — то есть имеет статус 200, — мы получим JSON-ответ.

```
# Проверка успешности запроса:
if response.status_code == 200:
    data = response.json()
    
    # Вывод первых двух элементов JSON-ответа:
    print(data[:2])
else:
    print(f"Error: {response.status_code}") 
```

```
[{'country': 'Russian Federation',
  'web_pages': ['http://www.academiaga.ru/'],
  'alpha_two_code': 'RU',
  'domains': ['academiaga.ru'],
  'state-province': None,
  'name': 'St. Petersburg State University of Civil Aviation'},
 {'country': 'Russian Federation',
  'web_pages': ['http://www.adygnet.ru/'],
  'alpha_two_code': 'RU',
  'domains': ['adygnet.ru'],
  'state-province': None,
  'name': 'Adygeja State University'}] 
```

Теперь перепишем код: преобразуем данные из JSON-формата в `pandas.DataFrame` и посмотрим первые несколько строк для проверки.

```
# Проверка успешности запроса:
if response.status_code == 200:
    data = response.json()
    
    # Преобразование JSON-ответа в DataFrame:
    df = pd.DataFrame(data)
    
    # Вывод первых строк DataFrame:
    print(df.head())
else:
    print(f"Error: {response.status_code}") 
```

|—|**country**|**web_pages**|**alpha_two_code**|**domains**|**state-province**|**name**|
|---|---|---|---|---|---|---|
|0|Russian Federation|['[http://www.academiaga.ru/](http://www.academiaga.ru/)']|RU|['[http://academiaga.ru/](http://academiaga.ru/)']|—|St. Petersburg State University of Civil Aviation|
|1|Russian Federation|['[http://www.adygnet.ru/](http://www.adygnet.ru/)']|RU|['[http://adygnet.ru/](http://adygnet.ru/)']|—|Adygeja State University|
|2|Russian Federation|['[http://www.ael.ru/](http://www.ael.ru/)']|RU|['[http://ael.ru/](http://ael.ru/)']|—|Khabarovsk State Academy of Economics and Law|
|3|Russian Federation|['[http://agma.astranet.ru/](http://agma.astranet.ru/)']|RU|['[http://agma.astranet.ru/](http://agma.astranet.ru/)']|—|Astrahan State Medical Academy|
|4|Russian Federation|['[http://www.agtu.ru/](http://www.agtu.ru/)']|RU|['[http://agtu.ru/](http://agtu.ru/)']|—|Arhangelsk State Technical University|

На этом примере вы видите, как легко можно получить данные с помощью REST API. Потом их можно обработать и подготовить к дальнейшему анализу с использованием Python и библиотек Requests и pandas.

Вы познакомились с основами работы с REST API и научились получать данные в формате JSON. Теперь настало время применить эти знания на практике. Если данный урок показался вам слишком кратким и обзорным, а практики не хватило - загляните в дополнительные материалы второго модуля.

## Загрузка данных из базы SQL
В современном мире данные — это ключевой ресурс для принятия решений, прогнозирования и анализа. Умение подключаться к базам данных и извлекать данные с помощью Python — важнейший навык для аналитика.

Большинство организаций работают с огромными объемами информации, которые хранятся в базах данных. Эти данные могут включать в себя показатели о продажах, клиентах, продуктах, транзакциях и многом другом. Использование баз данных позволяет эффективно управлять и структурировать эту информацию.

Базы данных предоставляют централизованное хранилище для всех данных организации. Такие хранилища оптимизированы для быстрого поиска, извлечения и манипулирования сведениями. SQL-запросы позволяют извлекать необходимые данные быстро и эффективно.

В этом уроке мы рассмотрим, как Python может подключаться к базе данных, выполнять SQL-запросы и выгружать данные.

### **Подключение к базе данных и выполнения SQL-запросов**

В Python существует множество библиотек, которые могут использоваться для подключения к базам данных. Вот некоторые из них:

- **psycopg2 —** библиотека для работы с PostgreSQL;
- **PyMySQL** — библиотека для работы с MySQL;
- **SQLite (sqlite3)** — встроенная в Python библиотека для работы с SQLite;
- **PyODBC** — библиотека для работы с различными базами данных через ODBC (Open Database Connectivity — стандартный интерфейс для доступа к различным системам управления базами данных).

Для работы с базой данных в Python мы будем использовать библиотеку`SQLAlchemy`для создания подключения и библиотеку `pandas` для выполнения SQL-запросов и обработки данных.

`SQLAlchemy` — одна из самых популярных библиотек для работы с реляционными базами данных в Python. Она поддерживает множество баз данных: PostgreSQL, MySQL, SQLite, Oracle и многие другие. Это делает `SQLAlchemy` универсальным инструментом, который можно использовать в различных проектах.

**1. Импорт необходимых библиотек:**

```
import pandas as pd
from sqlalchemy import text, create_engine 
```

`create_engine` — это функция из библиотеки SQLAlchemy, которая используется для создания объекта «движка» (engine), представляющего соединение с базой данных. Этот объект считается основной точкой взаимодействия с базой данных, позволяя нам выполнять SQL-запросы, управлять транзакциями и работать с метаданными базы данных.

`text` — это функция из библиотеки SQLAlchemy, которая используется для создания SQL-запросов в виде строк. Она позволяет использовать «сырой» SQL, оборачивая строку запроса в объект, который можно безопасно передавать в методы выполнения запросов SQLAlchemy.

**2. Установка параметров подключения**

Следующим шагом будет установка параметров для подключения к базе данных:

```
db_config = {
    'user': 'praktikum_student', # имя пользователя
    'pwd': 'Sdf4$2;d-d30pp', # пароль
    'host': 'rc1b-wcoijxj3yxfsf3fs.mdb.yandexcloud.net',
    'port': 6432, # порт подключения
    'db': 'data-analyst-final-project-db' # название базы данных
} 
```

Тут содержатся данные о том, куда подключаться, какой логин и пароль показывать и в какую базу данных заглядывать. Когда вы подключаетесь к базе данных, важно правильно настроить параметры подключения.

С именем пользователя и паролем всё понятно, давайте рассмотрим ключевые параметры: `host`, `port` и `db`, которые необходимы для установления соединения.

**`host`** — это параметр, указывающий на адрес сервера базы данных. Это может быть доменное имя (например, `example.com`) или IP-адрес (например, `192.168.1.1`), где хостится ваша база данных.

- **локальный хост**: если база данных установлена на вашем локальном компьютере, `host` часто будет `localhost` или `127.0.0.1`;
- **удаленный сервер**: если база данных находится на удаленном сервере, вы указываете адрес этого сервера. В нашем примере, это `rc1b-wcoijxj3yxfsf3fs.mdb.yandexcloud.net`.

**`port`** — это параметр, указывающий на порт, через который происходит подключение к серверу базы данных. Каждый тип базы данных обычно имеет стандартный порт, но он может быть изменен.

- **стандартные порты**: для PostgreSQL стандартный порт — 5432, для MySQL — 3306, для SQL Server — 1433 и так далее;
- **кастомные порты**: иногда администраторы баз данных меняют стандартные порты по соображениям безопасности или для предотвращения конфликтов.

**`db`** — это параметр, указывающий на имя конкретной базы данных, к которой вы хотите подключиться. На сервере базы данных может находиться несколько баз данных, и этот параметр помогает идентифицировать ту, с которой вы собираетесь работать. В нашем примере, это `data-analyst-final-project-db`.

**3. Создание строки подключения**

Используя параметры подключения, создадим строку подключения:

```
connection_string = 'postgresql://{user}:{pwd}@{host}:{port}/{db}'\
                        .format(**db_config) 
```

Тут `**` — оператор, обратный функции `dict`, он распаковывает словарь в команды для метода `format`.

**4. Создание коннектора**

С помощью строки подключения создадим объект коннектора:

```
engine = create_engine(connection_string, 
                       connect_args={'sslmode':'require'}) 
```

Параметр `connect_args={'sslmode':'require'}` используется при создании соединения с базой данных с помощью библиотеки SQLAlchemy. В этом случае параметр `sslmode` устанавливается в значение `require`, потому что при соединении требуется использование SSL (Secure Sockets Layer) для защищенного шифрования трафика между клиентом (вашим приложением Python) и сервером базы данных. Этот параметр обеспечивает безопасное и защищенное соединение с базой данных, особенно если данные чувствительные и конфиденциальные.

Параметр `sslmode` имеет несколько вариантов значений, которые определяют уровень безопасности соединения с базой данных. Вот основные из них:

- `disable` — отключает использование SSL. Соединение установится без шифрования;
- `allow` — попытка использовать SSL, если это поддерживается сервером, но если SSL недоступен, то соединение установится без него;
- `verify-ca` — требует использование SSL и проверку сертификата SSL, подписанного доверенным центром сертификации (CA). Сертификат сервера должен быть проверен.

**5. Выполнение SQL-запроса и выгрузка данных**

Теперь мы можем выполнить SQL-запрос для получения данных из базы. В этом примере мы будем выбирать первые пять строк из таблицы `books`:

```
query = '''
    SELECT * 
    FROM books 
    LIMIT 5
'''

con = engine.connect()
df = pd.read_sql(sql=text(query), con=con) 
```

Объект `con` представляет собой соединение с базой данных через «движок» `engine.connect()` , который создан с помощью функции `create_engine`.

`pd.read_sql()` — это функция pandas, которая выполняет SQL-запрос к базе данных и загружает результаты в DataFrame.

`sql=text(query)` — здесь создается объект SQLAlchemy `text`, который представляет собой SQL-запрос в виде строки. Этот объект передается в `pd.read_sql()` в качестве аргумента `sql`.

Результаты SQL-запроса загружаются в DataFrame `df`, который можно использовать для анализа и обработки данных в Python.

||**book_id**|**author_id**|**title**|**num_pages**|**publication_date**|**publisher_id**|
|---|---|---|---|---|---|---|
|0|1|546|'Salem's Lot|594|2005-11-01|93|
|1|2|465|1 000 Places to See Before You Die|992|2003-05-22|336|
|2|3|407|13 Little Blue Envelopes (Little Blue Envelope #1)|322|2010-12-21|135|
|3|4|82|1491: New Revelations of the Americas Before Columbus|541|2006-10-10|309|
|4|5|125|1776|386|2006-07-04|268|

Теперь перейдем к практическому уроку, где вы сами выполните SQL-запрос к базе данных и загрузите полученные данные в DataFrame.

# Подготовка данных
## Массаж данных
В этом уроке вы разберёте,

- что такое широкий и длинный формат данных и какое между ними различие;
- как перевести данные из одного формата в другой, используя pandas и методы `melt`, `stack` и `unstack`;

### Формат данных

Данные, поступающие аналитику для изучения, могут быть представлены в разных форматах. Самые популярные форматы — широкий и длинный.

### Широкий формат

Широкий формат данных подразумевает хранение каждой переменной или признака в отдельном столбце. Каждая строка в такой таблице представляет собой одно уникальное наблюдение, а каждый столбец — отдельный признак.

Рассмотрим широкий формат на примере данных участников А/В-теста. Вы изучали его результаты в одном из предыдущих уроков:

```
# Загрузим необходимые библиотеки:
import pandas as pd

# Загрузим данные:
df_wide = pd.read_csv('p4da_ab_test_users.csv')

# Отобразим первые пять строк:
print(df_wide.head()) 
```

```
            user_id region   device    reg_date            test group
0  2E1BF1D4C37EA01F     EU       PC  2021-01-18  interface_test     A
1  50734A22C0C63768     EU   iPhone  2021-01-18  interface_test     B
2  5BE017E9C8CC42F8     EU  Android  2021-01-18  interface_test     B
3  96F27A054B191457     EU   iPhone  2021-01-18  interface_test     B
4  E6AF85675078215D     EU  Android  2021-01-18  interface_test     A 
```

В этих данных `user_id` представляет собой уникальное наблюдение, где одна строка — это один уникальный пользователь. Далее идут пять столбцов, в каждом из которых представлен свой уникальный признак: `region`, `device`, `reg_date`, `test` и `group`:

![](https://pictures.s3.yandex.net/resources/8.3.1_skhema_1_1715960181.png)

_В этих данных `user_id` представляет собой уникальное наблюдение, где одна строка — это один уникальный пользователь. Далее идут пять столбцов, в каждом из которых представлен свой уникальный признак: `region`, `device`, `reg_date`, `test` и `group`:_

Широкие таблицы более удобные и привычные для восприятия человеком, поскольку одна строка содержит необходимую информацию. Также широкие таблицы часто используют при хранении данных, с которыми работают в Excel. Большинство алгоритмов машинного обучения ожидают, что каждая строка данных соответствует одному наблюдению и содержит все значения необходимых для обучения модели признаков.

### Длинный формат

В длинном формате данные представлены иначе — каждый отдельный признак или переменная одного и того же наблюдения записаны в разных строках, а не в одной строке, как в широкой таблице. Такие таблицы ещё называют **плоскими** или **вертикальными**.

Рассмотрим на примере данных пользователей А/В-теста, как та же самая информация будет представлена в формате длинной таблицы:

```
# Загрузим необходимые библиотеки:
import pandas as pd

# Загрузим данные:
df_long = pd.read_csv('p4da_ab_test_users_long.csv')

# Отобразим первые пять строк:
print(df_long.head()) 
```

```
            user_id parameter           value
0  0002CE61FF2C4011     group               A
1  0002CE61FF2C4011      test  interface_test
2  0002CE61FF2C4011    device             Mac
3  0002CE61FF2C4011    region              EU
4  0002CE61FF2C4011  reg_date      2021-01-18 
```

В результате количество столбцов уменьшилось до трёх: идентификатор пользователя `user_id`, название параметра `parameter` и его значение `value`. Однако теперь каждая строка не является информацией об уникальном наблюдении (в нашем случае — уникальном пользователе), а является уникальной для одного признака пользователя. Поэтому для одного пользователя может быть несколько строк. Их количество будет зависеть от количества параметров, а общее количество строк будет больше, чем в случае с широкой таблицей.

![](https://pictures.s3.yandex.net/resources/8.3.1_skhema_2_1715960191.png)

_Длинный формат данных подразумевает хранение отдельного признака наблюдения в отдельной строке._

Такой формат данных менее удобен человеку для восприятия, ведь чтобы найти значение определённого параметра, необходимо фильтровать данные по этому параметру.

Длинный формат больше подходит для различных алгоритмов работы с данными — часто используется при визуализации данных и их анализе в системах BI. Также в данные, представленные в длинном формате, удобнее добавлять новый признак: для этого не нужно менять структуру данных и добавлять новый столбец с признаком, а достаточно добавить необходимые строки с новым признаком и его значениями.

### Изменение формата датасета

В своей работе аналитик часто переводит данные из одного формата в другой. Например, если данные представлены в виде длинной таблицы и необходимо провести анализ метрик по группам или сегментам. Для этого данные лучше подготовить в формате широкой таблицы. Так удобнее проводить агрегацию значений по столбцам.

Бывают обратные случаи, когда необходимо привести данные к длинному формату. Например, при подготовке визуализаций и создании дашбордов в популярных BI-системах.

### Преобразование длинного формата в широкий

Для того чтобы преобразовать данные из длинного формата в широкий, необходимо для каждого признака или параметра создать отдельный столбец. При этом для каждого наблюдения должна быть создана одна строка, содержащая все его признаки.

Рассмотрим алгоритм преобразования таблицы из длинного формата в широкий на примере данных пользователей А/В-теста — `p4da_ab_test_users_long.csv`, рассматриваемые как пример длинной таблицы. Первые строки вы загружали ранее, поэтому посмотрите на них ещё раз:

```
            user_id parameter           value
0  0002CE61FF2C4011     group               A
1  0002CE61FF2C4011      test  interface_test
2  0002CE61FF2C4011    device             Mac
3  0002CE61FF2C4011    region              EU
4  0002CE61FF2C4011  reg_date      2021-01-18 
```

Данные содержат три столбца: `user_id`, `parameter` и `value`. Для того чтобы преобразовать данные из длинного формата в широкий, используйте метод pandas `.unstack()`. Метод можно применять к DataFrame — он проводит преобразование уровней индексов в уровни колонок и возвращает новый DataFrame.

Схема преобразования длинной таблицы в широкую методом `.unstack()` будет такой:

![](https://pictures.s3.yandex.net/resources/8.3.1_skhema_3_1715960213.png)

_Метод `.unstack()` проводит преобразование уровней индексов датафрейма в уровни колонок._

Как следует из схемы, для применения преобразования длиной таблицы в широкую необходимо, чтобы исходная длинная таблица содержала ключевые значения в виде индексов, то есть используется мультииндекс строк.

В нашем примере мультииндекс будет содержать два индекса — номер пользователя `user_id` и значение параметра `parameter`. Один из них будет представлять собой уникальное наблюдения — `user_id`, а второй — содержать названия будущих столбцов, т.е. `parameter`. Уровней индексов в мультииндексе может быть больше, но общая логика остаётся: один из индексов (по умолчанию индекс, имеющий внутренний уровень) используется для создания столбцов, другие — в качестве индекса строк будущей широкой таблицы.

Если посмотреть на данные пользователей А/В-теста, то в качестве индекса строк выступают целочисленные значения, последовательно нумерующие строки:

```
            user_id parameter           value
0  0002CE61FF2C4011     group               A
1  0002CE61FF2C4011      test  interface_test
2  0002CE61FF2C4011    device             Mac
3  0002CE61FF2C4011    region              EU
4  0002CE61FF2C4011  reg_date      2021-01-18 
```

Такой индекс не подойдёт для преобразования таблицы в широкую. Поэтому следует отдельно задать индексы длинной таблице.

Для этого используем метод pandas `.set_index()`. Он позволяет задать датафрейму новый индекс. Если передать одно значение — индекс будет иметь один уровень, если передать список значений — многоуровневым, то есть мультииндексом. Передадим в качестве индекса список названий столбцов — `['user_id', 'parameter']`:

```
# Изменим индекс датафрейма с помощью метода .set_index():
df_long.set_index(['user_id', 'parameter'], inplace=True)

# Отобразим первые пять строк:
print(df_long.head())

# Отобразим имена индексов:
print(df_long.index.names) 
```

```
                                     value
user_id          parameter                
0002CE61FF2C4011 group                   A
                 test       interface_test
                 device                Mac
                 region                 EU
                 reg_date       2021-01-18
                 
['user_id', 'parameter'] 
```

Теперь данные представлены иначе — строки имеют два индекса: `user_id` и `parameter`, они представляют собой мультииндекс, где `parameter` является внутренним индексом.

С индексами разобрались, теперь можно приступить к трансформации таблицы из длинной в широкую. Для этого применим метод `.unstack()` с параметрами по умолчанию:

```
# Создадим широкий датафрейм на основе длинного:
df_wide = df_long.unstack()

# Отобразим первые пять строк:
print(df_wide.head()) 
```

```
                    value                                         
parameter          device group    reg_date region            test
user_id                                                           
0002CE61FF2C4011      Mac     A  2021-01-18     EU  interface_test
001E72F50D1C48FA      Mac     B  2021-01-28     EU  interface_test
002412F1EB3F6E38      Mac     A  2021-01-20     EU  interface_test
002540BE89C930FB  Android     A  2021-01-19     EU  interface_test
0031F1B5E9FBF708  Android     B  2021-01-25     EU  interface_test 
```

Сработало! Данные представлены в виде широкой таблицы. Метод `.unstack()` при работе проверяет, чтобы значения (в нашем случае столбец `value`) в данных были уникальными. Если для одного параметра одного наблюдения будет несколько значений — метод закончит работу с ошибкой.

Однако остался один момент — датафрейм имеет сложную систему мультииндекса столбца, что может быть не очень комфортно при анализе данных такого датафрейма, поскольку вместо названия столбца при агрегации или фильтрации данных может потребоваться указывать мультииндекс.

Выведем названия столбцов:

```
# Выведем индекс столбцов:
print(df_wide.columns) 
```

```
MultiIndex([('value',   'device'),
            ('value',    'group'),
            ('value', 'reg_date'),
            ('value',   'region'),
            ('value',     'test')],
           names=[None, 'parameter']) 
```

Столбцы имеют мультииндекс `[None, 'parameter']`, где верхний уровень без названия содержит значение `value`, а внутренний уровень `'parameter'` — информативные названия столбцов. Уберём этот уровень:

```
# Удалим верхний уровень индекса столбцов:
df_wide.columns = df_wide.columns.droplevel()

# Выведем индекс столбцов:
print(df_wide.columns) 
```

```
Index(['device', 'group', 'reg_date', 'region', 'test'], dtype='object', name='parameter') 
```

Для этого использовали метод pandas `.droplevel()`. Этот метод позволяет удалить уровни в индексе столбцов, по умолчанию — самый внешний уровень. После применения метода `.droplevel()` для индекса столбцов датафрейм будет представлен в таком виде:

```
# Отобразим первые пять строк:
print(df_wide.head()) 
```

```
parameter          device group    reg_date region            test
user_id                                                           
0002CE61FF2C4011      Mac     A  2021-01-18     EU  interface_test
001E72F50D1C48FA      Mac     B  2021-01-28     EU  interface_test
002412F1EB3F6E38      Mac     A  2021-01-20     EU  interface_test
002540BE89C930FB  Android     A  2021-01-19     EU  interface_test
0031F1B5E9FBF708  Android     B  2021-01-25     EU  interface_test 
```

В качестве индекса строк выступают данные `user_id`. Сбросим индекс на порядковый номер строки, используя метод pandas `.reset_index()`:

```
# Сбросим индекс на порядковый номер строки:
df_wide = df_wide.reset_index()

# Отобразим первые пять строк:
print(df_wide.head()) 
```

```
parameter           user_id   device group    reg_date region            test
0          0002CE61FF2C4011      Mac     A  2021-01-18     EU  interface_test
1          001E72F50D1C48FA      Mac     B  2021-01-28     EU  interface_test
2          002412F1EB3F6E38      Mac     A  2021-01-20     EU  interface_test
3          002540BE89C930FB  Android     A  2021-01-19     EU  interface_test
4          0031F1B5E9FBF708  Android     B  2021-01-25     EU  interface_test 
```

Теперь датафрейм содержит индекс строк в виде их порядкового номера. Название уровня индекса колонок `parameter` осталось прежним. Его при необходимости можно сбросить:

```
# Сбросим название индекса колонок:
df_wide.columns.name = None

# Отобразим первые пять строк:
print(df_wide.head()) 
```

```
            user_id   device group    reg_date region            test
0  0002CE61FF2C4011      Mac     A  2021-01-18     EU  interface_test
1  001E72F50D1C48FA      Mac     B  2021-01-28     EU  interface_test
2  002412F1EB3F6E38      Mac     A  2021-01-20     EU  interface_test
3  002540BE89C930FB  Android     A  2021-01-19     EU  interface_test
4  0031F1B5E9FBF708  Android     B  2021-01-25     EU  interface_test 
```

Теперь датафрейм имеет вид, к которому мы привыкли при загрузке данных из внешних источников.

Существует длинный датафрейм с такими данными:

- `order_id` — номер заказа;
- `user_id` — идентификатор пользователя;
- `parameter` — в этом столбце содержатся категории данных, такие как количество товаров, стоимость заказа, дата заказа, время доставки;
- `value` — значения для каждого параметра.

Каждая строка содержит уникальные данные, индекс строк — их порядковый номер. Какой код позволит преобразовать данные из длинного формата в широкий, чтобы каждый параметр был в уникальном столбце?

```
df_long.set_index(['order_id', 'user_id', 'parameter']).unstack() 
``` 

Верно! В начале нужно задать мультииндекс — столбцы `order_id`, `user_id`, `parameter`. Метод `.unstack()` будет использовать по умолчанию самый внутренний индекс для преобразования данных — `parameter`.

Привести данные из длинной таблицы в широкую можно с помощью создания сводной таблицы — `.pivot_table()`:

```
# Создадим сводную таблицу с помощью метода .pivot_table():
df_wide = df_long.pivot_table(index='user_id', columns='parameter', 
                    values='value', aggfunc='first').reset_index()

# Сбросим название уровня индекса столбцов:
df_wide.columns.name = None

# Отобразим первые пять строк:
print(df_wide.head()) 
```

```
            user_id   device group    reg_date region            test
0  0002CE61FF2C4011      Mac     A  2021-01-18     EU  interface_test
1  001E72F50D1C48FA      Mac     B  2021-01-28     EU  interface_test
2  002412F1EB3F6E38      Mac     A  2021-01-20     EU  interface_test
3  002540BE89C930FB  Android     A  2021-01-19     EU  interface_test
4  0031F1B5E9FBF708  Android     B  2021-01-25     EU  interface_test 
```

Результат получился аналогичным. Для индекса будущей широкой таблицы были использованы значения столбца `user_id`, в качестве колонок — `parameter`, а значения — `value`. Поскольку метод `.pivot_table()` основан на агрегации данных, чтобы сводная таблица была создана в качестве параметра `aggfunc`, использовали метод `first`, определяющий первое обнаруженное значение параметра. Так как исходные данные в длинном формате содержали уникальные значения для каждого параметра, то первое встреченное значение для параметра и будет его уникальным значением. Если бы значения были представлены в числовом виде, то тогда можно было бы использовать значение `aggfunc` по умолчанию как рассчитывающее среднее.

Как видно, широкую таблицу можно преобразовать из длинной несколькими путями. Метод `.unstack()` удобен тем, что использует систему индексов датафрейма. При этом он не использует агрегацию данных, а преобразует структуру таблицы. С помощью сводной таблицы `.pivot_table()` также можно создать широкую таблицу, однако в этом случае необходимо задавать индексы, колонки и значения будущей широкой таблицы. При этом, если метод `.unstack()` при работе проверяет уникальность значений для каждого параметра, то при создании сводной таблицы используется агрегация данных, и нужно выбрать метод их агрегирования.

Однако

При использовании `.pivot_table()` для трансформации длинной таблицы в широкую стоит убедиться, что все значения для каждого параметра — уникальны. Иначе, если одно наблюдения для одного параметра будет иметь несколько значений, то вместо трансформации в широкую таблицу произойдёт агрегация данных. А это уже сводная таблица. По формату она будет широкой, но часть данных может потеряться, заменившись средним значением.

### Преобразование широкого формата в длинный

При работе с данными также бывают случаи, когда необходимо выполнить обратное преобразование — из таблицы с широким форматом записи данных сделать таблицу с длинным форматом. Например, для анализа данных коллеги представили сводную таблицу с затратами на рекламные источники по месяцам:

```
# Создадим исходный датафрейм:
data = {
    'source': ['source_a', 'source_b'],
    'январь': [78454, 54750],
    'февраль': [88997, 74521],
    'март': [45450, 55004]
}
df = pd.DataFrame(data)

# Выведем датафрейм на экран:
print(df) 
```

```
     source  январь  февраль   март
0  source_a   78454    88997  45450
1  source_b   54750    74521  55004 
```

Анализировать такой датафрейм (например, визуализировать данные по месяцам) не совсем удобно. Поэтому приведём его к длинному формату, где будут следующие столбцы: `source` название источника, `month` — месяц и `costs` — затраты. Для этого используем метод pandas `.melt()`. Этот метод создаёт из нескольких столбцов исходного датафрейма два столбца — один будет содержать названия признака или параметра, а другой — его значение. Как раз то, что нужно для длинной таблицы!

Метод `.melt()` имеет следующие параметры, часто используемые для создания длинной таблицы:

- `id_vars`: названия столбцов — наблюдений;
- `value_vars`: названия столбцов, которые необходимые трансформировать в формат длинной таблицы;
- `var_name`: название столбца с перечислением параметров;
- `value_name`: название столбца с значениями.

Применим этот метод для нашего случая:

```
# Преобразуем широкий датафрейм в длинный:
df_long = df.melt(id_vars='source', 
             value_vars=['январь','февраль','март'],
             var_name='month',
             value_name='costs')

# Выведем датафрейм на экран:
print(df_long) 
```

```
     source    month  costs
0  source_a   январь  78454
1  source_b   январь  54750
2  source_a  февраль  88997
3  source_b  февраль  74521
4  source_a     март  45450
5  source_b     март  55004 
```

Результат — длинная таблица. Теперь данные можно обработать и проанализировать (например, построить barplot). При работе с методом `.melt()` можно не использовать параметр `value_vars` — тогда метод по умолчанию трансформирует значения всех столбцов, кроме указанных в параметре `id_vars`.

Преобразовать широкую таблицу в длинную можно также с помощью другого метода pandas — `.stack()`. Этот метод противоположен методу `.unstack()`: на основе широкого датафрейма — создаёт длинный. Алгоритм создания длинного датафрейма будет обратный, как в случае с методом `.unstack()`:

- сначала необходимо назначить столбец, значения которого являются наблюдениями, в нашем случае — это название источника `source`, в качестве индекса датафрейма;
- затем следует применить метод `.stack()`

В результате значения всех столбцов будут трансформированы в два столбца:

```
# Назначим новый индекс:
df_long = df.set_index('source')

# Создадим длинную таблицу:
df_long = df_long.stack()

# Выведем реузльтат на экран:
print(df_long) 
```

```
source           
source_a  январь     78454
          февраль    88997
          март       45450
source_b  январь     54750
          февраль    74521
          март       55004
dtype: int64 
```

В результате была сформирована серия значения, содержащая мультииндекс. Чтобы перевести эти данные в датафрейм, сбросим индексы:

```
# Сбросим индексы:
df_long = df_long.reset_index()

# Выведем датафрейм на экран:
print(df_long) 
```

```
     source  level_1      0
0  source_a   январь  78454
1  source_a  февраль  88997
2  source_a     март  45450
3  source_b   январь  54750
4  source_b  февраль  74521
5  source_b     март  55004 
```

Осталось переименовать названия столбцов:

```
# Назначим новые имена для столбцов:
df_long.columns = ['source','month','costs']

# Выведем датафрейм на экран:
print(df_long) 
```

```
     source    month  costs
0  source_a   январь  78454
1  source_a  февраль  88997
2  source_a     март  45450
3  source_b   январь  54750
4  source_b  февраль  74521
5  source_b     март  55004 
```

Длинный датафрейм готов!

Для анализа поступили следующие данные:

|purchase_datetime|player_id|item|price|purchase_id|
|---|---|---|---|---|---|
|0|2019-01-01 00:15:54|2434d4ef-7949-4311-a9e6-9263e2ea2b7c|armour|0.1|10423|
|1|2019-01-01 00:16:56|1ecd854d-f707-44a2-b174-0c0039668650|life|0.3|10424|
|2|2019-01-01 00:56:17|1156b1a0-732b-4732-ab41-79f77cd0e719|life|0.3|10425|
|3|2019-01-01 00:58:17|1f7b30f3-26ac-45bd-82b4-30fad1d1398d|armour|0.1|10426|
|4|2019-01-01 01:15:42|4697efd3-15b2-4f40-84fb-7e5c65e390c9|armour|0.1|10427|

Какой код позволит преобразовать широкую таблицу в длинную с такими итоговыми столбцами: `player_id`, `purchase_id`, `variable`, `value`.

Правильный ответ

```
df.melt(id_vars=['player_id','purchase_id'],
             value_vars=['item','price','purchase_datetime'],
             var_name='variable',
             value_name='value') 
``` 

Верно! Можно использовать метод `.melt()` для преобразования широкой таблицы в длинную. Этот метод позволяет выбрать столбцы, которые будут преобразованы в длинный формат и задать названия новым столбцам.

```
df_long = df.set_index(['player_id','purchase_id']).stack().reset_index()
df_long.columns = ['player_id','purchase_id','variable','value'] 
``` 

ПФ: Верно! Можно использовать метод `.stack()` для преобразования широкой таблицы в длинную. Важно выбрать индексы — данные, которые будут определять наблюдения.

Как и в случае с преобразованием длинной таблицы в широкую, обратное действие также можно сделать различными методами pandas. В первом случае метод `.melt()` позволяет создать длинную таблицу в одно действие, прописав в качестве параметров необходимые названия столбцов. При этом можно преобразовывать не все столбцы, а только несколько из них.

В этом уроке вы познакомились с двумя форматами записи данных в таблицы — широким и длинным, а также научились переводить данные из одного формата в другой используя методы `.stack()`, `.unstack()` и `.melt()`. В следующем уроке вы опробуете эти методы в работе.

## Объединение таблиц
В прошлом уроке вы разобрали, что данные могут быть представлены в виде широкой или длинной таблицы. Каждый формат представления данных оптимален для решения различных задач и аналитику важно уметь проводить трансформацию данных из одного формата в другой.

Но данные не всегда хранятся в полном объёме в одной таблице, широкого или длинного формата. Как правило, базы данных, содержащие всю важную для анализа информацию, состоят из множества таблиц. Каждая таблица хранит только определённую информацию. Например, если база данных содержит информацию об активности клиентов приложения, то в одной таблице будет содержаться информация о клиентах — их идентификационный номер и основная информация. В другой таблице будут записываться их действия, в ещё одной таблице — расшифровка действий (например, совершённые покупки). Таблиц может много — всё зависит от данных, которые собираются.

При анализе данных удобно, когда все они собраны в одной таблице — с ней проще совершать агрегацию и фильтрацию значений, а также визуализировать результаты. Поэтому в своей работе аналитики часто собирают необходимые данные из различных таблиц в одну.

В этому уроке вы разберете:

- плюсы хранения данных в отдельных таблицах и создания общих таблиц для работы;
- основные принципы объединения данных из разных таблиц с помощью метода `.merge()`;
- как добавить столбец к существующему датафрейму при помощи метода `.join()`

### Нормализация и денормализация данных

Аналитики работают с разными данными. Они могут быть достаточно простыми — состоять из нескольких столбцов (например, динамики изменения суточной температуры воздуха в регионе) или содержать большой объём информации (например, фиксация действий пользователя в приложении маркетплейса).

Чем больше данных, тем сложнее становится их хранить — их объём постоянно увеличивается, количество столбцов растёт по мере добавления признаков или расчёта метрик. Данные в таких таблицах могут дублироваться и могут возникать различные ошибки в записи. В результате может оказаться, что часть данных большой таблицы становится неактуальной.

Чтобы избежать проблемы хранения больших данных, дата-инженеры используют подход, разбивающий большой объём данных на фрагменты — небольшие логически связанные таблицы. Например, одна таблица содержит уникальные данные о пользователе — его имя, почту, телефон и уникальный идентификатор региона. Другая таблица содержит информацию о регионе. Ещё одна таблица содержит информацию о действиях клиента в приложении. Информация о купленных товарах хранится в другой таблице, а информация в действующих рекламных компания в ещё одной. Таких таблиц может быть много и все они охватывают большое разнообразие действий пользователя или его характеристики.

Такой подход к хранению информации получил название **нормализации данных**. Создание отдельных таблиц минимизирует дублирование информации, а также сокращает объём занимаемой данными памяти.

Однако проводить анализ данных на Python и pandas, когда интересующие значения разбросаны по разным таблицам, может быть не так удобно и эффективно, как проводить анализ с использованием одной или нескольких таблиц. Например, если нужно провести анализ активности пользователей в приложении в разрезе региона, то хорошо, когда данные об активности пользователей, наряду с идентификатором пользователя, содержат и название его региона или его идентификатор. Так вы сможете агрегировать данные по этому признаку и достичь необходимых результатов.

Поэтому при получении задачи аналитик оценивает исходные данные и рассматривает алгоритм создания одной или нескольких таблиц, необходимых для работы. Создание из нескольких нормализованных таблиц одной большой таблицы с нужной информацией называется **денормализацией данных**.

На заметку

Во многих примерах или задачах, рассматриваемых в курсе, использовался один источник данных, содержащий всю необходимую информацию.

### Принципы объединения таблиц

В зависимости от данных и поставленных задач, существует несколько принципов объединения таблиц. Рассмотрим их на небольшом примере. Есть две таблицы: в одной таблице `users` находится информация о посетителях интернет-магазина, а в другой таблице `orders` — количество покупок. Обратимся к данным:

```
# Загрузим библиотеки:
import pandas as pd

# Создадим два датафрейма:
data_users = {
    'user_id': ['667','235','735','468','931','137','341','386','181'],
    'name': ['Петров','Громыко','Лидова','Сергеев','Дмитриев',
             'Голубева','Иванов','Сидорова','Репин'],
}
users = pd.DataFrame(data_users)

data_orders = {
    'user_id': ['667','111','735','468','931','221','341','386','456'],
    'orders': [12,3,18,8,9,3,5,7,11],
}
orders = pd.DataFrame(data_orders)

# Выведем датафреймы на экран:
print(users)
print(orders) 
```

```
  user_id      name
0     667    Петров
1     235   Громыко
2     735    Лидова
3     468   Сергеев
4     931  Дмитриев
5     137  Голубева
6     341    Иванов
7     386  Сидорова
8     181     Репин

  user_id  orders
0     667      12
1     111       3
2     735      18
3     468       8
4     931       9
5     221       3
6     341       5
7     386       7
8     456      11 
```

Сейчас данные о покупках и о посетителях распределены среди двух таблиц. Это затрудняет их анализ. Например, если мы хотим посмотреть, какие пользователи из таблицы `users` сделали заказ, то без объединения данных не обойтись, поскольку информация о пользователях, которые сделали заказ, находится в таблице `orders`.

Объединяемые таблицы могут различаться по количеству записей, по сортировке данных, по столбцам. Поэтому первое, что необходимо знать перед объединением таблиц — какое значение или несколько значений использовать для объединения. Эти значения должны содержаться в двух таблицах и быть уникальными для каждой из таблиц, а для группы значение — уникально должно быть их сочетание. В нашем случае — это номер пользователя `user_id`. При этом, эти значения могут иметь различное название столбца в разных таблицах, например `user_id` в одной таблице и `id` — в другой.

Для анализа поступили данные о событиях пользователей в приложении. Есть две таблицы.

Одна таблица называется `users` и содержит такие столбцы: `id` — идентификатор пользователя, `region` — регион пользователя, `device` — устройство пользователя.

Вторая таблица с событиями `events`, содержит такие столбцы: `user_id` — идентификатор пользователя, `event_name` — название действия пользователя, `event_start_dt` — время начала события, `event_finish_dt` — время окончания.

По какому ключевому значению можно провести объединение таблиц?

  
Правильный ответ

`id` для таблицы `users` и `user_id` для таблицы `events`

Верно! Идентификаторы пользователя в двух таблицах содержатся в столбцах с разными названиями, поэтому следует использовать для каждой таблицы своё уникальное значение.

В pandas существует специальный метод — `.merge()` (от английского — слить, соединить), объединяющий данные двух таблиц. Объединить две таблицы в одну можно несколькими способами — они универсальны и встречаются во многих языках программирования. Рассмотрим их подробнее.

### Внутреннее объединение — `inner`

Такой тип предполагает объединение по «внутренней» — `inner` области, общей для двух таблиц. При результате в общую таблицу войдут только те данные, относящиеся к строкам в двух таблицах.

![](https://pictures.s3.yandex.net/resources/1_1715523211.png)

Например в случае объединения таблиц `users` и `orders` результат будет таким:

![](https://pictures.s3.yandex.net/resources/2_1715523230.png)

Значение `user_id`, равное `667`, присутствует в обеих таблицах. Поэтому фамилия из первой таблицы объединяется с числом покупок из второй.

![](https://pictures.s3.yandex.net/resources/3_1715523253.png)

То же самое происходит с другими покупателями, если значение в поле `user_id`встречается в обеих таблицах. Но не все покупатели вошли в финальную таблицу. Строки не попали в результат, если идентификаторы не совпали.

Рассмотрим пример работы метода `.merge()`.

Обратите внимание

При объединении данных есть две таблицы: одна, к которой применяется метод `.merge()`, будем называть левой таблицей, тогда присоединяемая таблица будет правой таблицей.

Рассмотрим базовый синтаксис `.merge()`:

```
# Базовый синтаксис .merge():
df_left.merge(df_right, on='', how='') 
```

- `df_left` — левый датафрейм, базовый при объединении таблиц. К нему применяется сам метод `.merge()`.
- `on` — название ключевого столбца, по которому таблицы объединяются, либо список из нескольких столбцов (тогда объединение происходит при совпадении значений всех таких столбцов). Предполагается, что эти значения есть в двух объединяемых столбцах и имеют одинаковое название.
- `how` — параметр, указывающий тип объединения: `inner`, `outer`, `left`, `right`, `cross`.

Это базовые параметры, необходимые для объединения таблиц. Теперь объединим таблицы `users` и `orders`, используя внутренний тип объединения `inner`:

```
# Объединим таблицы по ключевому значению user_id, используя внутрнее объединение:
df_merge = users.merge(orders, on='user_id', how='inner')

# Выведем результат на экран:
print(df_merge) 
```

```
  user_id      name  orders
0     667    Петров      12
1     735    Лидова      18
2     468   Сергеев       8
3     931  Дмитриев       9
4     341    Иванов       5
5     386  Сидорова       7 
```

В результате в итоговую таблицу вошли только 6 записей — это количество покупок пользователей, по которым есть информация и об имени, и о заказах, то есть пользователи, чьи `user_id` были в двух датафреймах. Теперь можно перечислить фамилии покупателей, а не только их уникальные номера.

Такой тип объединения полезно использовать, когда необходимо оставить только полные наблюдения для анализа: если для ключевого значения данные будут отсутствовать, то они не войдут в итоговую таблицу. Обратите внимание, что этот тип соединения используется методом `merge()` по умолчанию.

### Внешнее объединение — `outer`

Внешнее объединение обозначается ключевым словом `outer`. Этот тип является противоположным `inner` — итоговый датафрейм будет содержать все данные как левого, так и правового датафреймов. Такое объединение может называться **полным внешним**.

![](https://pictures.s3.yandex.net/resources/4_1715523477.png)

В этом случае схема объединения будет следующей:

![](https://pictures.s3.yandex.net/resources/5_1715523493.png)

В результате итоговый датафрейм будет содержать все ключевые значения — user_id, а в тех столбцах, где данные отсутствовали, будут пропуски.

Посмотрим на результат внешнего объединения данных с помощью метода `.merge()`:

```
# Объединим таблицы по ключевому значению user_id, используя внешнее объединение:
df_merge = users.merge(orders, on='user_id', how='outer')

# Выведем результат на экран:
print(df_merge) 
```

```
   user_id      name  orders
0      667    Петров    12.0
1      235   Громыко     NaN
2      735    Лидова    18.0
3      468   Сергеев     8.0
4      931  Дмитриев     9.0
5      137  Голубева     NaN
6      341    Иванов     5.0
7      386  Сидорова     7.0
8      181     Репин     NaN
9      111       NaN     3.0
10     221       NaN     3.0
11     456       NaN    11.0 
```

Результаты демонстрируют, что часть пользователей не совершала покупок, а часть — не имеет заполненных данных в столбце `name`.

Такой тип объединения часто используется, когда нужно сохранить все данные и не упустить ни одного наблюдения. Это полезно, когда нужно найти пользователей, не совершавших покупку, или товары с отсутствующим описанием.

### Левое объединение — `left`

Этот тип объединения использует в качестве основного левый датафрейм.

![](https://pictures.s3.yandex.net/resources/6_1715523686.png)

Все ключевые значения из этого датафрейма останутся, а в случае отсутствия данных в присоединяемом правом датафрейме на их месте будут пропуски:

![](https://pictures.s3.yandex.net/resources/7_1715523721.png)

Датафрейм слева содержит информацию о фамилиях покупателей, поэтому все эти данные вошли в финальный датафрейм. Для некоторых фамилий не нашлось совпадений в правом датафрейме, поэтому в поле `orders` будет пропуск. Возможно при дальнейшем анализе мы заполним пропуски нулём. Данные о покупателях с идентификаторами `111`, `221` и `456` (у которых есть информация о заказах, но нет данных об фамилии) в результат слияния не войдут, ведь таких значений нет в левой таблице.

Рассмотрим реализацию такого объединения с помощью метода `.merge()`:

```
# Объединим таблицы по ключевому значению user_id, используя левое объединение:
df_merge = users.merge(orders, on='user_id', how='left')

# Выведем результат на экран:
print(df_merge) 
```

```
  user_id      name  orders
0     667    Петров    12.0
1     235   Громыко     NaN
2     735    Лидова    18.0
3     468   Сергеев     8.0
4     931  Дмитриев     9.0
5     137  Голубева     NaN
6     341    Иванов     5.0
7     386  Сидорова     7.0
8     181     Репин     NaN 
```

В итоговом датафрейме остались ключевые значения, содержащиеся в левом датафрейме. Такой тип объединения полезен, когда необходимо собрать информацию по заранее известным наблюдениям, находящимся в левом датафрейме. Например, был выделен ряд пользователей согласно каким-то признакам — региону, возрасту и так далее. И для этих пользователей необходимо провести анализ покупок. Тогда, используя левое объединение, в итоговый датафрейм войдёт информация только для этих пользователей, даже если какие-то данные для них отсутствуют.

### Правое объединение — `right`

Этот тип объединения по своему алгоритму похож на левое объединение, только в качестве основного датафрейма используется правый датафрейм.

![](https://pictures.s3.yandex.net/resources/8_1715523842.png)

Все ключевые значения из этого датафрейма останутся, а в случае отсутствия данных в левом датафрейме они заполнятся пропущенными значениями:

![](https://pictures.s3.yandex.net/resources/9_1715523864.png)

Такой тип объединения используется, когда необходимо оставить только данные из правого датафрейма, то есть собрать информацию о пользователях, совершивших покупку:

```
# Объединим таблицы по ключевому значению user_id, используя правое объединение:
df_merge = users.merge(orders, on='user_id', how='right')

# Выведем результат на экран:
print(df_merge) 
```

```
  user_id      name  orders
0     667    Петров      12
1     111       NaN       3
2     735    Лидова      18
3     468   Сергеев       8
4     931  Дмитриев       9
5     221       NaN       3
6     341    Иванов       5
7     386  Сидорова       7
8     456       NaN      11 
```

По результатам объединения данных видно, что часть покупателей не имеет фамилии — можно дополнительно запросить необходимую информацию для наполнения базы данных клиентов.

### Объединение с созданием всех возможных комбинаций — `cross`

При объединении двух датафреймов существует ещё один способ, когда при объединении создаются все возможные комбинации из строк двух датафреймов. При использовании `cross` объединения каждая строка из левого датафрейма будет объединена со всеми строками из правого датафрейма.

Рассмотрим на следующем примере: к таблице с информацией о клиентах `users` присоединим данные другой таблицы — `marketing_events`, содержащей информацию о маркетинговых событиях, которые можно предположить пользователям:

![](https://pictures.s3.yandex.net/resources/10_1715523967.png)

В этом случае все записи правого датафрейма были присоединены к данным левого датафрейма. В результате каждый пользователь из данных `users` получил информацию о возможных акциях.

Разберём это объединение с помощью метода `.merge()`:

```
# Создадим датафрейм с маркетинговыми компаниями:
data_events = {
    'event': ['Первая покупка','День рождения'],
    'duration': ['7 days','14 days'],
}
marketing_events = pd.DataFrame(data_events)

# Объединим таблицы по ключевому значению user_id, используя cross объединение:
df_merge = users.merge(marketing_events, how='cross')

# Выведем результат на экран:
print(df_merge) 
```

```
   user_id      name           event duration
0      667    Петров  Первая покупка   7 days
1      667    Петров   День рождения  14 days
2      235   Громыко  Первая покупка   7 days
3      235   Громыко   День рождения  14 days
4      735    Лидова  Первая покупка   7 days
5      735    Лидова   День рождения  14 days
6      468   Сергеев  Первая покупка   7 days
7      468   Сергеев   День рождения  14 days
8      931  Дмитриев  Первая покупка   7 days
9      931  Дмитриев   День рождения  14 days
10     137  Голубева  Первая покупка   7 days
11     137  Голубева   День рождения  14 days
12     341    Иванов  Первая покупка   7 days
13     341    Иванов   День рождения  14 days
14     386  Сидорова  Первая покупка   7 days
15     386  Сидорова   День рождения  14 days
16     181     Репин  Первая покупка   7 days
17     181     Репин   День рождения  14 days 
```

Обратите внимание

В этом случае параметр on метода `.merge()` не используется.

В результате получился итоговый датафрейм, где информация была сдублирована и для каждой строки левого датафрейма добавилась информация правого датафрейма. Такой метод объединения данных полезно использовать, когда необходимо добавить общую информацию к левому датафрейму (как информацию о возможных акциях и сроках их проведения для каждого клиента из примера выше). `Cross` объединение полезно, когда вам нужно получить все возможные сочетания строк в одной и другой таблице.

### Особенности использования метода `.merge()`

В примерах выше мы рассматривали случаи объединения датафреймов, когда левый и правый датафрейм содержат одинаковое название ключевого значения, используемого для объединения. Но могут быть ситуации с разными названиями ключевых столбцов. Для этого в методе `.merge()` существует параметры `left_on` и `right_on`, используемые для обозначения ключевых значений в левом и правом датафреймах, вместо параметра `on`:

```
# Загрузим библиотеки:
import pandas as pd

# Создадим два датафрейма:
data_users = {
    'user_id': ['667','235','735','468','931','137','341','386','181'],
    'name': ['Петров','Громыко','Лидова','Сергеев','Дмитриев',
             'Голубева','Иванов','Сидорова','Репин'],
}
users = pd.DataFrame(data_users)

data_orders = {
    'id': ['667','111','735','468','931','221','341','386','456'],
    'orders': [12,3,18,8,9,3,5,7,11],
}
orders = pd.DataFrame(data_orders)

# Объединим таблицы, используя левое объединение:
df_merge = users.merge(orders, left_on='user_id', right_on='id', how='left')

# Выведем результат на экран:
print(df_merge) 
```

```
  user_id      name   id  orders
0     667    Петров  667    12.0
1     235   Громыко  NaN     NaN
2     735    Лидова  735    18.0
3     468   Сергеев  468     8.0
4     931  Дмитриев  931     9.0
5     137  Голубева  NaN     NaN
6     341    Иванов  341     5.0
7     386  Сидорова  386     7.0
8     181     Репин  NaN     NaN 
```

Помимо разных названий ключевых столбцов, датафреймы могут содержать данные с одинаковым названием столбцов. Поскольку в одном датафрейме не может быть двух столбцов с одним индексом, то при соединении с одинаковыми названиями столбцов, будут внесены изменения в индексы этих столбцов:

```
# Загрузим библиотеки:
import pandas as pd

# Создадим два датафрейма:
data_users = {
    'user_id': ['667','235','735','468','931','137','341','386','181'],
    'name': ['Петров','Громыко','Лидова','Сергеев','Дмитриев',
             'Голубева','Иванов','Сидорова','Репин'],
}
users = pd.DataFrame(data_users)

data_orders = {
    'user_id': ['667','111','735','468','931','221','341','386','456'],
    'name': ['Петров','Рогулев','Лидова','Сергеев','Дмитриев','Колбешкин',
             'Голубева','Иванов','Репин'],
    'orders': [12,3,18,8,9,3,5,7,11],
}
orders = pd.DataFrame(data_orders)

# Объединим таблицы, используя левое объединение:
df_merge = users.merge(orders, on='user_id', how='left')

# Выведем результат на экран:
print(df_merge) 
```

```
  user_id    name_x    name_y  orders
0     667    Петров    Петров    12.0
1     235   Громыко       NaN     NaN
2     735    Лидова    Лидова    18.0
3     468   Сергеев   Сергеев     8.0
4     931  Дмитриев  Дмитриев     9.0
5     137  Голубева       NaN     NaN
6     341    Иванов  Голубева     5.0
7     386  Сидорова    Иванов     7.0
8     181     Репин       NaN     NaN 
```

В результате столбцы с одинаковым названием (`name`) получили префиксы — `_x` и `_y`, обозначающие принадлежность к левому датафрейму — `_x`, а `_y` — к правому. Чтобы задать собственные префиксы, можно использовать параметр `.merge()` `suffixes` в котором передаются значения префиксов:

```
# Объединим таблицы, используя левое объединение:
df_merge = users.merge(orders, on='user_id', how='left', 
                       suffixes=('_left_df','_right_df'))

# Выведем результат на экран:
print(df_merge) 
```

```
  user_id name_left_df name_right_df  orders
0     667       Петров        Петров    12.0
1     235      Громыко           NaN     NaN
2     735       Лидова        Лидова    18.0
3     468      Сергеев       Сергеев     8.0
4     931     Дмитриев      Дмитриев     9.0
5     137     Голубева           NaN     NaN
6     341       Иванов      Голубева     5.0
7     386     Сидорова        Иванов     7.0
8     181        Репин           NaN     NaN 
```

### Использование индекса строк при объединении данных

Объединять данные в pandas можно не только по значению ключевых полей, но также с использованием индекса строк. Для этого нужен метод `.join()`. Он похож на `.merge()`: метод применяется к левой таблице, а в самом методе указывается правая таблица. При этом можно не указывать параметр `on`:

```
# Установим индексы для датафреймов - user_id:
users.set_index('user_id', inplace=True)
orders.set_index('user_id', inplace=True)

# Объединим с помощью левого объединения по индексу:
joined_df = users.join(orders)

# Выведем итоговый датафрейм на экран:
print(joined_df) 
```

```
             name  orders
user_id                  
667        Петров    12.0
235       Громыко     NaN
735        Лидова    18.0
468       Сергеев     8.0
931      Дмитриев     9.0
137      Голубева     NaN
341        Иванов     5.0
386      Сидорова     7.0
181         Репин     NaN 
```

Данные были объединены по индексу, назначенному по полю `user_id`, используя метод `.set_index()`. Обратите внимание на такие особенности метода `.join()`:

- в отличие от `.merge()`, по умолчанию в `.join()` установлен тип слияния `how='left'`;
- параметр `suffixes` разделён на два независимых: `lsuffix` (от англ. left suffix, «левый суффикс») и `rsuffix` (от англ. right suffix, «правый суффикс»);
- без параметра `on` метод`.join()` будет искать совпадения по индексам в первом и втором датафреймах. Если же передать параметру `on` столбец, то `.join()` найдёт его в первом датафрейме и начнёт сравнивать с индексом второго.

Для упрощения кода можно не проводить назначение индекса каждому датафрейму, а использовать изменение индекса при объединении данных — это позволит создать нужный датафрейм, но не изменять исходные данные:

```
# Объединим с помощью левого объединения по индексу:
joined_df = users.set_index('user_id').join(orders.set_index('user_id'))

# Выведем итоговый датафрейм на экран:
print(joined_df) 
```

```
             name  orders
user_id                  
667        Петров    12.0
235       Громыко     NaN
735        Лидова    18.0
468       Сергеев     8.0
931      Дмитриев     9.0
137      Голубева     NaN
341        Иванов     5.0
386      Сидорова     7.0
181         Репин     NaN 
```

Таким образом метод `.join()` хорошо справляется при объединении датафреймов по их индексу при левом объединении. Метод `.merge()` используется при более сложном объединении по ключевым значениям или нескольким из них.

Ещё методом `.join()` можно объединять больше двух таблиц: их набор передают списком вместо второго датафрейма. Например, у нас есть три датафрейма:

1. `p4da_bank_clients.csv` — содержит информацию о клиенте: `user_id` — идентификатор клиента, `gender` — пол и `age` — возраст;
2. `p4da_bank_clients_city.csv` — содержит информацию о городе клиента: `user_id` — идентификатор клиента, `city` — название города:
3. `p4da_bank_clients_loyalty.csv` — содержит информацию о наличие у клиента карты лояльности: `user_id` — идентификатор клиента, `loyalty` — есть ли карта лояльности, метки `1` и `0`:

Объединим при помощи метода `.join()` три датафрейма в один и оценим, сколько клиентов имеют карты лояльности и как они распределены по городам.

```
# Выведем данные о этих датафреймах:
clients = pd.read_csv('p4da_bank_clients.csv')  
clients_city = pd.read_csv('p4da_bank_clients_city.csv')  
clients_loyalty = pd.read_csv('p4da_bank_clients_loyalty.csv')  

# Отобразим первые три строки каждого датафрейма:
print(clients.head(3))
print(clients_city.head(3))
print(clients_loyalty.head(3)) 
```

```
   user_id gender   age
0    94561      Ж  32.0
1    94590      Ж  30.0
2    95384      М  30.0
   user_id         city
0   183012  Красногорск
1   136273  Красногорск
2   138832  Красногорск
   user_id  loyalty
0   183012      0.0
1   146556      1.0
2   120722      1.0 
```

В данных о картах лояльности есть ноль — похоже, у какого-то клиента была карта, но потом она истекла, или он от неё отказался. Данные соответствуют описанию и имеют общий столбец — `user_id`. Используем его для объединения данных.

Теперь выведем данные об этих датафреймах, чтобы оценить объём данных и их наполнение:

```
# Получим информацию о данных:
clients.info()
clients_city.info()
clients_loyalty.info()  
```

```
<class 'pandas.core.frame.DataFrame'>
RangeIndex: 9854 entries, 0 to 9853
Data columns (total 3 columns):
 #   Column   Non-Null Count  Dtype  
---  ------   --------------  -----  
 0   user_id  9854 non-null   int64  
 1   gender   9854 non-null   object 
 2   age      9851 non-null   float64
dtypes: float64(1), int64(1), object(1)
memory usage: 231.1+ KB
<class 'pandas.core.frame.DataFrame'>
RangeIndex: 9854 entries, 0 to 9853
Data columns (total 2 columns):
 #   Column   Non-Null Count  Dtype 
---  ------   --------------  ----- 
 0   user_id  9854 non-null   int64 
 1   city     9113 non-null   object
dtypes: int64(1), object(1)
memory usage: 154.1+ KB
<class 'pandas.core.frame.DataFrame'>
RangeIndex: 9854 entries, 0 to 9853
Data columns (total 2 columns):
 #   Column   Non-Null Count  Dtype  
---  ------   --------------  -----  
 0   user_id  9854 non-null   int64  
 1   loyalty  9313 non-null   float64
dtypes: float64(1), int64(1)
memory usage: 154.1 KB 
```

Как видно, в данных есть пропуски по информации, но общее количество клиентов — 9854 человек. Поскольку нужно объединить три таблицы по одному полю, используем метод `.join()`. И раз этот метод использует по умолчанию левое присоединение, присоединим все данные к датафрейму с информацией о клиентах:

```
# Объединим данные в одну таблицу:
joined_df = clients.set_index('user_id').join([clients_city.set_index('user_id'),
                                             clients_loyalty.set_index('user_id')])

# Сбросим индекс:
joined_df = joined_df.reset_index()    

# Отобразим первые три строки:
print(joined_df.head(3))                          
```

```
   user_id gender   age         city  loyalty
0   183012      Ж  25.0  Красногорск      0.0
1   146556      Ж  37.0  Красногорск      1.0
2   120722      Ж  30.0  Красногорск      1.0 
```

Объединение прошло успешно! Теперь посмотрим на информацию об объединённом датафрейме:

```
# Посмотрим информацию о объединенном датафрейме:
print(joined_df.info())                      
```

```
<class 'pandas.core.frame.DataFrame'>
RangeIndex: 9854 entries, 0 to 9853
Data columns (total 5 columns):
 #   Column   Non-Null Count  Dtype  
---  ------   --------------  -----  
 0   user_id  9854 non-null   int64  
 1   gender   9854 non-null   object 
 2   age      9851 non-null   float64
 3   city     9113 non-null   object 
 4   loyalty  9313 non-null   float64
dtypes: float64(2), int64(1), object(2)
memory usage: 385.0+ KB 
```

Как видно не все клиенты имеют информацию о возрасте, городе и наличии карты лояльности.

Теперь можно объединить данные и проверить долю клиентов с картой лояльности в разрезе города и пола:

```
# Сгруппируем данные по городу и подсчитаем колчиество клиентов
# с картой лояльности - with_card и без - wo_card:
result = joined_df.pivot_table(index=['city','gender'], columns='loyalty',
                      values='user_id', aggfunc='nunique',
                              ).reset_index()

# Сменим названия столбцов:
result.columns = ['city','gender','with_card', 'wo_card']

# Рассчитаем доли клиентов с картой лояльности:
result['share'] = result['with_card'] / (result['with_card'] + result['wo_card'])

# Отобразим результат:
print(result)           
```

```
          city gender  with_card  wo_card     share
0  Красногорск      Ж        469      705  0.399489
1  Красногорск      М        343      779  0.305704
2       Мытищи      Ж        842     1647  0.338288
3       Мытищи      М        552     2041  0.212881
4        Химки      Ж        310      322  0.490506
5        Химки      М        224      379  0.371476 
```

Видим, что больше всего лояльных клиентов среди женщин по всем городам. А Химки в целом показывают большую долю клиентов с картами лояльности.

Таким образом данные могут храниться в разных датафреймах, и одна из главных задач аналитики при исследовании данных — понять, какие данные и из каких датафреймов необходимо собрать в единый датафрейм. От этого зависит выбор типа объединения данных и метода.

Теории было много, перейдём к практике.

# Время вперёд!
## Временной фактор
Анализ данных можно разделить на множество видов. Все они могут пересекаться между собой, но решать разные задачи бизнеса.

Данные можно рассматривать как в статике, так и в динамике.

Например, менеджер Юра сегодня заработал 100 000 рублей для компании. Можно ли говорить о том, что менеджер хорошо работает? Сегодня — да. Стоит ли возлагать на Юру в дальнейшем сложные задачи, повышать по карьерной лестнице или увеличивать Юре зарплату? Для таких выводов данных недостаточно: один удачный день не показывает общую картину.

На ум приходит динамическая задача — оценить работу менеджера в течение года. И в итоге может выясниться, что этот успех — единичный случай, а всё остальное время менеджер не показывал высоких результатов.

Данные, которые выбиваются из общего порядка, — выбросы — могут появляться как случайно, так и закономерно. К закономерным выбросам можно отнести ежегодное увеличение выручки в декабре, покупки в марте исключительно мужчинами или увеличение продаж канцтоваров и оргтехники в августе.

Динамический анализ данных более точно опишет картину, чем анализ в одной временной точке. Если оценивать доход компании только за декабрь, то можно ошибочно сделать вывод о том, что компания справляется неплохо. Но если оценить доходы в разрезе всего года и оценить выручку по месяцам, можно получить данные о выбросе: окажется, что в декабре сильно выросли продажи в связи с новогодними праздниками. А в остальные месяцы доходы компании были достаточно скромными.

При оценке доходности по времени анализ получается более точным. И, например, если случится, что покупательная способность в декабре упадёт, компания может обанкротиться, ведь предыдущий рост появился исключительно за счёт декабря. Такой анализ позволит сделать совсем другие выводы.

Анализ с учётом времени позволит не допустить ошибок, сделать прогноз, обезопасить компанию от рисков.

## Природа изменений во времени
Теперь понятно: анализировать данные с учётом времени очень важно. Это помогает, например, прогнозировать цены на акции или обнаруживать аномальный рост заходов на сайт и решать множество других задач. Такой анализ изменений данных во времени — большая область задач: аналитики выявляют тренды, исследуют, как показатели меняются в зависимости от сезона или времени дня.

В этом уроке вы узнаете, как видеть природу изменений во времени, что такое временной ряд и язык временных рядов и зачем вообще аналитику его понимать.

Изучение природы изменений во времени пришло в аналитику из науки, в основном из эконометрики, оттуда же аналитика позаимствовала и термины. С ними, скорее всего, вы ещё не сталкивались.

Например, показатели, меняющиеся во времени, обычно называют **временным рядом**. Чтобы научиться работать с такими данными, важно уметь их описывать, а для этого нужно познакомиться с основными терминами. Назовём это языком временных рядов.

### Язык временных рядов

#### **Тренд**

Показатели могут расти или падать. Тренд — это долгосрочное направление временного ряда. Тренд называют положительным, если показатели растут, или отрицательным — если падают.

#### **Цикличность**

Иногда поведение данных может повторяться. Например, раз в некоторое время показатели растут, а затем падают. Это называют циклами во временных рядах, а наличие таких циклов во временном ряде — цикличностью.

![](https://pictures.s3.yandex.net/resources/image_1716485766.png)

#### **Сезонность**

Сезонность, по сути, и есть цикличность, но связанная с сезонными событиями. Например, продажи билетов в парке аттракционов растут по выходным, а по будням падают, а продажи ёлочных игрушек растут под Новый год. Как правило, мы описываем временной ряд, используя сезонность, а не просто цикличность.

Временной ряд может одновременно иметь и тренд, и сезонность. Например, магазин игрушек растит обороты из года в год, то есть имеет положительный тренд, и при этом под Новый год имеет сезонность к увеличению продаж.

![](https://pictures.s3.yandex.net/resources/image_1716485781.png)

#### **Волатильность**

Временной ряд может быть изменчивым, линия превращается будто в пилу, значения прыгают то вверх, то вниз. Такой ряд называют волатильным.

![](https://pictures.s3.yandex.net/resources/image_1716485797.png)

Бывает так, что поведение линии резко меняется, часть графика гладкая, а другая волатильная. Такой волатильный участок иногда называют флуктуациями в данных.

![](https://pictures.s3.yandex.net/resources/image_1716485815.png)

#### **Шум**

Шум — это случайная компонента временного ряда. Иначе говоря, это то, что вносит случайность в данные. Шум есть практически всегда.

В анализе временные ряды стараются разложить на несколько основных компонент: тренд, сезонность и шум.

Взгляните ещё на один временной ряд: здесь прослеживаются и тренд, и сезонность.

![](https://pictures.s3.yandex.net/resources/image_1716485834.png)

Выделим из него каждую компоненту:

Шум, статистические характеристики которого не меняются со временем: среднее или дисперсия, — называют белым шумом.

![](https://pictures.s3.yandex.net/resources/image_1716485880.png)

#### **Нестационарность**

Временной ряд меняет свои значения во времени (например, среднее, дисперсию) или имеет тренд, и это нормально. Временной ряд, статистические характеристики которого меняются со временем, называют нестационарным. Как правило, работать приходится с нестационарными временными рядами.

Но бывают и стационарные ряды, характеристики которых не меняются. Например, белый шум — это стационарный ряд. Такой ряд просто моделировать, поэтому в задачах прогнозирования стараются привести ряд к стационарному виду.

#### **Гетероскедастичность**

За таким сложным словом скрывается свойство, при котором волатильность временного ряда меняется со временем. Значения меняются то сильно, то слабо.

![](https://pictures.s3.yandex.net/resources/image_1716485909.png)

Ряд, у которого волатильность сохраняется, называют гомоскедастичным.

![](https://pictures.s3.yandex.net/resources/image_1716485927.png)

Отлично! Вы научились понимать язык временных рядов.

### Итоги

Анализ изменений данных во времени позволяет выявлять тренды, исследовать сезонные изменения и прогнозировать поведение показателей. В такой работе вы будете сталкиваться с терминами «тренд», «цикличность», «сезонность», «волатильность» и «шум». Эти термины помогают в описании природы временного ряда и поэтому пригодятся в следующем уроке. Он посвящён пропускам во временных рядах.

## Пропуски временного ряда
В прошлом уроке вы узнали много новых терминов для описания временного ряда.

**В этом уроке вы узнаете**

- Как работать с пропусками в данных

Мы уже касались темы пропусков и выделяли контекстуальные пропуски. Они часто возникают во временных рядах, и для их заполнения используют разные подходы.

### Пропуски в данных

По разным причинам во временных рядах могут быть пропуски. Информация могла просто отсутствовать, а может, были технические сбои. В любом случае с пропусками нужно что-то делать. Если мы хотим сохранить целостность и достоверность данных, то нам нужно чем-то заполнить эти пропуски, а не просто удалить их.

Чем же заполнить пропуски? Есть несколько путей, как это можно сделать, и Pandas нам в этом сильно поможет. А то, какой способ выбрать, зависит от данных, с которыми вы работаете, и задачи, которую решаете.

Будем работать со следующими данными

```
import pandas as pd
import numpy as np

data = [
    {'date': '2023-01-01', 'value': 100},
    {'date': '2023-02-01', 'value': 110},
    {'date': '2023-03-01', 'value': 115},
    {'date': '2023-04-01', 'value': 90},
    {'date': '2023-05-01', 'value': np.nan},
    {'date': '2023-06-01', 'value': 100},
    {'date': '2023-07-01', 'value': 95},
    {'date': '2023-08-01', 'value': 105},
    {'date': '2023-09-01', 'value': 110},
    {'date': '2023-10-01', 'value': 97},
    {'date': '2023-11-01', 'value': np.nan},
    {'date': '2023-12-01', 'value': np.nan},
    {'date': '2024-01-01', 'value': 105},
    {'date': '2024-02-01', 'value': 100},
    {'date': '2024-03-01', 'value': 110}
]

df = pd.DataFrame(data) 
```

||date|value|
|---|---|---|
|0|2023-01-01|100|
|1|2023-02-01|110|
|2|2023-03-01|115|
|3|2023-04-01|90|
|4|2023-05-01|nan|
|5|2023-06-01|100|
|6|2023-07-01|95|
|7|2023-08-01|105|
|8|2023-09-01|110|
|9|2023-10-01|97|
|10|2023-11-01|nan|
|11|2023-12-01|nan|
|12|2024-01-01|105|
|13|2024-02-01|100|
|14|2024-03-01|110|

Выведем график, пока никак не заполняя пропуски.

```
from matplotlib import pyplot as plt

plt.plot(df['date'], df['value'], color='blue', marker='o', markersize=5)

plt.xticks(rotation=30)
plt.ylim(60, 150)
plt.grid(True)
plt.legend(['raw'])
plt.title('Значения с пропусками')

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1717168052.png)

### Заполнение средним или медианой

Это самый простой способ, который в первую очередь приходит в голову — найдём среднее значение или медиану от всей выборки, и заполним все пропуски этим значением. На графике эти точки выделим красным цветом.

```
# Создадим новую колонку
# Заполним все пропуски средним выборки
df['filled_value'] = df['value'].fillna(df['value'].mean())

# Линия с заполненными точками
plt.plot(df['date'], df['filled_value'], color='r', marker='o', markersize=5)
# Изначальная линия с пропусками
plt.plot(df['date'], df['value'], color='blue', marker='o', markersize=5)

plt.xticks(rotation=30)
plt.ylim(60, 150)
plt.grid(True)
plt.legend(['filled', 'raw'])
plt.title('Заполнили средним')

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1717168083.png)

Точно так же мы можем поступить и с медианой.

```
# Создадим новую колонку
# Заполним все пропуски медианой выборки
df['filled_value'] = df['value'].fillna(df['value'].median())

# Линия с заполненными точками
plt.plot(df['date'], df['filled_value'], color='r', marker='o', markersize=5)
# Изначальная линия с пропусками
plt.plot(df['date'], df['value'], color='blue', marker='o', markersize=5)

plt.xticks(rotation=30)
plt.ylim(60, 150)
plt.grid(True)
plt.legend(['filled', 'raw'])
plt.title('Заполнили средним')

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1717168115.png)

В нашем случае среднее не сильно отличается от медианы. Если временной ряд волатильный, то лучше использовать медиану, иначе лучше использовать среднее.

Такой способ подойдёт, если временной ряд без тренда и пропущено небольшое количество подряд идущих значений — одно или два. Он не подойдёт, если у вас есть длинные пропуски или временной ряд имеет долгосрочный тренд.

На заметку

Временной ряд без тренда называется **стационарным**.

### Заполнение предыдущим или последующим значением

Ещё один способ заполнить пропуски — просто повторять последнее непропущенное значение или повторять первое значение после пропуска.

Для этого в Pandas есть специальные методы `ffill()` для заполнения последним непустым значением `bfill()` для заполнения первым значением после пропуска.

```
# Заполним пропуски
df['filled_value'] = df['value'].ffill()

# Линия с заполненными точками
plt.plot(df['date'], df['filled_value'], color='r', marker='o', markersize=5)
# Изначальная линия с пропусками
plt.plot(df['date'], df['value'], color='blue', marker='o', markersize=5)

plt.xticks(rotation=30)
plt.ylim(60, 150)
plt.grid(True)
plt.legend(['filled', 'raw'])
plt.title('Заполнили последним непустым значением')

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1717168175.png)

```
# Заполним пропуски
df['filled_value'] = df['value'].bfill()

plt.plot(df['date'], df['filled_value'], color='r', marker='o', markersize=5)
plt.plot(df['date'], df['value'], color='blue', marker='o', markersize=5)

plt.xticks(rotation=30)
plt.ylim(60, 150)
plt.grid(True)
plt.legend(['filled', 'raw'])
plt.title('Заполнили первым значением после пропуска')

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1717168213.png)

Такой способ неплохо подходит для рядов с длинными пропусками.

### Интерполяция

Интерполяция работает следующим образом — она находит точки до и после пропуска. Затем она использует математические формулы, чтобы смоделировать пропущенное значение, основываясь на известных данных. Бывают разные виды интерполяции, основные две — это линейная и полиномиальная, хотя на деле они похожи.

В случае **линейной** интерполяции мы представляем, как расположились бы точки, если конец и начало пропуска были бы на одной прямой линии. Прямую линию описывает функция вида `y = kx + b`.

![](https://pictures.s3.yandex.net/resources/image_1717168450.png)

**Полиномиальная** интерполяция работает так же, но в этом случае мы используем функцию второй и выше степени, например квадратичную `y = ax^2 + bx + c`. Функция, которую используют при интерполяции, называется полиномом. Линейная интерполяция тоже основана на полиноме, но первой степени, а квадратичная функция — это полином второй степени и так далее. Обычно при полиномиальной интерполяции используют полином второй степени.

![](https://pictures.s3.yandex.net/resources/image_1717168344.png)

Для интерполяции в Pandas есть специальный метод `interpolate()`. Параметр `method` указывает тип интерполяции.

```
# Линейная интерполяция
df['filled_value'] = df['value'].interpolate(method='linear')

plt.plot(df['date'], df['filled_value'], color='r', marker='o', markersize=5)
plt.plot(df['date'], df['value'], color='blue', marker='o', markersize=5)

plt.xticks(rotation=30)
plt.ylim(60, 150)
plt.grid(True)
plt.legend(['filled', 'raw'])
plt.title('Линейная интерполяция')

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1717168365.png)

Используя полиномиальную интерполяцию, параметр `order` укажет степень полинома.

```
# Интерполяция полиномом второй степени
df['filled_value'] = df['value'].interpolate(method='polynomial', order=2)

plt.plot(df['date'], df['filled_value'], color='r', marker='o', markersize=5)
plt.plot(df['date'], df['value'], color='blue', marker='o', markersize=5)

plt.xticks(rotation=30)
plt.ylim(60, 150)
plt.grid(True)
plt.legend(['filled', 'raw'])
plt.title('Интерполяция полиномом 2-й степени')

plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1717168393.png)

Интерполирование используют, когда нам нужно заполнить длинные пропуски. Но и для коротких пропусков тоже отлично подойдёт.

### Итоги

Во временных рядах могут быть пропуски. Есть несколько способов того, как их заполнить:

- заменить все пропуски средним или медианой;
- заполнить первым или последним непустым значением;
- использовать интерполяцию.

Выбор метода заполнения пропусков зависит от данных, с которыми вы работаете. Например, длинные пропуски лучше интерполировать, а точечные пропуски в рядах с явным трендом лучше заполнять последним или первым непустым значением.

## Группируем по периодам
В прошлых уроках вы ознакомились с временными рядами — научились их описывать и заполнять пропуски.

В этом уроке вы узнаете:

- как показать изменение данных во времени;
- как группируют данные по периодам.

### Как меняются метрики

Когда от нас требуется показать изменение метрики во времени, мы выбираем интервал времени и группируем данные по нему. Вы уже занимались этим, когда учились строить линейные графики.

На графике этот интервал — это промежуток между точками. Мы собираем всю нужную информацию за интервал времени, будь то сумма значений, количество пользователей или нечто другое, и вкладываем в одну точку. Например, на графике точка 11 мая отражает всю информацию за всё 11 мая, то есть за период 11–12 мая.

![](https://pictures.s3.yandex.net/resources/image_1716054645.png)

Подобрать правильный интервал — важное дело. Этот интервал зависит от задачи — где-то мы группируем по кварталам, где-то по месяцам, а иногда мы группируем по минутам или даже по секундам.

Вы уже решали похожие задачи, когда показывали изменение метрики DAU, например. В случае DAU мы округляем время до дня и группируем количество заходов по этим интервалам. Примерно так выглядел наш код.

```
df['day'] = df['time'].dt.day
df.groupby('day').agg({'user_id': 'nunique'}) 
```

Просто и лаконично.

Но что делать, если нам нужен не такой стандартный интервал (например, 2 часа и 15 минут)? Такие, неудобные, интервалы мы часто встречаем в жизни, допустим, на работе могут быть смены по 8 часов, рекламные паузы по телевизору показывают интервалами по 20 минут, а автобус на остановке приходит каждые 35 минут. Мы можем как-то округлять время к нужному виду известными способами, но сделать это будет сложно. Для решения такой задачи в Pandas есть метод `resample()` — он помогает группировать данные по произвольным периодам времени.

```
df.resample(rule, offset) 
```

- `df` — датафрейм, индексом у которого считается время;
- `rule` — правило группировки в виде строки. Например, `5min` для группировки по 5-минутным интервалам или `D` для группировки по дням;
- `offset` — смещение интервала, оно также задаётся строкой. Например, `offset='30min'` сместит интервалы на 30 минут вправо.

Сейчас покажем, как с этим работать.

### Группируем по периодам

Для наглядности будем работать с данными из `events.csv` — это данные о действиях пользователя на сайте.

Колонка `event_time` — время события, `uid` — идентификатор пользователя.

```
import pandas as pd

df = pd.read_csv('https://code.s3.yandex.net/datasets/events.csv')
df.head() 
```

|**event_name**|event_time|uid|
|---|---|---|---|
|0|pageview|2019-08-20 00:27:30|80419792|
|1|pageview|2019-01-29 08:11:30|74610198|
|2|pageview|2019-01-29 01:33:02|101714|
|3|pageview|2019-02-22 19:41:04|90089350|
|4|pageview|2019-03-04 12:49:09|98247028|

Покажем, как менялось количество пользователей по дням. Но теперь не будем создавать новую колонку, используя округление дат c `dt`, а сразу применим `resample()`.

Для начала подготовим данные к анализу, приведём `event_time` к `datetime` и оставим только события `pageview` — это событие захода на страницу.

```
df = df[df['event_name'] == 'pageview']
df['event_time'] = pd.to_datetime(df['event_time']) 
```

По умолчанию, метод `resample()` ожидает, что индексом датафрейма будет время. Назначим дату индексом.

```
df = df.set_index('event_time')
df.head() 
```

|**event_time**|event_name|uid|
|---|---|---|
|2019-08-20 00:27:30|pageview|80419792|
|2019-01-29 08:11:30|pageview|74610198|
|2019-01-29 01:33:02|pageview|101714|
|2019-02-22 19:41:04|pageview|90089350|
|2019-03-04 12:49:09|pageview|98247028|

#### **Группируем**

А теперь сгруппируем данные по дню и посчитаем количество заходов. Укажем аргумент `D` — это обозначение интервала в 1 день.

```
df_res = df['uid'].resample('D').count()
df_res.head() 
```

|**event_time**|uid|
|---|---|
|2019-01-01 00:00:00|44|
|2019-01-02 00:00:00|30|
|2019-01-03 00:00:00|27|
|2019-01-04 00:00:00|24|
|2019-01-05 00:00:00|42|

Но мы и раньше могли это сделать. А теперь попробуем сгруппировать данные сразу по нескольким дням.

```
df_res = df['uid'].resample('2D').count()
df_res.head() 
```

|**event_time**|uid|
|---|---|
|2019-01-01 00:00:00|74|
|2019-01-03 00:00:00|51|
|2019-01-05 00:00:00|76|
|2019-01-07 00:00:00|80|
|2019-01-09 00:00:00|62|

Уже становится интересно. `D` — это алиас или обозначение интервала времени, мы можем добавить к нему число и кратно увеличим этот период, `2D` — 2 дня, `7D` — 7 дней. Полный набор алиасов можно посмотреть в [документации Pandas](https://pandas.pydata.org/docs/user_guide/timeseries.html#timeseries-offset-aliases).

Точно так же мы можем сгруппировать данные по часам. Например, сгруппируем данные по 12 часам, одна точка будет показывать трафик днём, другая ночью.

```
df_res = df['uid'].resample('12h').count()
df_res.head() 
```

|**event_time**|uid|
|---|---|
|2019-01-01 00:00:00|19|
|2019-01-01 12:00:00|25|
|2019-01-02 00:00:00|17|
|2019-01-02 12:00:00|13|
|2019-01-03 00:00:00|14|

Мы можем совмещать периоды, например, интервал 2 часа и 30 минут можно задать вот так:

```
df['uid'].resample('2h+30min').count() 
```

#### **Сдвигаем**

Наш интервал всегда начинается в 00:00, мы можем сдвинуть его, используя параметр `offset`, например, сдвинем всё на 9 часов. Тогда ночной интервал будет с 21:00 до 09:00, а дневной с 09:00 до 21:00, звучит более правдоподобно.

```
df_res = df['uid'].resample('12h', offset='9h').count()
df_res.head() 
```

Смещение тоже может состоять из нескольких частей, например, вот так мы сместим интервалы на 3 часа 45 минут:

```
df['uid'].resample('12h', offset='3h+45min').count() 
```

#### **Агрегируем**

Мы использовали функцию `count()` для подсчёта количества заходов, но использовать мы можем любую другую агрегирующую функцию — `unique`, `sum`, `max`/`min` и другие, всё как в обычной группировке. А если нужна специфичная функция, мы можем использовать метод `apply()`. Например, мы можем показать, какие пользователи попали в каждый из интервалов.

```
def group_distinct_array(x):
    return x.unique()

df_res = df['uid'].resample('12h', offset='9h').apply(group_distinct_array)
df_res.head() 
```

|**event_time**|uid|
|---|---|
|2018-12-31 21:00:00|[6974978, 53964311, 34000489, 65188910, 248977...|
|2019-01-01 09:00:00|[93314183, 69090691, 92875340, 48010573, 22672...|
|2019-01-01 21:00:00|[74782611, 17428361, 10372210, 52248252, 72795...|
|2019-01-02 09:00:00|[60076281, 89540374, 96928660, 8993021, 321268...|
|2019-01-02 21:00:00|[68052102, 16985865, 14519789, 8133687, 282132...|

### Итоги

- важно уметь подобрать правильный интервал времени для группировки;
- для группировки по произвольным интервалам используют метод `resample()`, а задавать частоту интервалов и их смещение мы можем с параметром `offset`.

## Скользим и сдвигаем
Теперь вы уже умеете группировать данные по произвольным временным интервалам. А в этом уроке узнаете, что такое скользящее среднее и при чём тут окно, как и зачем сдвигают временные ряды, что такое автокорреляция и как её рассчитать.

Планов много — поехали!

### Скользим

Представьте, что перед вами данные, которые меняются со временем. Например, цена акции или количество пользователей, посетивших сайт, как это было в прошлом уроке. На каждый период времени, будь то час, день, неделя или месяц, есть своя запись: сегодня было 10 посетителей, а вчера было 110. Иногда эти данные могут быть волатильными — скакать то вверх, то вниз. В таком случае бывает тяжело понять, что происходит на самом деле, делать выводы и строить прогнозы.

**Скользящее среднее** — способ сгладить эти резкие изменения, чтобы увидеть общую тенденцию в данных. Иногда скользящее среднее обозначают MA — сокращение от английского _moving average_.

Чтобы разобраться, как работает скользящее среднее, возьмём, например, первые пять значений временного ряда и посчитаем среднее среди них. Это значение и будет первым значением скользящего среднего.

Множество значений, среди которых посчитали среднее, называется окном, его размер — пять значений.

![](https://pictures.s3.yandex.net/resources/image_1716959579.png)

Потом нужно взять следующие пять значений и опять посчитать среднее. Так получится второе значение скользящего среднего. Алгоритм понятен.

![](https://pictures.s3.yandex.net/resources/image_1716959597.png)

И так мы продолжаем до самого конца, перемещая окно всё дальше. Окно будто скользит, оттого это и называют скользящим средним.

![](https://pictures.s3.yandex.net/resources/image_1716959611.png)

В результате получается новый ряд, который сглаживает резкие скачки и показывает общее направление метрики — рост, падение или полный штиль. Сглаживание помогает лучше понять данные, особенно когда они волатильные и шумные.

В конце ряда может получиться так, что нерассчитанных значений останется меньше, чем размер окна, а значит, рассчитать среднее мы не сможем. В этом случае останутся пропуски.

![](https://pictures.s3.yandex.net/resources/image_1716959628.png)

Есть опция — сделать окно динамическим. В таком случае под конец окно будет сжиматься в размере и мы всё так же будем рассчитывать среднее — просто по окну другого размера.

![](https://pictures.s3.yandex.net/resources/image_1716959656.png)

В примере мы рассчитываем окно по верхнему краю, или точнее — по левому краю, если расположить ряд горизонтально. Но можно считать и по-другому: по правому краю или по центру — это называют центральным окном.

Способ расчёта влияет на то, в каком месте будет записано новое, сглаженное значение. Перед решением задачи важно определиться, какой способ вам подходит.

![](https://pictures.s3.yandex.net/resources/image_1716959684.png)

В окне можно посчитать не только среднее, но и медиану, квантиль и вообще любую другую статистическую функцию. Хотя аналитики чаще всего используют именно среднее.

Рассчитать скользящее среднее можно прямо в датафрейме, используя только Pandas. В этом поможет метод `rolling()` со следующими параметрами:

- `window` — размер окна;
- `min_periods` — минимальное количество наблюдений в окне, чтобы посчитать значение (установив значение 1, мы включаем динамическое окно);
- `freq` — размер окна, но во временных интервалах (например, три дня `'3D'`);
- `center` — флаг True/False обозначает, нужно ли рассчитывать центральное окно.

```
df.rolling(5) 
```

Метод `rolling` только формирует скользящие окна, но ещё не рассчитывает значения в них. Для этого надо указать агрегирующую функцию — в нашем случае среднее.

```
df.rolling(5).mean() # Скользящее среднее 
```

В основу задачи возьмём данные:

```
import pandas as pd

data = [
    {'date': '2024-01-01', 'value': 1000},
    {'date': '2024-01-02', 'value': 10000},
    {'date': '2024-01-03', 'value': 500},
    {'date': '2024-01-04', 'value': 250},
    {'date': '2024-01-05', 'value': 1250},
    {'date': '2024-01-06', 'value': 6500},
    {'date': '2024-01-07', 'value': 3100},
    {'date': '2024-01-08', 'value': 500},
    {'date': '2024-01-09', 'value': 1500},
    {'date': '2024-01-10', 'value': 5400},
    {'date': '2024-01-11', 'value': 2000},
    {'date': '2024-01-12', 'value': 1000},
    {'date': '2024-01-13', 'value': 5400},
    {'date': '2024-01-14', 'value': 7800}
]

df = pd.DataFrame(data).set_index('date') 
```

|date|value|
|---|---|
|2024-01-01|1000|
|2024-01-02|10000|
|2024-01-03|500|
|2024-01-04|250|
|2024-01-05|1250|
|2024-01-06|6500|
|2024-01-07|3100|
|2024-01-08|500|
|2024-01-09|1500|
|2024-01-10|5400|
|2024-01-11|2000|
|2024-01-12|1000|

Как видно, данные довольно волатильны. Выведем график:

```
from matplotlib import pyplot as plt

plt.plot(df.index, df['value'])
plt.xticks(rotation=30)
plt.legend(['value', '3 MA'])
plt.grid(True)
plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1716959718.png)

На этих данных актуально применить скользящее среднее. Найдём его для колонки `value` с динамическим окном в три значения и сохраним результат в колонке `ma_3`.

```
df['ma_3'] = df['value'].rolling(3, min_periods=1).mean() 
```

|date|value|ma_3|
|---|---|---|
|2024-01-01|1000|1000|
|2024-01-02|10000|5500|
|2024-01-03|500|3833.33|
|2024-01-04|250|3583.33|
|2024-01-05|1250|666.667|
|2024-01-06|6500|2666.67|
|2024-01-07|3100|3616.67|
|2024-01-08|500|3366.67|
|2024-01-09|1500|1700|
|2024-01-10|5400|2466.67|
|2024-01-11|2000|2966.67|
|2024-01-12|1000|2800|
|2024-01-13|5400|2800|
|2024-01-14|7800|4733.33|

А теперь сравним изначальные данные и скользящее.

```
plt.plot(df.index, df['value'], marker='o', markersize=5)
plt.plot(df_ma_3.index, df['ma_3'], marker='o', markersize=5)
plt.xticks(rotation=30)
plt.legend(['value', '3 MA'])
plt.grid(True)
plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1716959756.png)

Уже сейчас видно, что удалось сгладить разброс значений.

Чем больше размер окна, тем сильнее мы сглаживаем значения. Попробуем увеличить размер окна до восьми измерений.

```
df['ma_8'] = df['value'].rolling(8, min_periods=1).mean() 
```

```
plt.plot(df.index, df['value'], marker='o', markersize=5)
plt.plot(df_ma_3.index, df['ma_3'], marker='o', markersize=5)
plt.plot(df_ma_8.index, df['ma_8'], marker='o', markersize=5)
plt.xticks(rotation=30)
plt.legend(['value', '3 MA', '8 MA'])
plt.grid(True)
plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1716959775.png)

Линия стала ещё более гладкой.

### Сдвигаем

Со скользящим разобрались. Теперь допустим, что у нас есть ряд значений — продажи каждый день.

```
data = [
    {'date': '2024-01-01', 'value': 650},
    {'date': '2024-01-02', 'value': 550},
    {'date': '2024-01-03', 'value': 750},
    {'date': '2024-01-04', 'value': 840},
    {'date': '2024-01-05', 'value': 870},
    {'date': '2024-01-06', 'value': 920},
    {'date': '2024-01-07', 'value': 930},
    {'date': '2024-01-08', 'value': 900},
    {'date': '2024-01-09', 'value': 850},
    {'date': '2024-01-10', 'value': 900},
    {'date': '2024-01-11', 'value': 950},
    {'date': '2024-01-12', 'value': 1000},
    {'date': '2024-01-13', 'value': 890},
    {'date': '2024-01-14', 'value': 950}
] 
```

В один день продали 650 товаров, а на следующий 550 — получилось на 100 товаров меньше. Мы можем не только скользить по этим данным, но и сдвигать их. Это пригодится, когда нужно найти разницу или дельту в данных за какой-то период.

Чтобы сдвигать данные, используют метод `shift()` с параметрами:

- `periods` — количество значений, на которые нужно сдвинуть данные;
- `freq` — то же, что и periods, но выраженное во временных интервалах (например, один день или один час);
- `fill_value` — значение, которым заполняются пропуски.

```
df.shift(periods=1) 
```

Сдвинем данные на одно значение:

```
df = pd.DataFrame(data=data)
df = df.set_index('date')

df['shifted_value_1'] = df['value'].shift(periods=1) 
```

|date|value|shifted_value_1|
|---|---|---|
|2024-01-01|650|nan|
|2024-01-02|550|650|
|2024-01-03|750|550|
|2024-01-04|840|750|
|2024-01-05|870|840|
|2024-01-06|920|870|
|2024-01-07|930|920|
|2024-01-08|900|930|
|2024-01-09|850|900|
|2024-01-10|900|850|
|2024-01-11|950|900|
|2024-01-12|1000|950|
|2024-01-13|890|1000|
|2024-01-14|950|890|

В данных появился NaN: это нормально, ведь мы сдвинули все значения. Используя параметр `fill_value`, мы можем сразу же заполнить новые пропуски (например, нулями).

```

df['shifted_value_1'] = df['value'].shift(periods=1, fill_value=0) 
```

|date|value|shifted_value_1|
|---|---|---|
|2024-01-01|650|0|
|2024-01-02|550|650|
|2024-01-03|750|550|
|2024-01-04|840|750|
|2024-01-05|870|840|
|2024-01-06|920|870|
|2024-01-07|930|920|
|2024-01-08|900|930|
|2024-01-09|850|900|
|2024-01-10|900|850|
|2024-01-11|950|900|
|2024-01-12|1000|950|
|2024-01-13|890|1000|
|2024-01-14|950|890|

А теперь найдём разницу — это покажет прирост в данных день ко дню.

```
df['delta'] = df['value'] - df['shifted_value_1'] 
```

|date|value|shifted_value_1|delta|
|---|---|---|---|
|2024-01-01|650|0|650|
|2024-01-02|550|650|-100|
|2024-01-03|750|550|200|
|2024-01-04|840|750|90|
|2024-01-05|870|840|30|
|2024-01-06|920|870|50|
|2024-01-07|930|920|10|
|2024-01-08|900|930|-30|
|2024-01-09|850|900|-50|
|2024-01-10|900|850|50|
|2024-01-11|950|900|50|
|2024-01-12|1000|950|50|
|2024-01-13|890|1000|-110|
|2024-01-14|950|890|60|

### Автокорреляция

Возможность сдвигать данные позволяет найти автокорреляцию.

**Автокорреляция** — это корреляция данных со значениями в прошлом. Коррелируют ли продажи неделю назад с продажами сегодня? А с продажами за прошлый день? На такие вопросы и отвечает автокорреляция.

Посчитаем корреляцию со значениями недельной давности, используя метод `shift()`:

```
# Сдвинем значения на неделю:
df['shifted_value_7'] = df['value'].shift(periods=7)
# Найдём коэффициент корреляции:
df['value'].corr(df['shifted_value_7']) 
```

```
0.66 
```

Есть и другой способ найти автокорреляцию — использовать метод `autocorr()` с параметром `lag`, который указывает смещение в данных. По сути, `lag` в `autocorr()` то же самое, что `periods` в `shift()`.

Метод делает то же самое, но в одну строчку: сдвигает и ищет корреляцию.

```
df['value'].autocorr(lag=7) 
```

```
0.66 
```

С помощью графика мы можем показать, как меняется коэффициент автокорреляции в зависимости от размера лага или смещения. Для этого воспользуемся модулем `plotting` и встроенным методом `autocorrelation_plot()`.

```
pd.plotting.autocorrelation_plot(df['value'])  
plt.show() 
```

![](https://pictures.s3.yandex.net/resources/image_1716959822.png)

Такой подход поможет найти цикличность в данных, например, увидеть, что раз в месяц продажи ведут себя похожим образом.

Выберите все возможные виды скользящего

Правильный ответ

Скользящее среднее

Мы можем рассчитывать любую агрегирующую функцию в скользящем окне.

Правильный ответ

Скользящая медиана

Мы можем рассчитывать любую агрегирующую функцию в скользящем окне.

Тоже правильный ответ

Скользящая дисперсия

Мы можем рассчитывать любую агрегирующую функцию в скользящем окне.

Тоже правильный ответ

Скользящий максимум

Мы можем рассчитывать любую агрегирующую функцию в скользящем окне.

### Итоги

- Скользящее среднее — это способ сгладить волатильные данные и увидеть общую тенденцию. Для расчёта скользящего среднего используют окно, оно может быть фиксированного размера или динамическим. Для расчёта скользящего среднего используют метод `rolling()`.
- Сдвиг в данных позволяет находить изменения в данных за период (например, дельту или разницу). Для сдвига используют метод `shift()`.
- Автокорреляция показывает, как данные коррелируют со значениями в прошлом. Это помогает находить цикличность в данных. Для расчёта автокорреляции используют метод `autocorr()`.