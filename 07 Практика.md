---
type: note
aliases:
  - "? 07 Практика"
tags: []
parent:
  - - Python для анализа данных
next: "[[08 Основы машинного обучения]]"
prev: "[[07 Статистический анализ]]"
---
# Как проводить статистические тесты. Практика

## Шпаргалка

В этом уроке мы будем практиковать методы и функции Python для проведения одновыборочного t-теста:

- `scipy.stats.ttest_1samp` — функция для проведения одновыборочного t-теста с проверкой того, как среднее выборки отличается от предполагаемого значения генеральной совокупности и насколько это статистически значимо;
    - `popmean` — предполагаемое среднее значение генеральной совокупности, с которым сравнивается выборочное среднее;
- `print()` — функция для вывода результатов на экран.
- Статистические понятия:
    - `t-статистика` — показывает, насколько далеко среднее выборочных данных находится от предполагаемого среднего в единицах стандартного отклонения;
    - `p-value` — вероятность получить такие или более крайние значения t-статистики, если нулевая гипотеза верна. Малые значения p-value (например, меньше 0.05) указывают на то, что различия статистически значимы.


## Задание 1.

Представим, что вы продуктовый аналитик в команде, которая недавно запустила рекламную кампанию для увеличения пользовательской активности на новом лендинге.

Вам нужно оценить, достигла ли кампания своей цели по повышению вовлечённости пользователей. Один из ключевых показателей успеха — увеличение среднего времени, которое пользователи проводят на сайте.

Среднее время, которое люди проводили на сайте раньше, равно пяти минутам. Определите, увеличилось ли время, проведённое пользователями на лендинге, после запуска маркетинговой кампании.

```python
from scipy.stats import ttest_1samp
  
time_spent = [6, 7, 5, 7, 6, 8, 5, 6, 7, 5, 4, 5, 6, 4, 5]
popmean = 5
  
t_statistic, p_value = ttest_1samp(time_spent, popmean)
print(f"t-статистика: {t_statistic:.2f}, p-value: {p_value:.4f}")
```

## Задание 2.

Мы получили значение p-value. Теперь определите, стоит ли отвергать нулевую гипотезу на основе результатов t-теста из Задания 1. Для этого используйте стандартный уровень значимости (0.05) для большинства подобных исследований (`alpha`).

```python
from scipy.stats import ttest_1samp
  
time_spent = [6, 7, 5, 7, 6, 8, 5, 6, 7, 5, 4, 5, 6, 4, 5]
popmean = 5
  
t_statistic, p_value = ttest_1samp(time_spent, popmean)
  
alpha = 0.05
if p_value < alpha:
    print("Отвергаем нулевую гипотезу: есть значимые статистические различия.")
else:
    print("Не отвергаем нулевую гипотезу: статистически значимых различий нет.")
```

## Задание 3.

Теперь вам нужно оценить изменение количества покупок и общего уровня удовлетворённости клиентов после запуска маркетинговой кампании.

Предполагается, что среднее количество покупок до кампании (`popmean1`) составляло `1.5`, а средний уровень удовлетворённости клиентов (`popmean2`) был `7`.

Используйте заданные значения для проведения t-тестов. Такой множественный анализ поможет оценить общее влияние кампании на различные аспекты взаимодействия пользователей с нашим продуктом.

```python
from scipy.stats import ttest_1samp
  
purchases = [
    1, 2, 4, 1, 1, 3, 1, 1, 2, 1, 2, 2, 1, 1, 2, 1, 1, 2, 1, 3, 1, 1,
    1, 3, 2, 1, 1, 2, 1, 1, 1, 3, 2, 1, 3, 4, 2, 2, 1, 2, 1, 1, 1, 1,
    1, 3, 1, 2, 2, 1]
satisfaction = [
    6,  8,  7,  7,  8,  8,  9,  8,  8,  8,  7,  9,  8,  6,  8,  8,  7,
    8,  8,  8,  8,  5,  7,  7,  7,  8,  1,  8,  6,  7,  8,  8,  9,  7,
    7,  8,  6,  8,  7,  7,  7,  8,  6,  8,  8,  9,  7,  7, 10,  8]
  
# Средние значения до начала маркетинговой кампании или изменений
popmean1 = 1.5
popmean2 = 7
  
# Анализ количества покупок
t_stat_purchases, p_value_purchases = ttest_1samp(purchases, popmean1)
print(f"Количество покупок: t-статистика {t_stat_purchases:.2f}, p-value {p_value_purchases:.4f}")
  
  
# Анализ удовлетворённости клиентов
t_stat_satisfaction, p_value_satisfaction = ttest_1samp(satisfaction, popmean2)
print(f"Удовлетворённость: t-статистика {t_stat_satisfaction:.2f}, p-value {p_value_satisfaction:.4f}")
```


# Сравнение средних. Практика

`scipy.stats.ttest_ind(a, b, equal_var=False)` — функция для проведения t-теста для двух независимых выборок, где `a` и `b` — списки данных для каждой из групп. Параметр `equal_var` указывает, равны ли дисперсии данных выборок (по умолчанию предполагается, что дисперсии равны).

`sum(list)` / `len(list)` — один из способов расчёта среднего значения списка в Python.

## Задание 1.

Розничная торговая сеть провела масштабную рекламную кампанию для увеличения продаж в двух ключевых магазинах. Используя данные о продажах до (`sales_before`) и после (`sales_after`) кампании, нужно анализировать изменение средних ежемесячных продаж. Исходя из уровня значимости `0.05` определите, привела ли кампания к значимому увеличению продаж, с использованием t-теста.

```python
from scipy.stats import ttest_ind
  
sales_before = [200, 210, 190, 205, 195, 220, 230, 215, 185, 195, 210, 220]
sales_after = [220, 230, 215, 225, 210, 235, 245, 225, 215, 205, 220, 230]
  
t_stat, p_value = ttest_ind(sales_before, sales_after, equal_var=False)
print(f"t-statistic: {t_stat}, p-value: {p_value}")
```

## Задание 2.

После анализа результатов рекламной кампании нужно оценить качество обслуживания клиентов в тех же двух магазинах. Данные по времени обслуживания собраны для трёх различных временных сегментов: утро (`service_times_morning_before` и `service_times_morning_after`), день (`service_times_afternoon_before` и `service_times_afternoon_after`) и вечер (`service_times_evening_before` и `service_times_evening_after`). Необходимо оценить, были ли улучшения в обслуживании клиентов в определённые части дня после кампании, используя отдельные t-тесты для каждого временного сегмента.

```python
from scipy.stats import ttest_ind
  
service_times_morning_before = [15, 16, 15, 14, 16, 15, 16, 15, 14, 15]
service_times_morning_after = [13, 14, 13, 12, 13, 14, 12, 13, 14, 12]
service_times_afternoon_before = [16, 15, 14, 16, 17, 16, 15, 16, 17, 15]
service_times_afternoon_after = [14, 13, 12, 14, 13, 14, 13, 12, 14, 13]
service_times_evening_before = [17, 15, 17, 16, 15, 16, 17, 15, 16, 17]
service_times_evening_after = [16, 15, 16, 15, 16, 15, 16, 15, 15, 16]
  
t_stat_morning, p_value_morning = ttest_ind(service_times_morning_before, service_times_morning_after, equal_var=False)
t_stat_afternoon, p_value_afternoon = ttest_ind(service_times_afternoon_before, service_times_afternoon_after, equal_var=False)
t_stat_evening, p_value_evening = ttest_ind(service_times_evening_before, service_times_evening_after, equal_var=False)
  
print(f"Morning - t-statistic: {t_stat_morning}, p-value: {p_value_morning}")
print(f"Afternoon - t-statistic: {t_stat_afternoon}, p-value: {p_value_afternoon}")
print(f"Evening - t-statistic: {t_stat_evening}, p-value: {p_value_evening}")
```

## Задание 3.

После анализа продаж и качества обслуживания розничная сеть также заинтересовалась анализом влияния рекламной кампании на продажи разных категорий товаров. Компания выделила две категории — бытовая техника и товары для дома — и хочет узнать, увеличились ли продажи в них после рекламной кампании по сравнению с продажами до неё.

Используя данные о продажах до (sales_appliances_before, sales_home_goods_before) и после (sales_appliances_after, sales_home_goods_after) кампании, определите с помощью t-теста для независимых выборок, привела ли кампания к значимому увеличению продаж в каждой из категорий отдельно.

```python
import pandas as pd
from scipy.stats import ttest_ind
  
sales_df = pd.DataFrame({
    'sales': [110, 120, 115, 118, 105, 125, 130, 119, 117, 112, 123, 115, 120, 116, 118,
              100, 105, 98, 103, 102, 107, 95, 110, 104, 97, 99, 105, 101, 96, 108, 165,
              170, 175, 180, 160, 165, 172, 169, 167, 175, 168, 163, 171, 178, 166, 150,
              145, 160, 155, 150, 148, 152, 153, 147, 160, 149, 154, 145, 158, 151],
    'time': ['after'] * 15 + ['before'] * 15 + ['after'] * 15 + ['before'] * 15,
    'category': ['appliances'] * 30 + ['home_goods'] * 30
})
  
sales_appliances_before = sales_df.query('category == "appliances" and time == "before"')['sales']
sales_appliances_after = sales_df.query('category == "appliances" and time == "after"')['sales']
sales_home_goods_before = sales_df.query('category == "home_goods" and time == "before"')['sales']
sales_home_goods_after = sales_df.query('category == "home_goods" and time == "after"')['sales']
  
t_stat_appliances, p_value_appliances = ttest_ind(sales_appliances_before, sales_appliances_after, equal_var=True)
print(f"Appliances - t-statistic: {t_stat_appliances}, p-value: {p_value_appliances}")
t_stat_home_goods, p_value_home_goods = ttest_ind(sales_home_goods_before, sales_home_goods_after, equal_var=True)
print(f"Home Goods - t-statistic: {t_stat_home_goods}, p-value: {p_value_home_goods}")
```

# Асимметричные проверки. Практика

Для проведения односторонних t-тестов в Python методам `ttest_1samp` и `ttest_ind` нужно передать аргумент `alternative`.

Для одной выборки — то есть метода `ttest_1samp` — `alternative` задаёт направление сравнения истинного среднего (μμ) совокупности, из которой взята выборка, с заранее выбранным числом aa.

`alternative='less'` задаёт альтернативную гипотезу H1:μ<aH1​:μ<a.

## Задание 1.

Учитель игры на гитаре знает, что в среднем его ученики тратят 11 часов в неделю на практику. Учитель дал группе своих учеников код доступа к новому приложению, помогающему практиковаться, и попросил каждого из них прислать, сколько часов — с точностью до часа — этот ученик потратил на практику за неделю после получения доступа к новому приложению. Проверьте гипотезу учителя, что среднее количество часов практики увеличилось.

```python
from scipy import stats as st
  
# результаты группы учеников:
practice_hours_with_app = [11, 13, 11, 10, 9, 12, 13, 13, 14, 10, 14, 10, 12, 13]
  
hours_before_app = 11
  
alpha = 0.05
  
results = st.ttest_1samp(practice_hours_with_app, hours_before_app, alternative='greater')
  
# выведите на экран полученное p-значение
p_value = results.pvalue
print('p-значение:', p_value)
  
# сравните p-значение с заданным уровнем значимости alpha
if p_value < alpha:
    print('Отвергаем нулевую гипотезу')
else:
    print('Нет оснований отвергнуть нулевую гипотезу')
```

## Задание 2.

Менеджер онлайн-магазина думает, что пользователи, которые приходят от тематического блогера, более лояльны, чем те, кто приходит по баннерной рекламе, и совершают больше покупок в течение следующих трёх месяцев. Он собрал выборки из данных о покупках за три месяца тех, кто зарегистрировался на маркетплейсе по обоим каналам и совершил хотя бы одну покупку за это время.

Проверьте предположение менеджера.

```python
from scipy import stats as st
  
# количество покупок пользователями,
# пришедшими через два разных канала:
native_bloger_ad_purchases = [ 3, 5, 4, 2, 3, 2, 3, 1, 3, 2, 4, 3, 5,
    3, 4, 4, 5, 5, 2, 3, 4, 5, 5, 3, 6, 5, 4, 3, 6, 3, 4, 3, 2, 3, 3,
    6, 2, 3, 3, 1, 4, 4, 4, 4, 4, 3, 3, 4, 2, 2, 5, 5, 6, 2, 4, 3, 3,
    2, 4, 6, 3, 3, 2, 4, 1, 3, 4, 3, 5, 4]
banner_ad_purchases =[3, 1, 2, 1, 3, 4, 4, 2, 4, 2, 2, 5, 2, 2, 1, 4,
    3, 7, 2, 5, 1, 3, 2, 3, 3, 4, 1, 3, 4, 4, 3, 2, 2, 6, 2, 3, 3, 5,
    3, 2, 2, 2, 3, 4, 4, 2, 2, 4, 3, 2, 3, 6, 6, 5, 3, 4, 2, 2, 4, 2,
    2, 5, 2, 5, 4, 1, 4, 2, 2, 5]
  
alpha = 0.05
  
results = st.ttest_ind(native_bloger_ad_purchases, banner_ad_purchases, alternative='greater')
  
# выведите на экран полученное p-значение
p_value = results.pvalue
print('p-значение:', p_value)
  
# сравните p-значение с заданным уровнем значимости alpha
if p_value < alpha:
    print('Отвергаем нулевую гипотезу')
else:
    print('Нет оснований отвергнуть нулевую гипотезу')
```

## Задание 3.

Интернет-сервис разработал новую посадочную страницу (лендинг). По историческим данным, те посетители, которые затем что-то купили, в среднем просматривали 4.5 блока старого лендинга.

Первые несколько блоков лендинга изменили согласно новому гайдлайну. Может быть, новый дизайн помог пользователям быстрее принимать решение о покупке? Проверьте, так ли это: в этом случае количество проскролленных блоков сократится.

В приведённом наборе данных `screens` — количество просмотренных блоков нового лендинга пользователями, совершившими покупку.

```python
from scipy import stats as st
  
screens=[4, 2, 4, 5, 5, 4, 2, 3, 3, 5, 2, 5, 2, 2, 2, 3, 3, 4, 8, 3,
4, 3, 5, 5, 4, 2, 5, 2, 3, 7, 5, 5, 6, 5, 3, 4, 3, 6, 3, 4, 4, 3, 5,
4, 4, 8, 4, 7, 4, 5, 5, 3, 4, 6, 7, 2, 3, 6, 5, 6, 4, 4, 3, 4, 6, 4,
4, 6, 2, 6, 5, 3, 3, 3, 4, 5, 3, 5, 5, 4, 3, 3, 3, 1, 5, 4, 3, 4, 6,
3, 1, 3, 2, 7, 3, 6, 6, 6, 5, 5]
  
prev_screens = 4.5
  
alpha = 0.05
  
results = st.ttest_1samp(screens, prev_screens, alternative='less')
  
# выведите на экран полученное p-значение
p_value = results.pvalue
print('p-значение:', p_value)
  
# сравните p-значение с заданным уровнем значимости alpha
if p_value < alpha:
    print('Отвергаем нулевую гипотезу')
else:
    print('Нет оснований отвергнуть нулевую гипотезу')
```

# Робастное сравнение выборок. Практика

## **Шпаргалка:**

- `mannwhitneyu(a, b, alternative='two-sided')` — функция для проведения U-теста Манна-Уитни для двух независимых выборок, где `a` и `b` — списки данных для каждой из групп. Параметр `alternative` указывает тип альтернативной гипотезы (двусторонняя, только больше, только меньше).
- `alternative='two-sided'` — проверяет, есть ли различие между двумя группами в любом направлении.
- `alternative='less'` и `alternative='greater'` — проверяют, имеют ли данные первой выборки тенденцию быть меньше или больше данных второй выборки.

## Задание 1.

Компания «Web Innovate» провела редизайн своего сайта. Аналитический отдел компании собрал данные об удовлетворённости клиентов до изменений (`satisfaction_before`) и сразу после них (`satisfaction_after`). Предполагается, что удовлетворённость до редизайна, скорее всего, была меньше, чем после него. Медианные значения совпали.

Ваша задача — проанализировать эти данные и определить, привёл ли редизайн к значительному улучшению клиентского опыта, исходя из уровня значимости `0.05`.

```python
from scipy.stats import mannwhitneyu
  
satisfaction_before = [ 5, 2, 4, 4, 3, 4, 5, 5, 4, 4,
      4, 4, 5, 5, 2, 4, 6, 3, 4, 5, 4, 5, 4, 5, 4, 4,
      3, 4, 4, 3, 4, 4, 3, 3, 4, 3, 7, 4, 2, 2, 6, 5,
      3, 4, 5, 4, 3, 4, 3, 5 ]
satisfaction_after =[2, 5, 4, 6, 4, 3, 4, 4, 5, 5, 4,
      5, 6, 5, 4, 5, 3, 5, 6, 4, 4, 1, 2, 5, 4, 1, 5,
      3, 4, 4, 5, 6, 4, 6, 5, 5, 4, 2, 4, 4, 5, 4, 5,
      5, 3, 6, 5, 5, 5, 4]
  
stat, p_value = mannwhitneyu(satisfaction_before, satisfaction_after, alternative='less')
print(f"Статистика U-теста: {stat}, p_value: {p_value}")
```

## Задание 2.

Спустя месяц после редизайна команда «Web Innovate» продолжает следить за реакцией клиентов, собрав новые данные.

Исследуйте, как изменялась удовлетворённость клиентов на разных этапах после редизайна, анализируя данные из трёх временных точек: перед редизайном (`satisfaction_before`), сразу после редизайна (`satisfaction_after`) и через месяц после редизайна (`satisfaction_month_after`).

Проанализируйте и сравните уровни удовлетворённости между данными перед редизайном и данными через месяц после редизайна, а также между данными непосредственно после редизайна и данными через месяц. Это позволит оценить устойчивость изменений в удовлетворённости клиентов.

```python
from scipy.stats import mannwhitneyu
  
satisfaction_before = [ 5, 2, 4, 4, 3, 4, 5, 5, 4, 4,
      4, 4, 5, 5, 2, 4, 6, 3, 4, 5, 4, 5, 4, 5, 4, 4,
      3, 4, 4, 3, 4, 4, 3, 3, 4, 3, 7, 4, 2, 2, 6, 5,
      3, 4, 5, 4, 3, 4, 3, 5 ]
satisfaction_after =[2, 5, 4, 6, 4, 3, 4, 4, 5, 5, 4,
      5, 6, 5, 4, 5, 3, 5, 6, 4, 4, 1, 2, 5, 4, 1, 5,
      3, 4, 4, 5, 6, 4, 6, 5, 5, 4, 2, 4, 4, 5, 4, 5,
      5, 3, 6, 5, 5, 5, 4]
satisfaction_month_after =[4, 6, 2, 5, 4, 5, 6, 3, 5,
      5, 3, 6, 3, 5, 5, 3, 3, 7, 5, 5, 4, 7, 4, 4, 3,
      5, 5, 6, 5, 3, 5, 4, 3, 3, 4, 5, 3, 4, 4, 4, 4,
      5, 5, 5, 5, 4, 3, 7, 5, 3]
  
# Сравнение удовлетворённости до и через месяц после редизайна
stat_before_month, p_value_before_month = mannwhitneyu(satisfaction_before, satisfaction_month_after, alternative='two-sided')
print(f"Статистика U-теста между ДО и МЕСЯЦ ПОСЛЕ: {stat_before_month}, p_value: {p_value_before_month}")
  
# Сравнение удовлетворённости сразу после и через месяц после редизайна
stat_after_month, p_value_after_month = mannwhitneyu(satisfaction_after, satisfaction_month_after, alternative='two-sided')
print(f"Статистика U-теста между ПОСЛЕ и МЕСЯЦ ПОСЛЕ: {stat_after_month}, p_value: {p_value_after_month}")
```

## Задание 3.

В предыдущей задаче мы получили неоднозначный результат.

Уровень удовлетворённости, повышенный сразу после внедрённых изменений, сохраняется и спустя месяц (между «после» и «месяц после» нет разницы), подтверждая устойчивость положительного эффекта.

При этом уровни удовлетворённости до редизайна и месяц после также не различаются достаточно значимым образом, что говорит скорее об отсутствии эффекта от редизайна.

Теперь команда «Web Innovate» направила усилия на оценку совокупного влияния редизайна. Для этого сотрудники объединили данные об удовлетворённости клиентов сразу после редизайна (`satisfaction_after`) и данные, собранные через месяц после редизайна (`satisfaction_month_after`), в одну группу. Этот анализ поможет сравнить уровень удовлетворённости до внесения изменений (`satisfaction_before`) с удовлетворённостью после редизайна, учитывая как непосредственные, так и долгосрочные эффекты изменений.

```python
from scipy.stats import mannwhitneyu
  
satisfaction_before = [5, 2, 4, 4, 3, 4, 5, 5, 4, 4,
                       4, 4, 5, 5, 2, 4, 6, 3, 4, 5,
                       4, 5, 4, 5, 4, 4, 3, 4, 4, 3,
                       4, 4, 3, 3, 4, 3, 7, 4, 2, 2,
                       6, 5, 3, 4, 5, 4, 3, 4, 3, 5]
  
satisfaction_after = [2, 5, 4, 6, 4, 3, 4, 4, 5, 5,
                      4, 5, 6, 5, 4, 5, 3, 5, 6, 4,
                      4, 1, 2, 5, 4, 1, 5, 3, 4, 4,
                      5, 6, 4, 6, 5, 5, 4, 2, 4, 4,
                      5, 4, 5, 5, 3, 6, 5, 5, 5, 4]
  
satisfaction_month_after = [4, 6, 2, 5, 4, 5, 6, 3, 5,
                            5, 3, 6, 3, 5, 5, 3, 3, 7,
                            5, 5, 4, 7, 4, 4, 3, 5, 5,
                            6, 5, 3, 5, 4, 3, 3, 4, 5,
                            3, 4, 4, 4, 4, 5, 5, 5, 5,
                            4, 3, 7, 5, 3]
  
# Объединение данных сразу после редизайна и через месяц после редизайна
satisfaction_after_combined = satisfaction_after + satisfaction_month_after
  
# Сравнение данных до редизайна и объединённых данных после редизайна
stat, p_value = mannwhitneyu(satisfaction_before, satisfaction_after_combined, alternative='two-sided')
print(f"Статистика U-теста между данными до редизайна и после (объединённые): {stat}, p_value: {p_value}")
```

# Сравнение долей. Практика

Функция `proportions_ztest(count, nobs, alternative='two-sided')` используется для проведения Z-теста для сравнения долей (пропорций) успехов между двумя группами. Параметры функции:

- `count` — количество успехов в каждой из групп (может быть списком или массивом).
- `nobs` — общее количество наблюдений (попыток) в каждой группе.
- `alternative` — определяет альтернативную гипотезу: `'two-sided'` (различие между группами в любом направлении), `'smaller'` (первая группа имеет меньшую долю успехов) или `'larger'` (первая группа имеет большую долю успехов).

## Задание 1.

Образовательное учреждение внедрило изменения в одну из своих программ, добавив новые интерактивные методы обучения. Ваша задача — сравнить результаты экзаменов студентов до (`students_before`, `successes_before`) и после (`students_after`, `successes_after`) внедрения этих изменений, чтобы оценить, привели ли они к улучшению учебных результатов. Установите уровень значимости (`alpha`) `0.05`.

```python
from statsmodels.stats.proportion import proportions_ztest
  
# Данные по студентам
students_before = 150
successes_before = 60
  
students_after = 150
successes_after = 75
  
alpha = 0.05
z_stat, p_value = proportions_ztest(count=[successes_before, successes_after] , nobs=[students_before, students_after] , alternative='two-sided')
  
print(f"z-статистика: {z_stat}, p-value: {p_value}")
```

## Задание 2.

После первоначального анализа результатов студентов, вам необходимо оценить статистическую значимость наблюдаемых изменений. Определите, можно ли отвергнуть нулевую гипотезу о том, что изменения в учебной программе не повлияли на результаты студентов. Используйте конструкцию `if` для определения, можно ли на основе полученного `p_value` отвергнуть нулевую гипотезу о том, что изменения в учебной программе не повлияли на результаты студентов.

```python
from statsmodels.stats.proportion import proportions_ztest
  
# Данные по студентам
students_before = 150
successes_before = 60
  
students_after = 150
successes_after = 75
  
alpha = 0.05
  
z_stat, p_value = proportions_ztest([successes_before, successes_after],
                                    [students_before, students_after])
print("P-value:", p_value)
  
# Используйте if для проверки статистической значимости
if p_value < alpha:
    print("Отвергаем нулевую гипотезу: различия статистически значимы.")
else:
    print("Не отвергаем нулевую гипотезу: статистически значимых различий нет.")
```

> [!summary]
> На основе проведённого анализа, мы не нашли статистически значимых доказательств того, что внесённые изменения в учебную программу оказали значительное влияние на результаты экзаменов студентов. Это означает, что изменения не привели к улучшению или ухудшению успеваемости на уровне, который можно было бы считать статистически подтверждённым. Возможно, потребуется пересмотреть внедрённые методики или провести дополнительные исследования для определения других факторов, которые могли бы способствовать улучшению образовательных результатов.

## Задание 3.

Хотя результат предыдущего теста не показал статистически значимых изменений, возможно, эти изменения есть внутри различных групп студентов. Поэтому разделите студентов на группы по специализациям: технические (`students_tech_before`, `successes_tech_before` для данных до изменений и `students_tech_after`, `successes_tech_after` для данных после изменений) и гуманитарные (`students_human_before`, `successes_human_before` для данных до изменений и `students_human_after`, `successes_human_after` для данных после изменений).

Сравните, повлияли ли изменения в программе на эти специализации. Так мы сможем увидеть, какие группы извлекли пользу из нововведений или, возможно, пострадали от них. Уровень значимости задан как `0.05`.

```python
from statsmodels.stats.proportion import proportions_ztest
  
# Данные по техническим специализациям
students_tech_before= 79
successes_tech_before= 36
  
students_tech_after= 81
successes_tech_after= 50
  
# Данные по гуманитарным специализациям
students_human_before= 69
successes_human_before= 24
  
students_human_after= 71
successes_human_after= 26
  
alpha = 0.05
# Технические специализации
z_stat_tech, p_value_tech = proportions_ztest([successes_tech_before, successes_tech_after], [students_tech_before, students_tech_after])
print(f"Для технических специализаций:\n\tZ-статистика: {z_stat_tech}, P-value: {p_value_tech}")
if p_value_tech < alpha:
    print("\tОтвергаем нулевую гипотезу: различия статистически значимы.")
else:
    print("\tНе отвергаем нулевую гипотезу: статистически значимых различий нет.")
print()
  
# Гуманитарные специализации
z_stat_human, p_value_human = proportions_ztest([successes_human_before, successes_human_after], [students_human_before, students_human_after])
print(f"Для гуманитарных специализаций:\n\tZ-статистика: {z_stat_human}, P-value: {p_value_human}")
if p_value_human < alpha:
    print("\tОтвергаем нулевую гипотезу: различия статистически значимы.")
else:
    print("\tНе отвергаем нулевую гипотезу: статистически значимых различий нет.")
```

> [!summary]
> Изменения в учебной программе принесли значительные улучшения для студентов технических специализаций, как подтверждает p-value `0.04`. Это указывает на успешное внедрение нововведений, которые позитивно сказались на их учебных результатах. Однако для гуманитарных специализаций изменения не повлияли на результаты студентов, что отражено в p-value `0.82`.
> Между результатами до и после изменений нет статистически значимой разницы. Такие результаты могут использоваться учебным заведением для анализа и возможной корректировки учебных программ, особенно для гуманитарных дисциплин, где нужны дополнительные улучшения или адаптация подходов.

# Проверка связанных выборок. Практика

## **Шпаргалка**

Метод `scipy.stats.ttest_rel(before, after, alternative)` используется для проведения t-теста для сравнения связанных (парных) выборок. Аргументы метода:

- `before` — значения переменной в первой выборке;
- `after` — значения переменной во второй выборке (объекты, для которых измерена переменная, должны идти в том же порядке, что и в первой выборке, а размеры выборок должны совпадать).
- `alternative` — необязательный аргумент, он указывает направление альтернативной гипотезы:
    - `'two-sided'` (различие между выборками в любом направлении);
    - `'less'` (среднее совокупности, из которой взята выборка `before`, **меньше**, чем среднее совокупности, из которой взята выборка `after`);
    - `'greater'` (наоборот: среднее совокупности, из которой взята выборка `before`, **больше**, чем среднее совокупности, из которой взята выборка `after`).

## Задание 1.

Сотрудники логистической компании «Вжух» развозят грузы по городу. Недавно они внедрили способ распределения маршрутов и создали приложение с новой маршрутизацией. Теперь менеджер компании хочет проверить, больше ли грузов в неделю успевают развозить сотрудники с этим приложением.

Он взял данные по количеству заказов, которые 27 постоянных сотрудников компании успели доставить в течение семи дней до внедрения нового способа и в течение семи дней после. Наборы данных упорядочены по внутреннему айди-номеру сотрудников.

Помогите менеджеру проверить его гипотезу с уровнем значимости 5%.

```python
from scipy import stats as st
  
orders_in_7_days_before = [36, 30, 28, 28, 28, 29, 28, 29, 29, 31,
29, 32, 32, 28, 29, 31, 33, 32, 30, 24, 28, 36, 36, 27, 28, 30, 33]
  
orders_in_7_days_after = [24, 31, 29, 33, 34, 35, 36, 34, 34, 29,
32, 35, 30, 31, 36, 32, 31, 35, 36, 34, 35, 29, 29, 36, 31, 34, 37]
  
alpha = 0.05
  
results = st.ttest_rel(orders_in_7_days_before, orders_in_7_days_after, alternative='less')
  
# выведите на экран полученное p-значение
p_value = results.pvalue
print('p-значение:', p_value)
  
# сравните p-значение с заданным уровнем значимости alpha
if p_value < alpha:
    print('Отвергаем нулевую гипотезу: сотрудники стали развозить больше заказов')
else:
    print('Не получилось отвергнуть нулевую гипотезу: нет оснований полагать, что сотрудники стали развозить больше заказов')
```

## Задание 2.

Приведены два набора данных: время в секундах использования одними и теми же пользователями личного кабинета сайта до редизайна и после. Каждый пользователь в обоих наборах данных стоит на одном и том же месте (они упорядочены по user_id). Проверьте гипотезу, что после редизайна время использования личного кабинета на сайте изменилось.

Уровень статистической значимости стандартный — `0.05`.

```python
from scipy import stats as st
  
time_before = [1732, 1301, 1540, 2247, 1632, 1550, 754, 1946, 1889,
          2748, 1349, 1648, 1665, 2416, 1470, 1681, 1868, 1629,
          1271, 1633, 2131, 942, 1599, 1127, 2200, 661, 1207,
          1737, 2410, 1486]
  
time_after = [955, 2577, 360, 139, 1618, 990, 644, 1796, 1487, 949, 472,
         1906, 1758, 1258, 2554, 612, 309, 1864, 1294, 1487, 1164, 1559,
         491, 2286, 1270, 2069, 1553, 1629, 1704, 1623]
  
alpha = 0.05
  
results = st.ttest_rel(time_before, time_after)
  
# выведите на экран полученное p-значение
print('p-значение:', results.pvalue)
  
# сравните p-значение с заданным уровнем значимости alpha
if results.pvalue < alpha:
    print('Отвергаем нулевую гипотезу о равенстве среднего времени использования личного кабинета')
else:
    print('Не получилось отвергнуть нулевую гипотезу о равенстве среднего времени использования личного кабинета')
```

## Задание 3.

Разработчики компьютерной игры в жанре автогонок запустили новый тип соревнований, стимулирующий игроков к использованию нитро-ускорения, — опции, которая немного выдаётся игроку при регистрации и каждую неделю при заходе в игру, но дополнительно и продаётся. Нитро-ускорение продаётся поминутно, а уже во время гонки игрок может включить его на время.

У вас есть два датасета: в них количество минут нитро-ускорения, которые покупают одни и те же постоянные игроки, до и после изменения. Проверьте гипотезу о том, что после запуска нового типа соревнований количество продаваемых минут нитро-ускорения увеличилось.

Уровень статистической значимости стандартный — 5%.

```python
from scipy import stats as st
  
nitro_before = [821, 1164, 598, 854, 455, 1220, 161, 1400, 479, 215,
          564, 159, 920, 173, 276, 444, 273, 711, 291, 880,
          892, 712, 16, 476, 498, 9, 1251, 938, 389, 513]
  
nitro_after = [904, 220, 676, 459, 299, 659, 1698, 920, 514, 1086, 1499,
         1262, 829, 476, 1149, 996, 1047, 993, 1324, 532, 1458, 898,
         1837, 455, 1167, 898, 474, 558, 639, 812]
  
alpha = 0.05
  
results = st.ttest_rel(nitro_before, nitro_after, alternative='less')
  
# выведите на экран полученное p-значение
print('p-значение:', results.pvalue)
  
# сравните p-значение с заданным уровнем стат. значимости
if results.pvalue < alpha:
    print('Отвергаем нулевую гипотезу о равенстве средних покупок нитро в минутах')
else:
    print('Не получилось отвергнуть нулевую гипотезу о равенстве средних покупок нитро в минутах')
```

> [!summary]
> Похоже, новые соревнования стимулировали постоянных игроков покупать нитро.
> Но на уровне значимости `0.01` это уже было бы не так. Возможно, маркетингу стоит подумать и о других способах продвигать продажи.

# Множественные проверки. Практика

Если в рамках одного эксперимента вы проверяете больше одной гипотезы и статистически значимый результат хотя бы в одной из них означает значимый результат всего эксперимента — необходимо применять поправку на множественную проверку гипотез.

Самый простой вариант — поправка Бонферрони: разделить уровень значимости, выбранный для всего эксперимента, поровну на проверяемые гипотезы: αi=α/kαi​=α/k, где kk — количество проверяемых гипотез, αα — уровень статистической значимости всего эксперимента.

## Задание 1.

Маркетплейс онлайн-игр изменил систему поощрения блогеров, которые стримят самые популярные четыре игры. Средняя длина стрима с предыдущей системой поощрения — 79 минут.

Руководство маркетплейса хочет проверить гипотезу, что теперь средняя длина стрима увеличилась, — хотя бы для одной игры.

Проверьте эту гипотезу по приведённым длинам стримов этих четырёх игр популярными блогерами. Уровень значимости для всей проверки — `0.05`.

```python
from scipy import stats as st
  
game1_streams = [63, 77, 84, 79, 72, 82, 75, 63, 68, 62, 59, 79, 103, 71, 65, 75, 93, 79, 94, 83, 97, 64, 112, 93, 78, 79, 87, 79, 88, 69, 79, 59, 49, 78, 71, 61, 73, 75, 71, 78, 80, 82, 87, 83]
game2_streams = [71, 77, 65, 81, 87, 90, 58, 63, 60, 67, 77, 77, 84, 78, 84, 62, 74, 83, 85, 93, 55, 84, 69, 95, 69, 70, 85, 77, 76, 82, 58, 91, 65, 91, 72, 74, 81, 107]
game3_streams = [82, 88, 85, 81, 87, 78, 90, 77, 73, 74, 70, 95, 78, 89, 88, 113, 67, 91, 94, 107, 54, 101, 69, 83, 66, 85, 90, 79, 85, 76, 87, 94, 95, 83, 93, 71, 77, 88, 96, 91, 88, 80]
game4_streams = [79, 81, 78, 72, 85, 99, 95, 86, 87, 64, 79, 92, 84, 82, 87, 86, 95, 94, 65, 84, 78, 73, 69, 103, 85, 65, 73, 97, 71, 94, 86, 88, 77, 68, 57, 88, 85, 81, 86, 85, 81, 65, 79, 58, 70, 76, 87, 90]
  
usual_stream_time = 79
  
alpha = 0.05
  
alpha_i = alpha / 4
  
results1 = st.ttest_1samp(game1_streams, usual_stream_time, alternative='greater')
results2 = st.ttest_1samp(game2_streams, usual_stream_time, alternative='greater')
results3 = st.ttest_1samp(game3_streams, usual_stream_time, alternative='greater')
results4 = st.ttest_1samp(game4_streams, usual_stream_time, alternative='greater')
  
print("p-значение для 1 игры:", results1.pvalue)
print("p-значение для 2 игры:", results2.pvalue)
print("p-значение для 3 игры:", results3.pvalue)
print("p-значение для 4 игры:", results4.pvalue)
  
if results1.pvalue < alpha_i or results2.pvalue < alpha_i or results3.pvalue < alpha_i or results4.pvalue < alpha_i:
    print('Отвергаем нулевую гипотезу: длина стримов увеличилась')
else:
    print('Не получилось отвергнуть нулевую гипотезу: нет оснований полагать, что длина стримов увеличилась')
```

> [!summary]
> По полученным p-значениям видно, что для первых двух игр время стримов не увеличилось, но оно слабо увеличилось для 4-й игры и значимо для 3-й — что позволяет нам сделать вывод и об общем увеличении времени стримов для этих популярных игр.

## Задание 2.

Крупная производственная компания производит товары повседневного спроса. Менеджеров компании интересует какая часть жителей стран и городов, где присутствует компания, знает её бренды. Для этого они регулярно проводят бренд-лифт исследования. Бренд-лифт (англ. brand lift — “подтяжка” бренда, улучшение узнаваемости) — это исследование, которое позволяет оценить, как изменилась доля пользователей, которым знаком тот или иной бренд.

Компания провела в одном городе общую рекламную кампанию для всех 12 своих брендов, товары которых продаются в этом городе. До и после кампании проводилось исследование: вживую и в сети опрашивали жителей города, при этом исследователей интересовала доля респондентов, знакомых с брендами. Для каждого бренда были получены данные о пропорции знакомых с брендом до и после рекламной кампании.

Бренды пронумерованы от 1 до 12. Данные представлены в списках по порядку от бренда № 1 до бренда № 12:

- `sample_sizes_before`: сколько человек прошли опрос по каждому бренду до проведения рекламной кампании.
- `brand_recognition_before`: сколько человек оказались знакомы с брендами из прошедших опросов до проведения рекламной кампании.
- `sample_sizes_after`: сколько человек прошли опрос по каждому бренду после проведения рекламной кампании.
- `brand_recognition_after`: сколько человек оказались знакомы с брендами из прошедших опрос после проведения рекламной кампании.

Проверьте гипотезу менеджеров, что рекламная кампания сработала, с уровнем статистической значимости 5%. Если хотя бы один тест покажет значимый результат, нужно вывести строку `"Рекламная кампания успешна"`, иначе — `"Нужно больше рекламы богу рекламы"`. Выведите только одну из двух предлагаемых строк в зависимости от результата.

## Задание 

## Задание 