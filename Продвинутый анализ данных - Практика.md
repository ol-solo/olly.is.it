---
type: note
aliases:
  - "? Продвинутый анализ данных"
tags: []
parent: [[Python для анализа данных]]
next:
prev:
---
# Работа с файлами. Практика

Управление контекстом:

- `with open(filename, mode) as file:` — управляет контекстом для работы с файлами, автоматически закрывая файл после выполнения операций.

Параметры функции `open`:

- `mode='r'` — открывает файл в режиме чтения (read). Файл должен существовать, иначе будет вызвана ошибка.
- `mode='w'` — открывает файл в режиме записи (write). Если файл уже существует, его содержимое будет стерто. Если файл не существует, он будет создан.

После открытия файла, являющегося архивом, получить доступ к файлу внутри архива можно через метод `zipfile.ZipFile(filename, mode)`. Используйте режим `'r'` для чтения архива и `'w'` для его создания или перезаписи.

## Задание 1.

Дан текст и файл, в который этот текст нужно записать. Запишите текст в файл, используя оператор контекста `with`.

```python
FILE_NAME = 'new_file.txt'
text_table = '''
╔═════════╤═════════╗
║ \033[93m\033[1mМесяц\033[0m   │ \033[93m\033[1mПродажи\033[0m ║
╟─────────┼─────────╢
║ Январь  │   7_832 ║
╟─────────┼─────────╢
║ Февраль │   9_891 ║
╟─────────┼─────────╢
║ Март    │  11_203 ║
╚═════════╧═════════╝
'''
  
with open(FILE_NAME, mode='w') as file:
    file.write(text_table)
with open(FILE_NAME, mode='r') as file:
    print(file.read())
```

## Задание 2.

Прочитайте строки файла, путь к которому задан в константе FILE_PATH, удалите начальные и конечные переносы строк и пробелы и если в строке остались символы, запишите их количество в переменную `n_symbols` для вывода среднего количества символов в строке.

```python
FILE_PATH = '/datasets/wiki_extract.txt'
n_symbols = []
  
with open(FILE_PATH, 'r') as file:
    for line in file:
        clean = line.strip()
        if clean:
            n_symbols.append(len(clean))
  
print(round(sum(n_symbols)/len(n_symbols)))
```

## Задание 3.

Найдите в папке `/datasets/` все файлы, начинающиеся на `'megaline_'` и кончающиеся на `'.csv'` и запакуйте их в архив `'megaline_archive.zip'` методом `ZIP_DEFLATED`. Выведите полный путь к файлу из названия файла и пути к папке.

```python
import os
import zipfile
  
ZIPARCHIVE = 'megaline_archive.zip'
  
def percentation(number):
    if type(number) not in [int, float]:
        return "NaN"
    else:
        return str(round(number,3)*100)[:4] + "%"
data_dir = '/datasets/'
  
with zipfile.ZipFile('megaline_archive.zip', 'w', zipfile.ZIP_DEFLATED) as archive:
    for f in os.listdir(data_dir):
        if f.startswith('megaline_') and f.endswith('.csv'):
            full_path = os.path.join(data_dir, f)
            archive.write(full_path)
  
  
with zipfile.ZipFile(ZIPARCHIVE, 'r') as archive:
#    archive.printdir()
    print(*[file.filename + ":\t" + percentation(file.compress_size/file.file_size)
            for file in archive.infolist()],sep="\n")
```

# Работа с таблицами. Практика

## **Шпаргалка**

`pd.read_html(url, header=0)` — функция для извлечения таблиц из HTML-контента по указанному URL. Параметр `header=0` указывает, что первая строка таблицы содержит заголовки столбцов.

`df.to_excel('file.xlsx', index=False, sheet_name='Sheet1', engine='openpyxl')` — метод для сохранения DataFrame в файл Excel. Параметр `index=False` указывает не сохранять индексы строк, `sheet_name` задает имя листа, `engine='openpyxl'` необходим для сохранения стилей.

## Задание 1.

Вам нужно помочь аналитическому отделу компании собрать данные для анализа. Ваша задача — извлечь таблицы из официального документа на сайте «Консультант.ру», содержащие информацию о степенях защиты оборудования.

```python
import pandas as pd
  
document_topic = "Степень защиты пожарозащищенного электрооборудования"
document_url = "https://www.consultant.ru/document/cons_doc_LAW_78699/"
table1_id = "21dad0e85d9ed3d7519ed7e02caa9ed41f75f425/"
table2_id = "21edc7e83db0d519e94d68de3d447db923252108/"
  
try:
    df_table1 = pd.read_html(document_url + table1_id, header=0)[0]
    df_table2 = pd.read_html(document_url + table2_id, header=0)[0]
    for df in [df_table1, df_table2]:
        df.set_index(df.columns[0], inplace=True)
  
    with pd.option_context('display.max_colwidth', None):
        print(document_topic)
        print()
        print(df_table1)
        print()
        print('*' * 20)
        print(df_table2)
except Exception as e:
    print(f"Ошибка при получении данных: {e}")
```

## Задание 2.

Теперь, когда вы извлекли данные, необходимо сохранить их в формате xlsx для анализа. Ваша задача — сохранить DataFrame в файл Excel с именем `'fire_data.xlsx'`. Не сохраняйте индексы строк.

```python
import pandas as pd
  
document_topic = "Степень защиты пожарозащищенного электрооборудования"
document_url = "https://www.consultant.ru/document/cons_doc_LAW_78699/"
table1_id = "21dad0e85d9ed3d7519ed7e02caa9ed41f75f425/"
table2_id = "21edc7e83db0d519e94d68de3d447db923252108/"
  
try:
    df_table1 = pd.read_html(document_url + table1_id, header=0)[0]
    df_table2 = pd.read_html(document_url + table2_id, header=0)[0]
    for df in [df_table1, df_table2]:
        df.set_index(df.columns[0], inplace=True)
  
    col1, col2 = df_table1.columns[0], df_table2.columns[0]
    result = []
    for i in df_table1.index:
        for j in df_table2.index:
            result.append({
                "code": str(i) + str(j),
                "value1": df_table1.loc[i, col1],
                "value2": df_table2.loc[j, col2]
            })
    df_result = pd.DataFrame(result)
    df_result
  
    # Проверяем, что данные объединились
    df_result.info()
    print("*"*20)
    filename = 'fire_data.xlsx'
  
    # Выгружаем файл на диск
    df_result.to_excel(filename, index=False, sheet_name='data', engine='openpyxl')
  
    # Проверяем, загружается ли файл
    df = pd.read_excel(filename)
    print("Читаем содержимое с диска:")
    print(df.head())
  
except Exception as e:
    print(f"Ошибка при получении данных: {e}")
```

# Подготовка графики. Практика

В этом уроке вы попрактикуетесь в подготовке визуализаций к сохранению в виде отдельного файла.

Вы решите несколько задач:

- построите график месячной динамики изменения денежных вложений в основные источники привлечения пользователей развлекательного приложения;
- добавите оформление графика — заголовок, подписи по осям;
- сохраните визуализацию в виде отдельного графического файла формата PNG;
- создадите субплот с двумя графиками: месячной и недельной динамиками изменения затрат на рекламные источники — и сохраните его в виде графического файла в формате SVG.

Для построения графиков используйте библиотеку seaborn. При оформлении графика вам могут пригодиться такие методы и их аргументы:

|**Метод / аргумент**|**Описание**|
|---|---|
|plt.figure()|Создание контейнера для графика|
|plt.figure(figsize=())|Создание контейнера для графика заданного размера|
|plt.subplots()|Создание контейнера для нескольких графиков (субплотов)|
|plt.title()|Создание заголовка для графика|
|plt.xlabel() и plt.ylabel()|Создание подписей осей x и y|
|.set_title(), .set_xlabel() и .set_ylabel()|Применяются к элементу ax для создания заголовка субплота и подписей по осям|
|plt.xticks(rotation=)|Метод, позволяющий заменять оформление меток по осям, где аргумент rotation поворачивает подписи по оси x на переданный аргументу угол|
|plt.yticks(rotation=)|Метод, позволяющий заменять оформление меток по осям, где аргумент rotation поворачивает подписи по оси y на переданный аргументу угол|
|plt.grid()|Добавляет для графика сетку|
|plt.tight_layout()|Позволяет автоматически разместить элементы графиков между собой, чтобы они не накладывались друг на друга|
|plt.savefig()|Позволяет сохранить график в виде графического файла|

Для построения необходимых визуализаций используйте данные `costs_info_short.csv`, содержащие информацию о затратах на рекламные вложения по каждому рекламному источнику:

- `dt` — дата совершения затрат;
- `Channel` — название рекламного источника привлечения новых пользователей;
- `costs` — сумма затрат в условных единицах.

## Задание 1.

Изучите месячную динамику изменения затрат на привлечение новых пользователей по каждому рекламному источнику.

Для этого загрузите данные, добавьте дополнительный столбец с месяцем затрат, затем сгруппируйте данные для каждого источника и по месяцам, рассчитайте суммарные затраты.

При агрегации данных используйте методы `groupby` и `agg`. Это позволит создать длинную, или вертикальную, таблицу, которую будет удобно использовать при визуализации данных в seaborn. Агрегированные данные сохраните в переменной `m_result`.

Используя полученные данные, постройте линейный график динамики затрат, где по оси `x` отложите месяц затрат, а по оси `y` — общую сумму затрат по каждому рекламному источнику. Для построения графика используйте библиотеку seaborn. Полученную визуализацию выведите на экран.

В этом задании можно построить базовую версию визуализации — добавлять подписи по осям, заголовок графика и менять размер визуализации не нужно. Это вы сделаете в следующем задании.

```python
# Импортируем необходимые библиотеки:
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
  
# Загрузим данные:
costs = pd.read_csv('/datasets/costs_info_short.csv')
  
# Скорректируем тип данных в столбце с датой затрат:
costs['dt'] = pd.to_datetime(costs['dt'])
  
# Создадим столбец с месяцем затрат:
costs['month'] = costs['dt'].dt.month
  
# Сгруппируем данные и рассчитаем суммарные месячные затраты для каждого источника:
m_result = costs.groupby(['Channel', 'month']).agg({'costs': 'sum'}).reset_index()
  
# Выведем первые пять строк на экран:
print(m_result.head())
  
# Визуализируем результаты:
sns.lineplot(data=m_result, x='month', y='costs', hue='Channel')
  
plt.show()
```

## Задание 2.

Используя прекод, содержащий один из возможных вариантов кода графика из предыдущего задания, приведите оформление визуализации в порядок:

- Измените размер графика на 7 × 5 дюймов.
- В качестве заголовка укажите: «Месячная динамика изменения затрат развлекательного приложения на привлечение новых пользователей в разрезе рекламных каналов привлечения». Следите за тем, чтобы общее название графика не выходило за его пределы. Для этого используйте перенос слов на новую строку с помощью `\n`.
- В качестве подписей по осям укажите «Месяц» для оси `x` и «Сумма затрат, у.е.» для оси `y`.
- Установите шрифт заголовка графика 14-го кегля, а шрифт подписей по осям — 12-го кегля.
- Добавьте сетку графика.

Результат визуализации данных выведите на экран.

```python
# Импортируем необходимые библиотеки:
import pandas as pd
from matplotlib import pyplot as plt
import seaborn as sns
  
# Загрузим и подготовим данные:
costs = pd.read_csv('/datasets/costs_info_short.csv')
costs['dt'] = pd.to_datetime(costs['dt'])
costs['month'] = costs['dt'].dt.month
m_result = costs.groupby(['Channel','month']).agg({'costs':'sum'}).reset_index()
  
# Выведем первые пять строк на экран:
print(m_result.head())
  
# Визуализируем результаты:
plt.figure(figsize=(7, 5))
sns.lineplot(x='month',
             y='costs',
             hue='Channel',
             data=m_result)
  
plt.title('Месячная динамика изменения затрат\n'
          'развлекательного приложения на привлечение новых пользователей\n'
          'в разрезе рекламных каналов привлечения',
          fontsize=14)
  
plt.xlabel('Месяц', fontsize=12)
plt.ylabel('Сумма затрат, у.е.', fontsize=12)
plt.grid()
  
plt.show()
```

## Задание 3.

Сохраните визуализацию в отдельный файл с названием `m_costs` в растровом формате с сохранением прозрачных областей и с разрешением 300 точек на дюйм.

Обратите внимание: если график при сохранении будет обрезан, то используйте параметр метода `plt.savefig()` — `bbox_inches = "tight"`. Это позволит сохранить все элементы графика без обрезки по размеру стандартного контейнера.

```python
# Импортируем необходимые библиотеки:
import pandas as pd
from matplotlib import pyplot as plt
import seaborn as sns
  
# Загрузим и подготовим данные:
costs = pd.read_csv('/datasets/costs_info_short.csv')
costs['dt'] = pd.to_datetime(costs['dt'])
costs['month'] = costs['dt'].dt.month
m_result = costs.groupby(['Channel','month']).agg({'costs':'sum'}).reset_index()
  
# Выведем первые пять строк на экран:
print(m_result.head())
  
# Визуализируем результаты:
plt.figure(figsize=(7, 5))
sns.lineplot(x='month',
             y='costs',
             hue='Channel',
             data=m_result)
# Добавим оформление графика:
plt.title('Месячная динамика изменения затрат \nразвлекательного приложения на привлечение \nновых пользователей в разрезе \nрекламных каналов привлечения', fontsize=14)
plt.xlabel('Месяц', fontsize=12)
plt.ylabel('Сумма затрат, у.е.', fontsize=12)
  
# Добавим сетку графика:
plt.grid()
  
plt.savefig('m_costs.png', dpi=300, transparent=True, bbox_inches='tight')
  
plt.show()
```

## Задание 4.

Детализируем анализ данных о денежных затратах на рекламные каналы и добавим график с недельной динамикой. Для визуализации результатов используйте субплот, содержащий два графика: слева график недельной динамики, справа — месячной динамики. Для решения задачи сначала рассчитайте недельные затраты по каждому источнику и сохраните их в переменной `w_result`.

Затем по аналогии с графиком месячной динамики постройте график с недельной. Два графика оформите в виде одной визуализации. При создании визуализации используйте следующие значения:

- Размер визуализации: 12 на 8 дюймов.
- Шрифт заголовка всех графиков — 14-го кегля, а шрифт подписей по осям — 12-го кегля.
- Для графика с недельной динамикой в качестве заголовка укажите: «Недельная динамика изменения затрат развлекательного приложения на привлечение новых пользователей в разрезе рекламных каналов привлечения». Следите за тем, чтобы общее название графика не выходило за его пределы. Для этого используйте перенос слов на новую строку с помощью `\n`. В качестве подписей по осям укажите «Неделя» для оси `x` и «Сумма затрат, у.е.» для оси `y`.
- В качестве графика с месячной динамикой используйте график из предыдущего задания.
- Итоговую визуализацию сохраните в формате `svg` с названием `costs`.

При построении визуализации следите, чтобы элементы отдельных графиков не накладывались друг на друга.

```python
# Импортируем необходимые библиотеки:
import pandas as pd
from matplotlib import pyplot as plt
import seaborn as sns
  
# Загрузим и подготовим данные:
costs = pd.read_csv('/datasets/costs_info_short.csv')
costs['dt'] = pd.to_datetime(costs['dt'])
  
# Создадим столбец с месяцем затрат:
costs['month'] = costs['dt'].dt.month
  
# Создадим столбец с номером недели затрат:
costs['week'] = costs['dt'].dt.isocalendar().week
  
# Сгруппирируем данные по месяцу:
m_result = costs.groupby(['Channel','month']).agg({'costs':'sum'}).reset_index()
  
# Сгруппирируем данные по номеру недели:
w_result = costs.groupby(['Channel', 'week']).agg({'costs':'sum'}).reset_index()
  
# Создание фигуры и осей для сабплотов
fig, ax = plt.subplots(1, 2, figsize=(12, 8))
  
# График недельной динамики:
sns.lineplot(
    data=w_result,
    x='week',
    y='costs',
    hue='Channel',
    ax=ax[0]
)
  
# Добавим форматирование графика:
ax[0].set_title(
    'Недельная динамика изменения затрат\n'
    'развлекательного приложения на привлечение\n'
    'новых пользователей в разрезе\n'
    'рекламных каналов привлечения',
    fontsize=14
)
ax[0].set_xlabel('Неделя', fontsize=12)
ax[0].set_ylabel('Сумма затрат, у.е.', fontsize=12)
ax[0].grid()
  
# График месячной динамики:
sns.lineplot(
    data=m_result,
    x='month',
    y='costs',
    hue='Channel',
    ax=ax[1]
)
  
# Добавим форматирование графика:
ax[1].set_title(
    'Месячная динамика изменения затрат\n'
    'развлекательного приложения на привлечение\n'
    'новых пользователей в разрезе\n'
    'рекламных каналов привлечения',
    fontsize=14
)
ax[1].set_xlabel('Месяц', fontsize=12)
ax[1].set_ylabel('Сумма затрат, у.е.', fontsize=12)
ax[1].grid()
  
# Включим автоматическое распределение элементов внутри контейнера:
plt.tight_layout()
  
# Сохраним визуализацию в графический файл:
plt.savefig('costs.svg', format='svg', bbox_inches='tight')
  
plt.show()
```

# Формат данных JSON. Практика

## **Шпаргалка**

- Для преобразования JSON-строки обратно в объект Python используется функция `json.loads()`.
- Для преобразования Python-объекта в JSON-строку используйте `json.dumps()`.

## Задание 1.

У вас есть данные о квартирах в виде вложенного списка, где каждая квартира представлена как словарь с ключами `id`, `address`, `total_area`, и `last_price`. Необходимо преобразовать эти данные в JSON-строку.

```python
# Импортируем библиотеку
import json
  
# Данные о квартирах
real_estate_data = [
    {
        "id": 1,
        "address": "1 Lenin St",
        "total_area": 56,
        "last_price": 5000000
    },
    {
        "id": 2,
        "address": "2 Pushkin St",
        "total_area": 74,
        "last_price": 7500000
    },
    {
        "id": 3,
        "address": "3 Chekhov St",
        "total_area": 65,
        "last_price": 6200000
    }
]
  
real_estate_json = json.dumps(real_estate_data)
print(real_estate_json)
```

## Задание 2.

У вас есть JSON-строка, содержащая данные о квартирах, где каждая жилплощадь имеет вложенные словари с информацией о владельце и характеристиках. Необходимо преобразовать эту JSON-строку в объект Python (список словарей) и вывести информацию о каждом владельце.

```python
# Импортируем библиотеки
import json
  
import pandas as pd
  
# JSON-строка с данными о квартирах
real_estate_json = '''
[
    {
        "id": 1,
        "address": "1 Lenin St",
        "total_area": 56,
        "last_price": 5000000,
        "owner": {
            "name": "John Smith",
            "age": 45,
            "contacts": {
                "phone": "+1234567890",
                "email": "john.smith@example.com"
            }
        }
    },
    {
        "id": 2,
        "address": "2 Pushkin St",
        "total_area": 74,
        "last_price": 7500000,
        "owner": {
            "name": "Peter Johnson",
            "age": 38,
            "contacts": {
                "phone": "+0987654321",
                "email": "peter.johnson@example.com"
            }
        }
    },
    {
        "id": 3,
        "address": "3 Chekhov St",
        "total_area": 65,
        "last_price": 6200000,
        "owner": {
            "name": "Mary Davis",
            "age": 32,
            "contacts": {
                "phone": "+1122334455",
                "email": "mary.davis@example.com"
            }
        }
    }
]
'''
  
# Напишите ваш код здесь
real_estate_data = json.loads(real_estate_json)
  
# Извлекаем информацию
# Напишите ваш код здесь
extracted_data = []
for apartment in real_estate_data:
    owner = apartment['owner']
    contacts = owner['contacts']
    extracted_data.append({
        "id": apartment['id'],
        "address": apartment['address'],
        "total_area": apartment['total_area'],
        "last_price": apartment['last_price'],
        "owner_name": owner['name'],
        "owner_age": owner['age'],
        "owner_phone": contacts['phone'],
        "owner_email": contacts['email']
    })
  
df = pd.DataFrame(extracted_data)
print(df)
```

# Принципы работы с API. Практика

## **Шпаргалка**

Определение URL и параметров запроса:

```
url = "http://universities.hipolabs.com/search"
params = {"country": "Russian Federation"} 
```

Отправление GET-запроса и получение ответа:

```
response = requests.get(url, params=params) 
```

Преобразование ответа в формат JSON и создание DataFrame:

```
data_json = response.json()
df = pd.DataFrame(data_json) 
```

## Задание 
Вам необходимо получить данные по API об университетах в Австралии (в параметрах укажите `Australia`). Выведите первые пять строк DataFrame для оценки полученных данных.

```python
import requests
import pandas as pd
  
# URL API для получения данных об университетах:
url = "http://universities.hipolabs.com/search"
params = {"country": "Australia"}
  
# Отправка GET-запроса:
response = requests.get(url, params=params)
  
# Проверка успешности запроса:
if response.status_code == 200:
    data = response.json()
    # Преобразование JSON-ответа в DataFrame:
    df = pd.DataFrame(data)
    # Вывод первых строк DataFrame:
    print(df.head())
else:
    print(f"Ошибка: {response.status_code}")
```

# Загрузка данных из базы SQL. Практика

## **Шпаргалка**

Установка параметров подключения:

```
db_config = {
    'user': 'praktikum_student', # имя пользователя
    'pwd': 'Sdf4$2;d-d30pp', # пароль
    'host': 'rc1b-wcoijxj3yxfsf3fs.mdb.yandexcloud.net',
    'port': 6432, # порт подключения
    'db': 'data-analyst-final-project-db' # название базы данных
} 
```

Создание строки подключения:

```
connection_string = 'postgresql://{user}:{pwd}@{host}:{port}/{db}'\
                        .format(**db_config) 
```

Создание коннектора:

```
engine = create_engine(connection_string, 
                       connect_args={'sslmode':'require'}) 
```

Выполнение SQL-запроса и выгрузка данных:

`pd.read_sql()` — это функция pandas, которая выполняет SQL-запрос к базе данных и загружает результаты в DataFrame.

`sql=text(query)` — здесь создается объект SQLAlchemy `text`, который представляет собой SQL-запрос в виде строки.

# Массаж данных. Практика

В этом уроке вы потренируетесь переводить данные из одного формата в другой, используя методы `.melt()`, `.stack()` и `.unstack()`.

## Задание 1.

Для анализа профиля пользователя мобильного приложения поступили данные в виде датасета длинного формата — `p4da_mobile_long.csv`. Он состоит из таких столбцов:

- `user_id` — идентификатор пользователя;
- `variable` — признак для каждого пользователя: `first_ts` — время первого посещения, `channel` — канал привлечения, `device` — устройство входа, `region` — регион регистрации;
- `value` — значение признака.

С помощью метода `unstack` переведите данные из формата длинной таблицы в формат широкой, чтобы в одной строке были собраны все признаки для одного пользователя. После создания широкого датафрейма приведите индекс столбцов к одному уровню и сбросьте индекс строк.

```python
# Загрузим необходимые библиотеки:
import pandas as pd
  
# Загрузим исходные данные:
df = pd.read_csv('https://code.s3.yandex.net/datasets/p4da_mobile_long.csv')  
  
# Изменим индекс датафрейма с помощью метода .set_index():
df = df.set_index(['user_id', 'variable'])
  
# Создадим широкий датафрейм на основе длинного:
df_wide = df.unstack()
  
# Удалим верхний уровень индекса столбцов и сбросим название уровня:
df_wide.columns = df_wide.columns.droplevel()
  
# Сбросим индекс на порядковый номер строки:
df_wide = df_wide.reset_index()
  
# Выведем на экран первые пять строк:
print(df_wide.head())
```

## Задание 2.

Вам нужно проанализировать данные по маркетинговым затратам на рекламные источники. Коллеги прислали сводную таблицу `p4da_costs_pivot.csv`. Она содержит информацию об источниках привлечения и расходы за предыдущий период на первое число каждого месяца.

Преобразуйте данные в длинную таблицу, содержащую столбцы `source_id`, `month` и `costs`, и подсчитайте суммарные затраты на каждый источник. Для преобразования данных в длинную таблицу используйте метод `.stack()`, а для подсчёта значений —группировку данных с помощью методов `.groupby()` и `.agg()`. Результат выведите на экран.

```python
# Загрузим необходимые библиотеки:
import pandas as pd
  
# Загрузим исходные данные:
df = pd.read_csv('https://code.s3.yandex.net/datasets/p4da_costs_pivot.csv')  
  
# Изменим индекс датафрейма с помощью метода .set_index():
df = df.set_index('source_id')
  
# Преобразуем в длинную таблицу:
df_long = df.stack()
  
# Сбросим индекс:
df_long = df_long.reset_index()
  
# Зададим новые названия столбцам:
df_long.columns = ['source_id','month','costs']
  
# Подсчитаем затраты на каждый источник:
result = df_long.groupby('source_id').agg({'costs' : 'sum'})
  
# Выведем результат на экран:
print(result)
```

## Задание 3.

Потренируйтесь преобразовывать широкую таблицу в длинную при помощи метода `.melt()`. Загрузите данные из датасета `/datasets/support_data.csv` с информацией о работе службы поддержки банка.

Необходимо подготовить данные для дальнейшей визуализации в системах BI. Измените формат данных на длинную таблицу, содержащую такие столбцы:

- `customer_id` — идентификатор пользователя;
- `segment` — сегмент пользователя;
- `variable` — названия признака;
- `value` — значение признака.

Результат сохраните в датафрейме `df_long` и выведите первые пять строк на экран.

```python
import pandas as pd
  
df = pd.read_csv('/datasets/support_data.csv')
  
df_long = df.melt(id_vars=['customer_id', 'segment'],
                  var_name='variable',
                  value_name='value'
)
  
# Выведем результат на экран:
print(df_long.head())
```

# Объединение таблиц. Практика

Разберём на практике особенности соединения таблиц на данных по А/В-тесту интернет-магазина. С похожими данными вы работали, когда изучали результаты А/В-теста, но тогда данные были подготовлены к анализу. В этом уроке данные не подготавливались и распределены между тремя датасетами.

Первый датасет `/datasets/final_ab_new_users.csv` содержит информацию о новых пользователях, использующих приложение:

- `user_id`: идентификатор пользователя;
- `first_date`: дата регистрации;
- `region`: регион регистрации;
- `device`: устройство регистрации.

Второй датасет `/datasets/final_ab_participants.csv` содержит информацию об участниках теста:

- `user_id`: идентификатор пользователя;
- `group`: группа эксперимента;
- `ab_test`: название эксперимента.

Третий датасет `/datasets/final_ab_events.csv` содержит информацию о действиях всех пользователей:

- `user_id`: идентификатор пользователя;
- `event_dt`: время события;
- `event_name`: название события;
- `details`: информация о событии.

На этом наборе данных решим несколько задач, используя различные стили объединения таблиц. Будет несколько задач на использование метода `.merge()` и одна задача на метод `.join()`.

Вспомним, что объединения нескольких таблиц могут быть разными. Выделяют левое `left`, правое `right` объединения, полное `outer` и внутреннее — `inner`. Метод `.merge()` по умолчанию использует внутреннее объединение, а `.join()` — левое. В двух методах тип объединения задается параметром `how`. Датафрейм, к которому применяется один из методов будет левым, а присоединяемые данные — правым.

Используя метод `.merge()` необходимо указать название столбца или столбцов, по которым будет происходить объединение. Для этого следует использовать параметр `on`. Если ключевые значения имеют разное название в двух датафреймах, тогда эти названия указывают для каждого датафрейма отдельно, используя параметры `left_on` и `right_on`.

## Задание  1.

Создайте общую таблицу, которая будет содержать данные о всех пользователях приложения и информацию об участии их в А/В-тестах. Подсчитайте, какая доля пользователей из региона `EU` участвовала во всех А/В тестах. Для выполнения задания используйте описание данных, метод `.merge()` и прекод. Необходимые данные, ключевые столбцы для объединения данных и стили объединений выберите сами.

```python
# Загрузим нужные библиотеки:
import pandas as pd
  
# Загрузим данные по всем пользователям:
all_users = pd.read_csv('/datasets/final_ab_new_users.csv')
  
# Загрузим данные по участникам теста:
test_users = pd.read_csv('/datasets/final_ab_participants.csv')
  
# Объединим данные в один датафрейм:
merged_df = all_users.merge(
    test_users,
    on='user_id',
    how='left'
)
  
# Найдем общее количество пользователей из региона `EU`:
total_EU_users = merged_df.query("region == 'EU'")['user_id'].nunique()
  
# Найдем количество пользователей из региона `EU`, участвующих в тестах:
test_EU_users = merged_df.query("region == 'EU' & ab_test.notna()")['user_id'].nunique()
  
# Найдем долю пользователей из региона `EU`, участвующих в тестах:
share_EU_users = test_EU_users / total_EU_users
  
# Выведем результат на экран:
print(f'Доля пользователей из региона EU, участвующих в тесте: {share_EU_users:.2%}')
```

## Задание 2.

К данным из предыдущей задачи (используйте прекод) присоедините данные по событиям пользователей, которые участвовали в тестах. При этом оставьте только тех пользователей, которые имеют хотя бы одно событие. В конце подсчитайте долю пользователей из региона `EU`, участвующих в тесте и имеющих хотя бы одно событие.

Для выполнения задания используйте описание данных, метод `.merge()` и прекод. Необходимые данные, ключевые столбцы для объединения данных и стили объединений выберите сами.

```python
# Загрузим нужные библиотеки:
import pandas as pd
  
# Код из предыдущего задания:
# Загрузим данные по всем пользователям:
all_users = pd.read_csv('https://code.s3.yandex.net/datasets/final_ab_new_users.csv')  
# Загрузим данные по участникам теста:
test_users = pd.read_csv('https://code.s3.yandex.net/datasets/final_ab_participants.csv')
  
# Объединим данные о пользователях в один датафрейм:
merged_df = all_users.merge(test_users, on='user_id', how='left')
  
# Найдем долю пользователей из региона `EU`, участвующих в тестах:
total_EU_users = merged_df.query('region == "EU"')['user_id'].nunique()
test_EU_users = merged_df.query('ab_test == ab_test and region == "EU"')['user_id'].nunique()
share_EU_users = test_EU_users / total_EU_users
  
# Загрузим данные с пользовательскими событиями:
events = pd.read_csv('https://code.s3.yandex.net/datasets/final_ab_events.csv')
  
# Создадим срез данных с пользователями, участвующих в тестах, используя merged_df:
merged_df_test = merged_df.query('ab_test == ab_test')
  
# Объединим данные об участниках теста и их события в один датафрейм:
events_joined = merged_df_test.merge(events, on='user_id', how='inner')
  
# Найдем общее количество пользователей из региона `EU`, совершивших события:
EU_users_with_events = events_joined.query('region == "EU"')['user_id'].nunique()
  
# Найдем долю пользователей, участвующих в тестах  из региона `EU` и совершивших события:
share_EU_users_with_events = EU_users_with_events / total_EU_users
  
# Выведем результат на экран:
print(f'Доля пользователей из региона EU, участвующих в тесте: {share_EU_users:.2%}')
print(f'Доля активных пользователей из региона EU, участвующих в тесте: {share_EU_users_with_events:.2%}')
```

## Задание 3.

В предыдущих заданиях вы отлично справились с задачами, используя метод `.merge()` для последовательного объединения данных трёх таблиц. Теперь потренируйтесь объединять несколько датасетов с помощью `.join()`.

Согласно данным найдите пользователей из региона `EU`, не совершивших ни одно событие. Выделите информацию об этих пользователей в датафрейм `non_active_users`. При объединении таблиц используйте метод `.join()`. Для решения задачи используйте прекод.

```python
# Загрузим нужные библиотеки:
import pandas as pd
  
# Загрузим данные:
all_users = pd.read_csv('https://code.s3.yandex.net/datasets/final_ab_new_users.csv')  
test_users = pd.read_csv('https://code.s3.yandex.net/datasets/final_ab_participants.csv')
events = pd.read_csv('https://code.s3.yandex.net/datasets/final_ab_events.csv')
  
# Создадим один общий датафрейм:
joined_df = joined_df = all_users.set_index('user_id').join(
    test_users.set_index('user_id'),
    how='left'
).join(
    events.set_index('user_id'),
    how='left'
)
  
# Сбросим индекс итоговой таблицы:
joined_df = joined_df.reset_index()
  
# Создадим срез данных:
non_active_users = joined_df.query('region == "EU" & event_name != event_name')
  
# Найдем количество неактивных пользователей:
non_active_users_len = non_active_users['user_id'].nunique()
print('Количество неактивных пользователей:', non_active_users_len )
```

# Пропуски временного ряда. Практика

## **Шпаргалка**

Вот несколько основных способов борьбы с пропусками во временных рядах:

- Заполнение средним значением или медианой. Для этого используйте метод `fillna()`.
- Заполнение последним или первым непустым значением:
    - `ffill()` заполнит последним непустым,
    - `bfill()` — первым непустым.
- Интерполяция с использованием полиномов первой, второй и других степеней. В этом поможет метод `interpolate()`:
    - `method` — linear или polynomial, метод интерполяции,
    - `order` — степень полинома.

## Задание 1.

Перед вами данные о количестве посещений сайта за последние две недели. Но в системе произошёл сбой и данные за некоторые дни оказались недоступны. Ваша задача — заполнить пропущенные значения.

Заполните пропуски средним значением и сохраните их в колонке `filled_value`.

```python
import pandas as pd
import numpy as np
from matplotlib import pyplot as plt
  
data = [
    {'date': '2024-05-01', 'value': 9530},
    {'date': '2024-05-02', 'value': 9420},
    {'date': '2024-05-03', 'value': np.nan},
    {'date': '2024-05-04', 'value': 9540},
    {'date': '2024-05-05', 'value': 9630},
    {'date': '2024-05-06', 'value': 9710},
    {'date': '2024-05-07', 'value': np.nan},
    {'date': '2024-05-08', 'value': 9357},
    {'date': '2024-05-09', 'value': 9214},
    {'date': '2024-05-10', 'value': 9402},
    {'date': '2024-05-11', 'value': np.nan},
    {'date': '2024-05-12', 'value': 9457},
    {'date': '2024-05-13', 'value': 9824},
    {'date': '2024-05-14', 'value': 9495}
]
  
df = pd.DataFrame(data)
  
# Ваш код
df['filled_value'] = df['value'].fillna(df['value'].mean())
  
# Линия без пропусков
plt.plot(df['date'], df['filled_value'], color='r', marker='o', markersize=5)
# Линия с пропусками
plt.plot(df['date'], df['value'], color='blue', marker='o', markersize=5)
  
plt.xticks(rotation=30)
plt.grid(True)
plt.legend(['filled', 'raw'])
  
plt.show()
```

## Задание 

## Задание 

## Задание 

## Задание 

## Задание 

## Задание 

## Задание 

## Задание 

## Задание 

## Задание 

## Задание 